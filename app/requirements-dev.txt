# Curator dev/test-only dependencies — NOT installed into the runtime image.
# respx mocks httpx so adapter tests run fully offline (no live Lidarr/Readarr).
# Versions human-approved as pinned (Phase 2 package-legitimacy checkpoint, 2026-05-30).
pytest
respx==0.22.0        # lundberg/respx — httpx MockTransport backend; approved compatible with httpx 0.28.x.
