# Curator dev/test-only dependencies — NOT installed into the runtime image.
# respx mocks httpx so adapter tests run fully offline (no live Lidarr/Readarr).
# Versions to confirm on first CI build (network present there) — see blocking checkpoint.
pytest
respx==0.22.0        # httpx MockTransport backend; pins to an httpx range — verify compat with httpx 0.28.x.
