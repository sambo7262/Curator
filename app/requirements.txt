fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1        # *arr REST client (Servarr v1 paged GET). Verify exact 0.28.x on first CI build (RESEARCH Package Audit; gated by blocking checkpoint).
