fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1        # *arr REST client (Servarr v1 paged GET). encode/httpx — human-approved as pinned (Phase 2 package-legitimacy checkpoint, 2026-05-30).
