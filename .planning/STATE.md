---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: Executing Phase null
last_updated: "2026-05-31T19:18:38.111Z"
progress:
  total_phases: 6
  completed_phases: 4
  total_plans: 23
  completed_plans: 18
  percent: 67
---

# Project State

<!-- GSD reads this file first to understand where the project is. Keep it current. -->

## Current Status

**Phase**: 5 - Autonomy, Sharing & Self-Recovery — IN PROGRESS (Wave 0: 05-01 ✓; Phase 4 ✓ COMPLETE)
**Plan**: 05-01 ✓ COMPLETE (1 of 5 in Phase 5) — Wave-0 persistence + config foundation landed
**Last action (05-01)**: Executed Phase 5 plan 05-01 sequentially on main (4 atomic commits: `84d9bb2` test scaffolds + conftest fixtures, `a1d6f4d` migration_0003 + db.py registration, `9f8e2a1` repo eligibility/backoff/attempt/counts DAOs + repo tests, `7b3c9d4` config Phase-5 tunables). migration_0003 adds attempt_count (NOT NULL DEFAULT 0) / next_attempt_at / last_checked_at + the `permanently-unavailable` status via an explicit-column-list table rebuild that preserves every existing row (the live ~1493-gap NAS ledger survives intact, proven by the v0002→v0003 preservation test on the conftest `seed_v0002_ledger` harness). repo.py gains `select_eligible` (two-branch grace+backoff+dormant predicate, ORDER BY discovered_at ASC, LIMIT room — GAP-03/D-08/D-09, all `?`-bound, firewall-clean), `record_attempt`, `status_counts`, `imported_recent`, `backoff_for` (1h/6h/24h capped) + `BACKOFF_SECONDS`. config.py gains the 7 Phase-5 tunables (acq_enabled/acq_dry_run/max_concurrent=3/acq_poll_interval_seconds=21600/acq_grace_seconds=259200/acq_max_attempts=3/acq_dormant_seconds=2592000), env-overridable with fail-fast casts, no secrets (D-13). 27 new Phase-5 tests green (test_migration_0003 + test_eligibility + test_backoff + repo/config cases in test_state_repo); firewall (test_adapter_protocol) clean over state/. NOTE: the impl was found pre-written + UNCOMMITTED in the working tree from a prior interrupted run — this execution verified all acceptance criteria, then committed the 4 tasks atomically. 1 deferred (out-of-scope) item logged: 2 pre-existing Phase-4 slskd-fixture test failures (`test_search_responses_fixture_builds_candidates`, `test_transfer_fixtures_parse`) caused by empty fixtures committed in HEAD — unrelated to 05-01, see `.planning/phases/phase-5/deferred-items.md`. Suite: 247 passed, 2 pre-existing failures.
**Status**: 03-05 ✓ COMPLETE — Phase 3 closed. `app/core/gate.py` is the single composition point: frozen `GateResult(decision, chosen, distance, reasons)` + `evaluate(candidates, manifest, profile, cfg=None, min_kbps=None)` mirroring `detect_gaps` — eligibility-BEFORE-acceptance (each candidate runs `quality.gate` then `fakeflac.check`; only those passing BOTH are scored via `matching.score`, then `matching.recommend` decides, then on accept `selector.select` picks the copy over the best-distance accept set), building `MatchConfig` + the fakeflac floor from `config.settings` (SP-4, env-tunable). `app/core/selector.py` is the dumb/swappable `select(accepted)->Candidate|None` tie-break ladder (distance asc primary → format preference → free_upload_slots desc → upload_speed desc) and the ONLY reader of the uploader fields (matching != selection, Pitfall 5 — proven by an attribute-access source grep). **QUAL-02 proven BOTH directions END-TO-END through the composed gate:** REJECT (`below_cutoff_mp3` excluded at the QUALITY stage vs lossless_only) AND PERMIT (`known_good_mp3_320` ACCEPTED vs mp3_320_cutoff — differs from known_good_flac ONLY in format → the cutoff/rank ladder doesn't over-reject, T-03-12). Full corpus grades to its INDEX labels with zero false-accepts (accepts: known_good_flac/alac/mp3_320, borderline, non_latin; declines split across quality/fakeflac/match stages; ambiguous twins via rec-gap). Adapters now implement `get_quality_profile(id)->Profile` + `get_manifest(foreign_id)->Manifest`: lidarr (primary, raise_for_status, local *arr-name→rank map, .get()-defensive A4-safe), readarr (best-effort over a PDF<MOBI<AZW3<EPUB book ladder; any fault degrades to empty-allowed Profile / stub-safe book Manifest, never raising, never gating music — ARR-02). The locked firewall grep (`ARR_FIELD_NAMES`) EXTENDED with the Pitfall-2 quality-profile-JSON leaks (qualityProfileId, items[/"items", "allowed", "cutoff") scoped to quoted-JSON-key forms so the neutral `cutoff_rank`/`allowed` identifiers + the gate's English reason prose stay clean — **0 forbidden tokens across all 8 core modules**, negative control confirmed (a deliberate qualityProfileId in core/selector.py failed the grep, then reverted). 3 deviations all auto-fixed (Rule 1 ×2: corrected an unrealistic multi-accept selector-test premise that contradicted MATCH-02's rec-gap ambiguity → replaced with a clear-winner gate test + a dedicated test_selector.py ladder unit; removed a `candidate.username` read from gate.py's label that the matching!=selection grep caught. Rule 3 ×1: reworded a matching.py docstring to drop .username dot-notation). 40 new tests; suite **128 passed** (was 88). 3 atomic commits: `aab4404` (gate+selector+end-to-end), `efad5b1` (adapter normalization), `89b4b18` (firewall extension). Prior: 03-04 ✓ COMPLETE — the two pure pre-download quality defenses landed. `app/core/quality.py` is the firewall-clean (QUAL-01) side: a frozen neutral `Profile(allowed: frozenset[int], cutoff_rank: int)` (SP-1) + an integer `QualityRank` ladder (MP3-192=1..FLAC=5; lossless extensions collapse to RANK_LOSSLESS=5 for gating so ALAC/.m4a is never false-rejected against a {4,5} lossless profile) + `rank_for(ext, bitrate)` (lossless extensions → lossless rank bitrate-agnostically; lossy by bitrate bucket; unknown ext / absent-bitrate-lossy → None, conservative reject) + `gate(candidate, profile)→(pass, reason)` verbatim per RESEARCH 442-451 that evaluates EVERY audio file (one below-cutoff/not-allowed file fails the whole candidate — QUAL-02 no-downgrade). **Both QUAL-02 directions proven against the labeled corpus:** REJECT (`below_cutoff_mp3` MP3-192 rank 1 vs `lossless_only` → DECLINE, T-03-08) AND PERMIT (`known_good_mp3_320` MP3-320 rank 3 vs `mp3_320_cutoff` → ACCEPT, making the mp3_320_cutoff profile load-bearing, T-03-12 over-rejection DoS guard); genuine FLAC + ALAC pass lossless_only. `app/core/fakeflac.py` (QUAL-03) is `check(candidate, min_kbps=400)→(pass, reason)` with three coarse non-spectral heuristics (bytes/sec floor, claimed-lossy-bitrate bucket, lossy source token) verbatim per RESEARCH 456-469 — each proven independently — plus `_has_lossy_source_token` (word-bounded ReDoS-safe alternation; 'web-dl' not bare 'web'). **HEADLINE (Pitfall 4 / T-03-09):** a FLAC with `length_seconds=None` (tiny size that WOULD fail the floor if length were known) PASSES — every sub-check skips on absent data, never rejects on a None input; `fake_flac` (~128 kbps effective) declined, `known_good_flac` passes. Default floor matches `Settings.fakeflac_min_kbps`. Both modules import NO rapidfuzz (run identically in the 3.9 sandbox + CI/NAS 3.12); firewall grep clean (0 *arr tokens) on both. Zero deviations (both tasks pure TDD, no auto-fixes). 22 new tests; suite **88 passed** (was 66). 2 atomic commits: `d751391` (quality), `8eb8feb` (fakeflac). Prior: 03-03 ✓ COMPLETE — the phase's hard core landed. `app/core/matching.py` is a pure, deterministic, explainable beets-ported weighted-distance matcher: `score(candidate, manifest, cfg)` is the weighted average `Σ(wᵢ·dᵢ)/Σ(wᵢ)` of artist/album/track-count sub-distances (always) + a track-title coverage term (only when `manifest.track_titles` is truthy — graceful omission from numerator AND denominator), driven by `rapidfuzz.fuzz.token_set_ratio` over NFKD-folded strings; `_track_count_distance` is the clamped completeness driver; empty/None parsed names fold to 1.0 max-penalty and NEVER raise. `recommend(scored, cfg)` collapses beets' tiers into ACCEPT/DECLINE: ACCEPT iff best ≤ `strong_thresh` (0.15) AND runner-up ≥ `rec_gap_thresh` (0.10) worse, else DECLINE with one of three explicit reasons. Every sub-score + decision emits a human-readable reason string (Soularr-opacity fixed). Calibrated against the labeled corpus with the **default MatchConfig unchanged** (no tuning needed): known_good=0.00, borderline=0.05, wrong_album=0.40, incomplete=0.33, ambiguous twins decline via the rec-gap branch (asserted distinctly from the strong-thresh branch). **ZERO false-accepts** across the corpus (headline test sweeps every score-driven fixture; ACCEPT iff and only iff ACCEPT-labeled). `fake_flac`/`below_cutoff_mp3` are quality/fakeflac declines (matcher accepts on identity) → proven in 03-05, excluded from the match corpus by design. Firewall/Pitfall grep clean: 0 *arr tokens, 0 executable selector-field reads (Pitfall 5), no year/label/catalog weight (Pitfall 1). 1 auto-fixed blocking bug (Rule 3): missing `import unicodedata`. 24 new tests; suite 66 passed (was 42). Single atomic commit `53d2584` (score+recommend ship as one cohesive module). Prior: 03-02 ✓ COMPLETE — rapidfuzz==3.13.0 pinned (human-verified PyPI legitimacy: rapidfuzz/RapidFuzz, Max Bachmann, MIT, cp312 manylinux x86_64 wheels; Phase-2 httpx checkpoint-comment precedent) and `app/config.py` Settings extended in place (SP-4, no pydantic-settings) with the 7 Phase-3 tunables — `match_strong_thresh=0.15`, `match_rec_gap_thresh=0.10`, weights `match_w_artist/album=3.0`, `match_w_track_count/track_titles=4.0`, `fakeflac_min_kbps=400` — each read via os.getenv in from_env() (RESEARCH §3 env names), cast float/int (fail-fast on bad operator value, T-03-12). Defaults equal MatchConfig defaults so no-env behavior is identical; Phase-2 fields + singleton intact. Verify printed config-ok (env override honored, unset default preserved); `import config` clean (no regression). Prior: 03-01 ✓ COMPLETE — Wave 0 contract + corpus laid down test-first. `app/core/candidate.py` defines the frozen neutral `Candidate`/`CandidateFile` dataclasses (the Phase 3→4 contract) with `audio_file_count`/`file_titles`/`audio_files()` helpers + the `build_candidate`/`Candidate.from_slskd` slskd-result factory (`.get()`-defensive on every optional attr → None, never KeyError). `app/core/manifest.py` defines the frozen `Manifest` (artist/album/track_count/track_titles|None/kind/year), reused for books per RESEARCH §9. `app/core/release_parse.py` is a pure, ReDoS-safe (anchored/bounded regexes, no nested quantifiers — THREAT T-03-01) NFKD-folding tokenizer → `ParsedRelease(artist/album/year/format/source/edition)` that returns all-None on garbage/empty/non-str and never raises. Firewall intact: 0 *arr field names in all 3 new core modules. The labeled corpus under `app/tests/fixtures/candidates/` has 14 candidates (5 ACCEPT/9 DECLINE) + 3 manifests + 2 profiles + INDEX.md, straddling the QUAL-02 cutoff BOTH directions (`known_good_mp3_320` ACCEPT vs `below_cutoff_mp3` DECLINE on `mp3_320_cutoff`). Suite 42 passed. Phase 2 ✓ COMPLETE (deployed & verified LIVE on NAS); Phase 1 ✓ COMPLETE.
**Last action**: Deployed Phase 2 to the NAS and verified LIVE. `POST /detect` ran a real pass against live Lidarr — ledger climbed (1407→1493 in ~15s), proving real-Lidarr read + GapItem parse + /db write end-to-end; Readarr=0 (keyless/empty → gracefully skipped, music unaffected, ARR-02 live). UAT test 6 now PASS → 02-UAT.md status: complete (6/6). Earlier: added `POST /detect` (single-writer, 409 guard — fixed the docker-exec `database is locked`); reconciled repo docker-compose.yml to LAN-IP defaults (overridable) + .env.example + DEPLOY.md Phase-2 deltas (/db mount, non-root, /detect). Logged a Phase-2→Phase-5 perf follow-up: detect_gaps does one fsync/row (synchronous=FULL) → ~5-6 rows/sec, slow on bulk → batch the pass in one txn in Phase 5 (.planning/phases/phase-5/RESEARCH-SEED.md + memory). Suite 33 passed.
**Last action**: Executed Phase 3 plan 03-01 sequentially on main (3 atomic commits: c6202e8 contract dataclasses, 6dd0d46 release_parse + tests, 986ca54 labeled corpus + INDEX). One auto-fixed bug (Rule 1): release_parse normalized `_`/`.` separators AFTER the ` - ` split, so `Artist_-_Album` failed to split — moved normalization before the split. Suite 42 passed; corpus JSON all valid; firewall grep clean on the 3 new core modules.
**Last action**: Executed Phase 3 plan 03-05 sequentially on main (3 atomic commits: `aab4404` gate+selector+end-to-end test_gate/test_selector, `efad5b1` adapter get_quality_profile/get_manifest normalization, `89b4b18` extended firewall grep + Protocol conformance). gate.evaluate composes all four gates into a labeled-corpus-correct GateResult; QUAL-02 proven BOTH directions end-to-end through the composed gate; firewall holds over all 8 core modules (negative control confirmed). 3 auto-fixed deviations (Rule 1 ×2, Rule 3 ×1). Suite 128 passed (was 88). **Phase 3 COMPLETE — all 5 plans done.**
**Last action**: Executed Phase 4 plan 04-01 sequentially on main (3 atomic commits: `669dbee` migration_0002 enum-widen + staged_files + db.py register, `8680149` staged_files DAOs + 8 Phase-4 Settings tunables (no Plex), `ba09adc` offline slskd/manualimport fixtures + 11 tests). Migration 0002 widens items.status to the 11-value acquisition state machine via a table rebuild (rows survive, UNIQUE preserved); set_status now accepts downloading/importing/quarantined/stuck with no IntegrityError. 2 deviations auto-fixed (Rule 1 ×1: stray conn2.close() displaced into a new test; Rule 3 ×1: load_fixture return-type hint). No new runtime dependency. Suite **139 passed** (was 128).
**Last action**: Executed Phase 4 plan 04-02 sequentially on main (4 atomic TDD commits: `47a1824` RED slskd tests, `a184e71` GREEN SlskdClient, `3692e2d` RED staging tests, `03997ae` GREEN staging.py). `app/adapters/slskd.py` = thin defensive client over slskd `/api/v0` with capital `X-API-Key` (host from settings.slskd_url/gluetun, never a container name): `search`/`search_state`/`search_responses`/`enqueue`/`transfer`/`cancel`, fail-fast on empty key, `.get()` never-subscript (T-04-06), `raise_for_status` primary posture (CircuitBreaker seam left for Phase 5), module-level A3 terminal-state constants for 04-04/04-05 to interpret/re-pin. `app/core/staging.py` = pure-stdlib firewall-clean lifecycle: `staging_path` (never creates), `assert_under_root` (resolve()+strict-parents traversal guard T-04-04 + shallow-root deny-list of /, /data, /data/media and <3-part roots T-04-05) gating every `purge_staging`(D-05)/`quarantine_staging`(D-06)/`purge_expired_quarantine`-TTL(D-06). IMPORT-01 hardlink `os.path.samefile` path-identity proven. 33 new tests; **suite 172 passed** (was 139). Zero deviations (clean TDD, no auto-fixes). Requirements ACQ-01/02/03 + IMPORT-01/05 contributed-to but kept Pending — fully satisfied once 04-04 composes the loop + 04-05 verifies live. **Phase 4 Wave 0 COMPLETE (04-01 + 04-02).**
**Last action**: Executed Phase 4 plan 04-03 sequentially on main (4 atomic TDD commits: `6a0b3a2` RED Lidarr import tests, `d6c6364` GREEN Lidarr ManualImport(Move)/candidates/verify + base.py docstrings, `d46dd7d` RED Readarr import tests, `f38b35c` GREEN Readarr best-effort import methods). Implemented the explicit *arr-agnostic import path (the reason Curator exists, replacing Soularr's blind rescan) behind the firewall: `LidarrAdapter.manual_import_candidates(path, download_id)` GETs `/api/v1/manualimport` (folder/downloadId/filter flags) and ADAPTER-FILTERS to the importable subset (empty `rejections` + non-empty `tracks`) so core gets opaque dicts and never reads an *arr key; `execute_import(decisions)` POSTs `/api/v1/command` `name=ManualImport`, `importMode=Move` (D-09 atomic hardlink), per-file `files[]` (path/artistId/albumId/albumReleaseId/trackIds/quality), NEVER a `DownloadedAlbumsScan` (T-04-09 asserted); `verify_imported(item)` re-queries `get_wanted()` and returns True only when the item LEFT the wanted/missing+cutoff list (D-03 — downloaded != imported). `ReadarrAdapter` mirrors all three best-effort, each wrapping its body in the swallow block → safe defaults `[]`/`None`/`False`; verify issues the wanted re-query DIRECTLY (not via the already-swallowing `get_wanted`) so a 5xx degrades to False not a fake True (Pitfall 5 — false-negative forces quarantine). `[ASSUMED A1]` importMode casing + `files[]` envelope flagged inline + asserted against the single `expected_post.json` fixture (pinned live in 04-05). **NO Plex** — D-04 revised, the 04-03 plan was Plex-free as built. All *arr wire keys + the rejections/tracks reads stay in lidarr.py/readarr.py — firewall grep clean over app/core + app/state. **Zero deviations** (clean TDD, no auto-fixes). 13 new tests; **suite 185 passed** (was 172). IMPORT-02/03 adapter surface landed (traceability → In progress; full satisfaction once 04-04 composes the loop). **Phase 4 Wave 1 COMPLETE (04-03).**
**Last action**: Executed Phase 4 plan 04-04 sequentially on main (2 atomic commits: `d76cfa8` feat acquire_item loop + neutral slskd seams + 15 fake-backed tests, `ee8feca` test firewall grep over acquire.py). `app/core/acquire.py` is the Phase-4 composition point — `acquire_item(item, adapter, slskd, conn, settings, now=…, gate_evaluate=…, build_candidate=…, poll_hook=…) -> "imported"|"quarantined"|"stuck"` mirroring `detect_gaps`' single-composition shape and honoring D-01..D-10: purge-expired-quarantine FIRST (D-06 TTL-on-next-run, never raises) → fetch neutral Manifest+Profile (stuck WITHOUT searching if either unavailable, None-or-raise normalized by `_safe_call`) → collection-window search via `search_is_complete` poll to the monotonic deadline (D-07) → `gate.evaluate` ONCE → on full decline, ONE relaxed-query retry (`_relax_query` drops year/edition noise, D-08) → download the winner then the next-best on stall/failure (runner-up order re-runs the SAME gate, never re-judges match, D-02) → `_watch_to_completion` diffs the NEUTRAL `TransferProgress.bytes_done` over the injected clock, resets the timer on ANY byte advance (Pitfall 4), `cancel_transfer(remove=True)` on no-progress for `acq_stall_seconds` (D-01), exhausted accepted set → stuck (never loops) → on terminal success `_import_and_verify` consumes the adapter's pre-filtered importable subset AS-IS (empty → quarantine; non-empty → `execute_import(that exact list)`), `verify_imported` True → `purge_staging` (D-05) + status imported, else/raise → `quarantine_staging` + `record_quarantine` + status quarantined (D-06, never blind-purged). State machine pending→searching→downloading→importing→imported/quarantined/stuck via `repo.set_status`; a `staged_files` row per attempt. **NO Plex call** (revised D-04 — IMPORT-04 external). `build_acquire_clients(settings)` = lazy-httpx `SlskdClient` factory (caller owns close, CR-02). The slskd client gained the NEUTRAL seam so no wire vocab nor the SELECTOR-ONLY uploader identity crosses into core: `search_is_complete` (reads `isComplete`), `enqueue_candidate(candidate)`→opaque `TransferHandle` (reads `username`+files), `transfer_progress(handle)`→`TransferProgress` (interprets `state`/`bytesTransferred`+A3 substrings), `cancel_transfer(handle)`. **1 deviation auto-fixed (Rule 3):** the natural `candidate.username` read in acquire.py to enqueue/watch tripped the pre-existing matching!=selection grep — moved the uploader-identity read into the slskd client (opaque `TransferHandle`), the correct firewall posture; both the matching!=selection grep AND the new *arr/slskd firewall grep hold over acquire.py (negative control confirmed: a deliberate `downloadId` leak fails it, then reverted). ACQ-01/02/03 + IMPORT-01/02/03/05 all marked Complete (offline mechanism done; A1/A3 live strings pinned in 04-05). 16 new tests; **suite 201 passed** (was 185). **Phase 4 Wave 2 COMPLETE (04-04) — only the 04-05 live test remains.**
**Last action**: Executed Phase 4 plan 04-05 Task 3 (live-probe reconciliation) sequentially on main (3 atomic commits: `94db977` A3 slskd terminal-state pin, `fba83fb` A2 real-landing-dir resolution, `0c5d786` A1 ManualImport envelope pin). Tasks 1 & 2 (D-11 share checkpoint + the 3 live probes) were completed by the owner — observations recorded in 04-05-LIVE-PROBE.md (shares confirmed >0 live; A3 success=`Completed, Succeeded`; A2 no-batchId, files land at staging_root/<leaf-of-remote-folder>; A1 ManualImport POST captured via DevTools). Task 3 folded all three into the offline suite: **A3** re-pinned `STATE_COMPLETED_SUCCEEDED="Completed, Succeeded"` + robust rule in `transfer_progress` (TERMINAL iff state contains "Completed", SUCCESS iff also "Succeeded", any other "Completed, *" → FAILURE; named failure/cancelled constants kept as docs since only the success family was observed). **A2** (a real code change, not the no-op branch — slskd ignores batchId): added neutral `TransferHandle.landing_dir_name` + `_remote_folder_leaf` (splits on `\` and `/`) in slskd.py, `acquire.py` now resolves the staging dir from the handle's leaf AFTER enqueue and points import + purge/quarantine at the REAL landing folder; `curator-{app}-{id}` demoted to a leaf-less fallback; firewall stays clean (the token `folder` avoided even in acquire.py docstrings, since the grep only strips `#` comments). **A1** reconciled lidarr+readarr `execute_import` to the captured envelope: `importMode:"move"` lowercase (Curator's deliberate D-09 hardlink, not the UI `"copy"`, not the old capital `"Move"`), top-level `replaceExistingFiles:false`+`sendUpdatesToClient:true`, per-file full QualityModel echo, **dropped** the per-file `downloadId`; `expected_post.json`+`get_mapping.json` reconciled (real leaf landing paths); the lidarr import test STRENGTHENED (asserts `move` + downloadId absence). **2 deviations auto-fixed (both Rule 1 — correctness bugs surfaced by reconciling to live truth: the A2 staging-path mismatch would have quarantined every real import; the A1 capital-`Move`/stray-downloadId envelope).** No weakened assertions (fixtures/constants moved to truth, then tests re-pinned). 4 new tests; **suite 205 passed** (was 201); firewall grep over core/acquire.py clean. **Phase 4 Wave 3 COMPLETE (04-05) — Phase 4 COMPLETE, all 5 plans done.**
**Next action**: Phase 5 PLANNED (2026-05-31) — 5 plans in 4 waves (`05-01..05-05-PLAN.md`), plan-check PASSED on re-verify (3 blockers + 3 warnings from iter-1 all fixed: 05-03 conftest→tests.fakes import + `searching` orphan reset to pending without burning an attempt; VALIDATION test_daemon→test_scheduler/test_concurrency; 05-05 distinct lifecycle test; 05-04 conftest reuse; RESEARCH open-Qs marked RESOLVED). RESEARCH.md (stdlib daemon thread + ThreadPoolExecutor, single-writer LockedConn, slskd shares API GET /api/v0/application + PUT /api/v0/shares, migration_0003, eligibility SQL, infra-vs-genuine classifier) + PATTERNS.md (13/13 analogs) + VALIDATION.md (Nyquist) all committed. All 7 req IDs covered. Wave structure: W0 05-01 (migration_0003 + backoff/eligibility DAOs + config tunables + test scaffolds) ‖ 05-02 (slskd shares methods + *arr get_queue_status + acquire.py INFRA_EXC classifier); W1 05-03 (core/shares.py ensure-self-heal + core/reconcile.py startup orphan reset); W2 05-04 (detect batch-txn + core/scheduler.py daemon); W3 05-05 (lifecycle wiring + GET /status HTML+JSON + A2/A3 live-probe checkpoint, autonomous:false). Run `/clear` then `/gsd:execute-phase 5`. NOTE: do NOT auto-run the daemon live after build — the bounded rollout (ACQ_DRY_RUN → MAX_CONCURRENT=1 observe-one-album → raise to 3) is an owner-driven staged step in DEPLOY.md, not a code task. Locked decisions: 3-day grace from discovered_at (existing ~1493 backlog already past it → eligible at launch; race-avoided via *arr queue check, GAP-03); 6h daemon poll loop + 3 concurrent steady-state, staged rollout via simple env flags `ACQ_DRY_RUN`/`MAX_CONCURRENT`/`ACQ_ENABLED` kill-switch, **live acceptance test = observe the first capped daemon pass at MAX_CONCURRENT=1** one album end-to-end (REL-01, supersedes seed single-item-trigger); 3 attempts→permanently-unavailable, exponential 1h→6h→24h backoff, 30-day dormant re-check (STATE-03); sharing ensure+self-heal (verify count>0 / rescan / surface, NO slskd.yml rewrite), share music+books read-only (SHARE-01/02 — researcher to confirm slskd share/rescan API); browser-viewable bare HTML status page + JSON for the P6 widget, Pushover push DEFERRED to Phase 6 (REL-03); startup reconciliation restarts in-flight cleanly w/ double-import verify-guard + infra-outage burns no per-item attempt (REL-02, Claude-discretion default); batch detection-pass fsync carry-forward + migration_0003 for backoff/attempt/last-checked fields. Phase 4 COMPLETE (all 5 plans, suite 205, firewall intact, pushed to origin/main); acquisition loop pinned to live slskd/Lidarr reality; D-11/D-12 NAS preconditions applied + recorded in 04-05-LIVE-PROBE.md. Superseded Phase-4 04-05 plan notes (kept for history below):
**Superseded next action**: Continue Phase 4 — 04-05 (Wave 3: live-NAS verification). It PAUSES at the D-11 slskd-share-confirmation checkpoint (shares CONFIRMED LIVE 2026-05-31) + the 3 live probes (A1 ManualImport POST envelope, A2 batchId settability, A3 slskd terminal-state strings) — autonomous:false. Offline code paths already use [ASSUMED] fixtures/named constants so nothing is blocked; 04-05 re-pins A1/A3 in ONE place (expected_post.json + slskd.py terminal-state constants) and runs the first real end-to-end download against live slskd+Lidarr after the owner applies DEPLOY.md §"Step 8 — Phase 4 setup" (D-12 path-identity: slskd directories.downloads=/data/downloads/soulseek matching staging_root, owned 1031:65536; curator /data :ro→rw; *arr root folder /data/media/music). Earlier: 5 plans in 4 waves (`04-01..04-05-PLAN.md`), plan-check PASSED (1 MED + 2 LOW findings applied: adapter-side importable filter, manifest/profile fetch provenance, quarantine-TTL wiring). RESEARCH.md + PATTERNS.md + VALIDATION.md all committed. Run `/gsd:execute-phase 4` to build. NOTE: 04-05 pauses at the D-11 slskd-share-confirmation checkpoint + the 3 live-NAS probes (A1 ManualImport POST envelope, A2 batchId settability, A3 slskd terminal-state strings) — these are autonomous:false; offline code paths use [ASSUMED] fixtures/named constants so execution is never blocked on the NAS. Earlier Phase 4 context: 12 decisions locked (D-01..D-12) in 04-CONTEXT.md. Locked decisions: stall-based death + next-candidate fallback (ACQ-03); *arr-confirmed import is the DONE bar (IMPORT-03); **Plex DROPPED — D-04 revised 2026-05-31, Curator does NOT call Plex; IMPORT-04 satisfied by owner's existing Plex auto-scan (external precondition, no PLEX_TOKEN in stack)**; quarantine-on-failure + purge-on-success w/ TTL (IMPORT-05); fixed search-collection window → gate.evaluate once + one relaxed retry (ACQ-01/02); GET-mapping→POST-ManualImport Move-mode via the *arr-agnostic adapter, owner takes zero manual actions (IMPORT-02). **Live-test preconditions (owner setup, applied on next NAS teardown AFTER execution lands in git): D-11 slskd shares (CONFIRMED LIVE 2026-05-31) + D-12 path setup (slskd directories.downloads=/data/downloads/soulseek matching staging_root; pre-create that dir owned 1031:65536; curator /data mount :ro→rw; verify *arr mounts full /data + root folder /data/media/music). Full owner checklist: DEPLOY.md §"Step 8 — Phase 4 setup".** Offline waves 0-2 build hands-off with no NAS setup; only the 04-05 live test needs the above. Deferred to Phase 5: daemon/scheduling/backoff, SHARE-01/02 self-healing, detection batch-fsync perf.

## Active Phase Detail

Phase 1 delivers the substrate: gluetun (PIA/OpenVPN, kill-switch, port forwarding, control-server auth) + slskd (shared netns, native PF sync, user 1031:65536) + Curator FastAPI health stub, all from one compose pulling a CI-built Docker Hub image, with a single identical /volume1/data mount for atomic hardlinks. All six INFRA requirements (01-06) addressed in code; final proof is the on-NAS Go/No-Go smoke test (criteria that need live VPN/NAS were NOT runnable in the dev sandbox).

## Recent Decisions

- **Live-probe reconciliation pinned the three load-bearing wire shapes to reality (04-05)** — **A3:** slskd `state` is a compound `"<phase>, <completion>"` flag; live success = `Completed, Succeeded`. The robust rule (in slskd.py `transfer_progress`): TERMINAL iff state contains `"Completed"`, SUCCESS iff also `"Succeeded"`, any other terminal `"Completed, *"` → FAILURE. Only the success family was observed live, so the substring rule (not an unobserved exact literal) is authoritative; named failure/cancelled constants are documentation. **A2:** slskd does NOT honor a batchId — it lands files at `staging_root/<leaf-of-remote-folder>/` (last segment of the peer's `\`-separated remote folder, no username/batchId subdir). This forced a REAL code change: `TransferHandle` gained a neutral `landing_dir_name` (derived by the client via `_remote_folder_leaf`), and `acquire.py` resolves the staging dir from that leaf AFTER enqueue so the ManualImport + purge/quarantine target the REAL folder (the old `curator-{app}-{id}` label is now a leaf-less fallback). Left unfixed this would have quarantined every real import. The firewall holds — the leaf logic lives in the client, and the token `folder` appears nowhere in acquire.py (the grep only strips `#` comments, so even docstrings avoid it). **A1:** the Lidarr ManualImport POST is `importMode:"move"` (LOWERCASE — Curator's deliberate D-09 atomic-hardlink choice, NOT the UI default `"copy"`, NOT the old `[ASSUMED]` capital `"Move"`), top-level `replaceExistingFiles:false`+`sendUpdatesToClient:true`, per-file `path/artistId/albumId/albumReleaseId/trackIds/quality(full QualityModel)/indexerFlags/disableReleaseSwitching`, and NO per-file `downloadId` (command-queue metadata, not body). Mirrored in readarr.py (D-10). No weakened assertions — fixtures/constants moved to the live truth, then tests re-pinned (the lidarr import test was strengthened). Suite 205 passed; firewall clean. **Phase 4 COMPLETE.**

- **The importability decision lives INSIDE the adapter; manual_import_candidates returns only the pre-filtered subset (04-03)** — `manual_import_candidates` reads the *arr `rejections`/`tracks` keys IN-adapter and returns ONLY resources with empty rejections + non-empty tracks as opaque dicts, so core passes them straight back to `execute_import` and NEVER inspects an *arr key (the *arr-keyed importability decision is made once, in the adapter). `execute_import` POSTs an explicit `ManualImport` command with `importMode=Move` and a per-file `files[]` envelope — NEVER a `DownloadedAlbumsScan` blind rescan (T-04-09, asserted both directions). `verify_imported` re-queries and returns True only when the item LEFT the wanted/missing list (D-03 — "downloaded" != "imported"). Readarr mirrors all three best-effort (swallow→`[]`/`None`/`False`); critically its `verify_imported` issues the wanted re-query DIRECTLY rather than via `get_wanted` (whose `_paged` already swallows a fault to `[]`, which would yield a fake True), so a Readarr 5xx degrades to False — Pitfall 5: a false-negative forces quarantine (safe), a false-positive would skip cleanup. Books never gate music (ARR-02). `[ASSUMED A1]` importMode casing + files[] envelope pinned live in 04-05 via the single `expected_post.json` fixture. **No Plex** (D-04 revised). Firewall holds. Suite 185 passed.

- **SlskdClient mirrors lidarr.py but slskd-specific: /api/v0 base + capital X-API-Key + raise_for_status primary posture (04-02)** — the only Curator→slskd client differs from the Servarr adapters in exactly two spots (`base + "/api/v0"`, `{"X-API-Key": ...}` capital `API`), is fail-fast on an empty key, reads every response field via `.get()` (never subscript — peer→slskd→Curator is an untrusted boundary, T-04-06), and surfaces hard faults via `raise_for_status()` because slskd is now the primary download path (the `CircuitBreaker` seam is left available for Phase 5, not wrapped here). The host always comes from `settings.slskd_url` (gluetun's published NAS-IP:5030), never a container name (slskd is `network_mode: service:gluetun`, off synobridge — Pitfall 7). The `[ASSUMED]` A3 transfer terminal-state strings are module-level named constants the client EXPOSES (it returns the raw transfer dict); acquire.py (04-04) interprets them and 04-05 re-pins the live strings in one place. respx is NOT required — the 16 tests use httpx `MockTransport` so they pass in the 3.9 offline sandbox too.

- **staging.py gates every destructive fs op behind a resolve()+strict-parents guard AND a shallow-root deny-list (04-02)** — `assert_under_root` runs BEFORE any `rmtree`/`move`: it `resolve()`s both path and root first (normalizing away `../` and symlink escapes — T-04-04 peer-filename traversal), refuses a root resolving to `/`, `/data`, `/data/media` or having `< 3` non-anchor parts (T-04-05 — a mis-set `quarantine_root` can never wipe the library), then requires the resolved root to be a strict parent of the resolved target. `staging_path` only COMPUTES the per-item dir (slskd creates it via batchId). `purge_staging`(D-05, verified-import cleanup), `quarantine_staging`(D-06, move to `{label}-{ts}`), and `purge_expired_quarantine`(D-06, `st_mtime < now-ttl` selective sweep) are all guarded. Pure stdlib core module — firewall-clean (0 *arr tokens), speaks only filesystem paths. IMPORT-01 proven via `os.link` + `os.path.samefile` (a Move within one filesystem is an atomic hardlink). Suite 172 passed.

- **Migration 0002 widens the status enum BEFORE adding staged_files, and the table-rebuild copies columns verbatim (04-01)** — SQLite has no ALTER COLUMN, so widening `items.status` to the 11-value acquisition state machine (adding `downloading/importing/quarantined/stuck`) is the standard rename→recreate-with-widened-CHECK→`INSERT … SELECT *`→drop rebuild, run inside the runner's single BEGIN/COMMIT (WR-02). The enum widen ships first so the existing status-preserving upsert protects the new in-flight states the moment they exist (Pitfall 6 — `set_status('downloading')` never raises IntegrityError). The recreated `items` copies ALL 0001 columns + the `UNIQUE(arr_app, arr_id)` dedup primitive verbatim so `INSERT … SELECT *` aligns by position and pre-existing rows survive identity-and-status intact (proven by a v1-only-then-full-migrate test). `staged_files` (item_id FK + staging/quarantine paths + reason) and its two `?`-only DAOs (`record_staged_file`/`record_quarantine`) round-trip. **No Plex fields on Settings** (revised D-04). 8 Phase-4 tunables added env-overridable with fail-fast float casts. Offline slskd/manualimport fixtures landed with `[ASSUMED]` provenance for the A1/A3 live-probe values (named-constant intent documented) so 04-02/03/04 are provable with no live services. No new runtime dependency. Suite 139 passed (was 128).

- **gate.evaluate is eligibility-before-acceptance and selector ratifies the accept set; matching!=selection is structurally proven (03-05)** — `gate.evaluate` filters each candidate through `quality.gate` + `fakeflac.check` BEFORE scoring, so a below-cutoff / fake-FLAC folder is structurally unable to slip past as a good match (it's recorded as `excluded` with its reason, never scored). On accept it snapshots the best-distance accept set (best + 1e-9 epsilon) and hands it to `selector.select`; `recommend()` already declines genuine rec-gap near-ties, so the selector only ever ratifies a single unambiguous winner (and would tie-break a true multi-accept set) — it never re-judges the match. `selector.py` is the LONE reader of `upload_speed`/`free_upload_slots`/`username`, enforced by an attribute-access source grep in test_gate.py (Pitfall 5 / T-03-06). **Corpus reality correction:** two near-identical copies of the SAME album are a rec-gap AMBIGUOUS decline (not an accept), so the plan's multi-accept selector test was replaced with a clear-winner gate test + a dedicated test_selector.py ladder unit — faithful to MATCH-02, no weakened assertion.

- **The firewall `cutoff`/`allowed`/`items` tokens are matched as QUOTED JSON KEYS, not bare words (03-05)** — `core/quality.py` legitimately emits English reason strings ("below cutoff", "cutoff met") and exposes the neutral `Profile.cutoff_rank`/`allowed` attrs, so `ARR_FIELD_NAMES` matches only the quoted-key forms `"cutoff"`/`"allowed"`/`"items"` (plus `qualityProfileId`, `items[`). This targets the *arr JSON-key access (`body.get("cutoff")`, `profile["items"][i]["allowed"]`) and admits the neutral identifiers + prose. Verified by an explicit negative-control suite AND an in-file control (a deliberate `qualityProfileId` in core/selector.py failed the grep, then reverted). Firewall holds: 0 forbidden tokens across all 8 core modules.

- **Adapters normalize *arr profile/manifest behind the firewall via a LOCAL quality-name→rank map; Readarr degrades to safe defaults (03-05)** — `lidarr.get_quality_profile` maps allowed *arr quality NAMES through a `_LIDARR_QUALITY_RANKS` dict kept inside lidarr.py to `Profile(allowed, cutoff_rank)` (cutoff resolved from the cutoff quality id, falling back to min(allowed) when unresolvable — A4-unconfirmed-safe, .get()-defensive, raise_for_status primary). `get_manifest` maps the album record to `Manifest` with `track_titles=None` graceful when no track list. `readarr.*` mirror this best-effort over a PDF<MOBI<AZW3<EPUB book ladder, swallowing ANY fault to an empty-allowed Profile / stub-safe book Manifest — never raising into the loop, never gating music (ARR-02). Only the neutral Profile/Manifest cross into core.

- **The matcher is a single cohesive pure module proven by corpus calibration, not by invented numbers (03-03)** — `score` + `recommend` ship together in `app/core/matching.py` (splitting mid-file = non-functional intermediate), reading only manifest identity + `candidate.parsed_*`/`audio_file_count`/`file_titles` — never uploader fields (Pitfall 5) and never year/label/catalog (Pitfall 1, anchoring rule). The **default MatchConfig calibrated the corpus correctly with NO tuning** (known_good=0.00, borderline=0.05 inside strong=0.15; wrong/incomplete/edition/garbage all >0.15; ambiguous twins decline via rec-gap), so the 03-02 config defaults stand. Calibration discipline is enforced in the test header: tune the number/weight, never the assertion (Pitfall 3). The two QUAL/fakeflac DECLINE fixtures (`fake_flac`/`below_cutoff_mp3`) are EXCLUDED from the match corpus — the matcher accepts them on identity; they are gate-layer declines proven in 03-05.

- **Phase-3 matcher/gate tunables live in the existing frozen Settings, not pydantic-settings (03-02)** — `MATCH_STRONG_THRESH`/`MATCH_REC_GAP_THRESH`/per-sub-distance weights/`FAKEFLAC_MIN_KBPS` were added in place to `app/config.py` Settings + from_env() (SP-4), each cast float/int so a non-numeric operator value fails fast with a clear ValueError at startup (T-03-12, accepted) rather than silently mis-gating. Defaults equal the MatchConfig defaults (RESEARCH 207-215) so no-env behavior is identical to the hard-coded scorer; gate.py (03-05) reads `config.settings` to build its MatchConfig. rapidfuzz pinned at **3.13.0** (current PyPI stable, verified via `pip index versions`; pre-authorized legitimate package — no human pause), with a provenance comment mirroring the Phase-2 httpx package-legitimacy checkpoint.

- **Phase 3→4 contract dataclasses are frozen + *arr-free, factory lives in candidate.py (03-01)** — `Candidate`/`CandidateFile`/`Manifest` are SP-1 frozen neutral dataclasses with zero *arr field names; `build_candidate`/`from_slskd` (the slskd-result→contract factory) lives in candidate.py as part of the contract type's surface and is `.get()`-defensive so a malformed/absent slskd attr yields None, never KeyError. QualityRank ladder is encoded only as neutral ints in the profile fixtures; the `rank_for()` mapping is intentionally left to plan 03-04 (clean file ownership).
- **Corpus straddles the QUAL-02 cutoff in BOTH directions (03-01)** — `known_good_mp3_320` (MP3-320, ACCEPT) vs `below_cutoff_mp3` (MP3-192, DECLINE) both scored against the `mp3_320_cutoff` profile, so the permit path is proven, not just the reject path; INDEX.md makes the `mp3_320_cutoff` profile load-bearing. release_parse regexes are all anchored/bounded (no nested quantifiers) as the structural ReDoS defense (T-03-01), proven by an adversarial 2000+ char bounded-time test.

- **Phase 4 precondition: slskd sharing must be live BEFORE first real download (2026-05-30, owner-raised)** — Phase 4 does Curator's first slskd downloads, but SHARE automation is Phase 5. A zero-share Soulseek account gets leech-blocked. Resolution: do NOT reorder; configure slskd-level shares MANUALLY (point slskd at read-only `/data/media/music` + `/data/media/books`, container-internal paths, verify shared-file count > 0) before any Phase 4 live test. SHARE-01/02 (Curator auto-ensuring shares) stays in Phase 5. Full error-free setup procedure in `.planning/phases/phase-4/RESEARCH-SEED.md`. Phase 3 (matching/gating) is unaffected — never touches slskd.
- **PIA region = CA Vancouver** — west-coast PF-capable (US has zero port forwarding; latency was the owner's concern but PF is mandatory for slskd).
- **Inline execution** of Phase 1 (not worktree subagents) — all owner-provided values were in context and 01-02/01-04 share docker-compose.yml; faster and conflict-free.
- **Owner deploys manually** in Container Manager (not auto-SSH) — DEPLOY.md is the handoff.
- **slskd-direct, not Soularr**; **v1 = music + books (Readarr best-effort)**; **staging→Manual-Import→auto-purge** cleanup (the 6th pain point).
- **Status-preserving upsert (02-02)** — the `ON CONFLICT(arr_app, arr_id)` SET clause omits BOTH `status` and `discovered_at`, so a re-detect of an acted-on/first-seen row never clobbers its lifecycle status (the #1 STATE-02 pitfall, proven by `test_upsert_preserves_status`).
- **WAL + PRAGMA user_version migrations (02-02)** — idempotent versioned runner reading schema.sql relative to db.py; the only f-string-into-SQL is the loop-controlled `user_version` bump; all data queries use `?` placeholders.
- **One ArrAdapter Protocol + GapItem firewall (02-03)** — *arr field names (foreignAlbumId/profileId/records[]/X-Api-Key) live ONLY in app/adapters/; enforced by a comment-aware grep test over app/core + app/state. Phase 2 implements only get_wanted(); import/command/profile/queue methods are declared-and-stubbed to lock the seam shape.
- **Primary-vs-best-effort fault policy (02-03)** — LidarrAdapter raise_for_status surfaces hard faults (NOT breaker-wrapped, it's primary); ReadarrAdapter `_paged` swallows faults → [] and `_map` returns None (skip+log) on bad records, all behind a CircuitBreaker → [] when open. Makes "books never gate music" structural.
- **Phase-2 Protocol conformance via attribute/callable checks, not isinstance (02-03)** — a runtime_checkable isinstance() over-asserts because the later-phase methods are stubbed (not implemented) on concrete adapters; tests assert `.app` + `callable(get_wanted)` (plan-sanctioned). ReadarrAdapter tolerates both `qualityProfileId` and `profileId` (A-R2).
- **detect_gaps counts = items SEEN, not rows inserted (02-04)** — `counts[app] = len(get_wanted())` reports the true per-app gap count even on a dedup re-run (where 0 new rows are added), which is the semantically useful number for UAT/observability.
- **detect_gaps proven with in-test fakes, not the httpx mock (02-04)** — FakeAdapter/FaultyAdapter give deterministic, network-free control of adapter output, decoupling the integration proof from the recorded *arr fixture envelopes (already exercised by the adapter tests). Plan-sanctioned ("either is acceptable, keep it offline"). The breaker-wrapped FaultyAdapter proves ARR-02 (readarr:0, all Lidarr rows upserted, no raise) at the integration point.
- **One-shot `python -m` trigger, NOT a daemon (02-04)** — the `__main__` block is a manual UAT affordance only; the periodic scheduler/grace-window is Phase 5. Enforced by the verify grep forbidding while-True/sleep/apscheduler/slskd in gap_detector.

## Project Reference

See: .planning/PROJECT.md (updated 2026-05-29)

**Core value:** Anything monitored in Lidarr/Readarr that the Usenet pipeline can't get is acquired automatically — correctly matched, right quality, no redundant downloads, no junk, zero manual interaction.
**Current focus:** Phase null

## Notes / Blockers

- **Unverified locally:** the dev sandbox has Python 3.9 + no network (offline pip), so `pytest` for the Curator stub was NOT run here. YAML structure for compose + CI workflow WAS validated (3 services, slskd has no ports/networks, single external net, CI targets linux/amd64). Tests + full smoke run execute on the NAS / in CI.
- Config: YOLO, standard granularity, parallel, quality models, research + plan-check on, verifier off, docs committed.
- Phase 1 NAS deploy needs from owner: `.env` with PIA_USER/PIA_PASSWORD, `GLUETUN_API_KEY` (genkey), `SLSKD_API_KEY`, LIDARR/READARR API keys. Everything else pre-filled.
- Deploy-time TODO (optional but recommended): pin gluetun/slskd `@sha256` digests in compose.
- Owner environment in memory: synobridge 172.20.0.0/16 (gw .1), PUID/PGID 1031/65536, /volume1/data, lidarr:8686, readarr:8787, curator:8674 (LAN/Tailscale-only firewall rule).
