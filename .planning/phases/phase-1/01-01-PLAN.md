---
phase: 01-vpn-routed-networking-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - .gitignore
  - .planning/phases/phase-1/NAS-RECON.md
autonomous: false
requirements: [INFRA-06]
user_setup:
  - service: private-internet-access
    why: "PIA OpenVPN credentials + a PF-capable non-US region are required for the gluetun tunnel (INFRA-01/02). Owner must supply creds; Claude cannot obtain them."
    env_vars:
      - name: PIA_USER
        source: "PIA account → OpenVPN/manual-config username (the p-number style login)"
      - name: PIA_PASSWORD
        source: "PIA account password for OpenVPN"
  - service: docker-hub
    why: "CI pushes the Curator image and the NAS pulls it (INFRA-04/05). Owner must create a Docker Hub access token."
    env_vars:
      - name: DOCKERHUB_USER
        source: "hub.docker.com account username"
      - name: DOCKERHUB_TOKEN
        source: "hub.docker.com → Account Settings → Security → New Access Token (NOT the account password)"
    dashboard_config:
      - task: "Add DOCKERHUB_USERNAME and DOCKERHUB_TOKEN as GitHub repo secrets"
        location: "GitHub repo → Settings → Secrets and variables → Actions → New repository secret"
  - service: synology-nas
    why: "Two values (A3 existing *arr /data mount shape, A4 PUID/PGID) and the hardlink/CIDR/tun probes can only be resolved by running commands ON the NAS. Owner runs them and pastes results."
    dashboard_config:
      - task: "Run the NAS recon command block (this plan, Task 1) over SSH and paste outputs back"
        location: "SSH session on the Synology DS423+"

must_haves:
  truths:
    - "The synobridge subnet CIDR is known and recorded (feeds FIREWALL_OUTBOUND_SUBNETS, must not overlap the VPN CIDR)."
    - "The /volume1 media-owner PUID/PGID are known and recorded (feeds slskd user: and *arr PUID/PGID parity)."
    - "An existing *arr container's /data mount shape is known — single identical /data mount confirmed, or a realignment task is recorded."
    - "A hardlink created across /data/downloads and /data/media on the NAS succeeds (proves single-FS, INFRA-06)."
    - "/dev/net/tun exists on the NAS (gluetun precondition)."
    - "A real .env exists on the NAS (gitignored) and a committed .env.example template exists with every required key."
    - "gluetun v3.41.1 and slskd 0.25.1 amd64 digests are captured for digest-pinning."
    - "A gluetun control-server API key is generated."
  artifacts:
    - path: ".env.example"
      provides: "Committed template of every runtime secret/config key (no values)"
      contains: "PIA_USER"
    - path: ".gitignore"
      provides: "Ensures real .env is never committed"
      contains: ".env"
    - path: ".planning/phases/phase-1/NAS-RECON.md"
      provides: "Recorded recon results: CIDR, PUID/PGID, *arr mount shape, hardlink result, tun presence, pinned digests, region"
      min_lines: 20
  key_links:
    - from: ".env.example"
      to: "docker-compose.yml (Plan 02/04)"
      via: "every ${VAR} in compose has a matching key here"
      pattern: "SYNOBRIDGE_CIDR|GLUETUN_API_KEY|PIA_USER"
---

<objective>
Resolve the only two genuinely-unknown inputs for Phase 1 (A3 existing *arr `/data` mount shape, A4 exact PUID/PGID) and the other NAS-local preconditions (synobridge CIDR, `/dev/net/tun`, hardlink capability), generate the gluetun control-server API key, pin image digests, and bootstrap the secrets surface (`.env` on the NAS + committed `.env.example` + `.gitignore`). This plan GATES every deploy in Phase 1: the compose files in later plans interpolate the values discovered here.

Purpose: The compose stack cannot be written correctly without the real synobridge CIDR (FIREWALL_OUTBOUND_SUBNETS), the real PUID/PGID (slskd `user:` + *arr parity), and proof that `/data` is a single hardlink-capable filesystem (INFRA-06, the #1 import-failure cause). RESEARCH.md flags A3/A4 as the two items that require on-NAS inspection.
Output: NAS-RECON.md (recorded values), .env.example, .gitignore, and a real .env on the NAS (never committed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-1/RESEARCH.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Run NAS recon block and capture results into NAS-RECON.md</name>
  <files>.planning/phases/phase-1/NAS-RECON.md</files>
  <read_first>
    - RESEARCH.md → "Single `/data` Mount + PUID/PGID (INFRA-06)" (the exact recon command block, verbatim)
    - RESEARCH.md → "Sequencing & Gotchas" Wave 0 steps 1-3
    - RESEARCH.md → "Environment Availability" (blocking unknowns to confirm)
    - PITFALLS.md → Pitfall 4 (path identity / EXDEV), Pitfall 6 (slskd user model)
  </read_first>
  <action>
    This is a human-action checkpoint because the values exist only on the Synology and require the owner's SSH session + PIA subscription. Present the owner with the recon command block from RESEARCH.md's "Single /data Mount" section and ask them to run it on the NAS and paste outputs. The block must capture, at minimum: (a) `docker network inspect synobridge --format '{{range .IPAM.Config}}{{.Subnet}}{{end}}'` for SYNOBRIDGE_CIDR; (b) `id <media-user>` for PUID/PGID (resolves A4; RESEARCH expects ~1026/100); (c) `docker inspect lidarr --format '{{range .Mounts}}{{.Source}} -> {{.Destination}}{{"\n"}}{{end}}'` for the existing *arr `/data` mount shape (resolves A3); (d) `ls -l /dev/net/tun`; (e) the hardlink smoke test from RESEARCH (touch + `ln` across `/data/downloads/soulseek` and `/data/media/music`, expect "HARDLINK OK"); (f) `df /volume1/data` (single filesystem row). Record every output verbatim into NAS-RECON.md. If A3 shows split/non-`/data` mounts, do NOT proceed silently — record a follow-up note that the existing *arr containers must be realigned to a single identical `/data` mount before Plan 04 deploy (this is the INFRA-06 precondition per Pitfall 4).
  </action>
  <verify>
    <automated>MISSING — human-run; verify by presence of recorded values: test -f .planning/phases/phase-1/NAS-RECON.md && grep -Eq 'SYNOBRIDGE_CIDR|Subnet' .planning/phases/phase-1/NAS-RECON.md && grep -Eq 'PUID|uid=' .planning/phases/phase-1/NAS-RECON.md && grep -q 'HARDLINK OK' .planning/phases/phase-1/NAS-RECON.md && echo RECON_OK</automated>
    <human-check>Owner confirms the hardlink test printed "HARDLINK OK", `/dev/net/tun` exists, and the *arr `/data` mount is a single identical mount (or a realignment note is recorded).</human-check>
  </verify>
  <done>NAS-RECON.md contains the synobridge CIDR, PUID/PGID, the existing *arr /data mount shape (with realignment note if split), a "HARDLINK OK" result, and confirmation /dev/net/tun exists.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Generate gluetun API key + pin image digests + record region</name>
  <files>.planning/phases/phase-1/NAS-RECON.md</files>
  <read_first>
    - RESEARCH.md → "Don't Hand-Roll" (genkey row), "Standard Stack" (gluetun v3.41.1, slskd 0.25.1 pin notes), "Verification Protocol" (digest-capture commands), "Package Legitimacy Audit" (Wave 0 gate: pin by digest)
    - RESEARCH.md → Pitfall 2 (PF region: 0/54 US, all 5 CA; recommend CA Toronto/Montreal)
  </read_first>
  <action>
    Generate the gluetun control-server API key with `docker run --rm qmcgaw/gluetun genkey` (22-char base58) and record it as the GLUETUN_API_KEY value into the NAS `.env` only (never into NAS-RECON.md or git). Capture the amd64 image digests for `qmcgaw/gluetun:v3.41.1` and `slskd/slskd:0.25.1` using the Verification Protocol curl commands in RESEARCH.md, and record the resulting `@sha256:...` strings in NAS-RECON.md so Plans 02/04 can digest-pin (per the Package Legitimacy Audit Wave-0 gate — no `:latest` in committed compose). Confirm the chosen PF region is PF-capable and non-US (default CA Toronto per RESEARCH live-verified list); optionally re-run the live PIA region query from the Verification Protocol if >30 days since 2026-05-30. Record the chosen region in NAS-RECON.md. The legitimacy table in RESEARCH (all four images first-party, slopcheck not run) means digests MUST be pinned here as the substitute control.
  </action>
  <verify>
    <automated>grep -Eq 'sha256:[0-9a-f]{12,}' .planning/phases/phase-1/NAS-RECON.md && grep -Eiq 'region' .planning/phases/phase-1/NAS-RECON.md && echo DIGESTS_OK</automated>
    <human-check>Owner confirms genkey output was placed only in the NAS .env (not git), and both amd64 digests + the non-US region are recorded in NAS-RECON.md.</human-check>
  </verify>
  <done>NAS-RECON.md records gluetun + slskd amd64 sha256 digests and the chosen non-US PF region; GLUETUN_API_KEY lives only in the NAS .env.</done>
</task>

<task type="auto">
  <name>Task 3: Commit .env.example template and .gitignore (secrets hygiene)</name>
  <files>.env.example, .gitignore</files>
  <read_first>
    - RESEARCH.md → "`.env.example` (commit this; real `.env` is gitignored)" (the verbatim template with every key)
    - RESEARCH.md → "Recommended Project Structure" (.gitignore MUST include .env)
    - PITFALLS.md → Pitfall 18 / "Security Mistakes" (no secrets in repo/image; Docker Hub access token not password)
  </read_first>
  <action>
    Create `.env.example` by adapting the verbatim template in RESEARCH.md's `.env.example` section. It MUST contain every key the compose files interpolate: PIA_USER, PIA_PASSWORD, PIA_PF_REGION (default `CA Toronto`, with the inline comment that US is never valid), SYNOBRIDGE_CIDR, GLUETUN_API_KEY, SLSKD_API_KEY (with the documented `role=...;cidr=...;<16-255 char>` format note), LIDARR_API_KEY, DOCKERHUB_USER, PUID (1026 default), PGID (100 default), TZ (America/Toronto). Values are blank or safe defaults only — NO real secrets. Create `.gitignore` (or append if it exists) so it ignores `.env` (and `*.env` except `.env.example`). Do NOT create the real `.env` in the repo — it lives only on the NAS (Task 1/2). Document in the SUMMARY that the real `.env` is host-local and chmod 600.
  </action>
  <verify>
    <automated>test -f .env.example && grep -q 'PIA_USER' .env.example && grep -q 'SYNOBRIDGE_CIDR' .env.example && grep -q 'GLUETUN_API_KEY' .env.example && grep -q 'DOCKERHUB_USER' .env.example && grep -vq '^#' .gitignore | true; grep -Eq '^\.env$|^/?\.env$|\.env' .gitignore && echo ENV_OK</automated>
  </verify>
  <done>.env.example lists every compose-interpolated key with no real values; .gitignore ignores .env; no real .env is tracked by git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner → git repo | Secrets must never cross into version control |
| owner → NAS .env | Real credentials live only host-local (chmod 600) |
| registry → NAS | Image pulls must be pinned by digest (supply-chain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | PIA creds / API keys committed to git | mitigate | `.gitignore` ignores `.env`; only `.env.example` (no values) committed; real `.env` host-local chmod 600 (Task 3) |
| T-01-02 | Tampering | Supply-chain image swap (gluetun/slskd) | mitigate | Capture amd64 digests in Task 2; later plans pin `@sha256` (Package Legitimacy Audit Wave-0 gate) |
| T-01-03 | Spoofing/Tampering | gluetun control server unauthenticated | mitigate | Generate apikey via `genkey` (Task 2); consumed by Plan 02 auth wiring |
| T-01-SC | Tampering | npm/pip/docker installs | mitigate | All four images are first-party (RESEARCH legitimacy table); slopcheck not run → digest-pin is the substitute control; no `[ASSUMED]`/`[SUS]` packages introduced |
</threat_model>

<verification>
- NAS-RECON.md exists with CIDR, PUID/PGID, *arr mount shape, HARDLINK OK, tun presence, two amd64 digests, chosen non-US region.
- .env.example committed with all keys; .gitignore ignores .env; `git status` shows no tracked .env.
</verification>

<success_criteria>
A3 and A4 are resolved and recorded; INFRA-06 single-FS hardlink capability is proven on the NAS; the secrets surface is bootstrapped without leaking anything into git; image digests are pinned-ready. Later plans can interpolate every value.
</success_criteria>

<output>
Create `.planning/phases/phase-1/01-01-SUMMARY.md` when done. Record the resolved CIDR/PUID/PGID/region, the *arr mount decision (single vs realign), the captured digests, and confirmation that no secret entered git.
</output>
