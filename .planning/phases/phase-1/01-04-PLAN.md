---
phase: 01-vpn-routed-networking-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - docker-compose.yml
  - scripts/smoke-test.sh
autonomous: false
requirements: [INFRA-03, INFRA-04, INFRA-06]
must_haves:
  truths:
    - "`docker compose up -d` from the single docker-compose.yml brings gluetun + slskd + curator online (all Up/healthy)."
    - "Curator (synobridge member) reaches Lidarr by container name (http://lidarr:8686 returns 200 with X-Api-Key)."
    - "Curator reaches slskd via http://gluetun:5030 (never http://slskd) and slskd's API is alive from synobridge."
    - "Curator can read the shared /data tree (GET /readyz: data_mount_present && data_readable true)."
    - "A file written by slskd into /data is hardlink-capable across the tree and owned by the *arr PUID/PGID."
    - "The published Curator image contains no baked secrets (docker history scan clean)."
    - "All four ROADMAP success criteria pass via scripts/smoke-test.sh → Go/No-Go is GO."
  artifacts:
    - path: "docker-compose.yml"
      provides: "Complete single-file stack: gluetun + slskd + curator + external synobridge network"
      contains: "curator"
    - path: "scripts/smoke-test.sh"
      provides: "Runnable NAS go/no-go checklist mapping to the four ROADMAP success criteria"
      min_lines: 30
  key_links:
    - from: "curator SLSKD_URL"
      to: "gluetun:5030"
      via: "published slskd port on synobridge — NEVER http://slskd"
      pattern: "http://gluetun:5030"
    - from: "curator /data:ro mount"
      to: "/volume1/data"
      via: "identical /data path across slskd + curator + *arr (atomic hardlinks)"
      pattern: "/volume1/data:/data"
    - from: "curator image"
      to: "Docker Hub ${DOCKERHUB_USER}/curator"
      via: "compose pulls the CI-built image (INFRA-04)"
      pattern: "/curator"
---

<objective>
Complete `docker-compose.yml` with the `curator` service (synobridge member, pulling the CI-built Docker Hub image, mounting `/data` identically) and the top-level external `synobridge` network, then write `scripts/smoke-test.sh` (the RESEARCH go/no-go checklist as a runnable script) and execute the full smoke test on the NAS. This is the phase gate: all four ROADMAP success criteria must pass (INFRA-03 reachability, INFRA-04 single-compose-up, INFRA-06 /data + PUID/PGID + hardlink), reusing the INFRA-01/02 results proven in Plan 02.

Purpose: Assemble the substrate into one declarative file and prove it comes online end-to-end — the Phase 1 Go/No-Go. This is what `/gsd:verify-work` will check against.
Output: completed docker-compose.yml (curator + networks), scripts/smoke-test.sh, and a passing Go/No-Go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/phase-1/RESEARCH.md
@.planning/phases/phase-1/NAS-RECON.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- curator service contract from RESEARCH.md "Annotated docker-compose.yml". -->
curator: image ${DOCKERHUB_USER}/curator:latest (pin sha tag in prod); networks:[synobridge];
  depends_on:[gluetun]; env: SLSKD_URL="http://gluetun:5030" (NEVER http://slskd),
  SLSKD_API_KEY, GLUETUN_CONTROL_URL="http://gluetun:8000", GLUETUN_API_KEY,
  LIDARR_URL="http://lidarr:8686", LIDARR_API_KEY, TZ; ports:["8674:8674"];
  volumes: /volume1/docker/curator/config:/config and /volume1/data:/data:ro (Phase-1 read-only proof).
networks: synobridge: { external: true }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add curator service + external synobridge network to docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <read_first>
    - RESEARCH.md → "Annotated `docker-compose.yml`" (curator service + networks block, verbatim)
    - ARCHITECTURE.md → Anti-Pattern 1 (do NOT put curator in gluetun netns), "Direction 1"
    - PITFALLS.md → Pitfall 5/11 (curator stays plain synobridge member), Pitfall 12 (ownership)
  </read_first>
  <action>
    Append the `curator` service to the existing docker-compose.yml (from Plan 02) per the RESEARCH annotated block: `image: ${DOCKERHUB_USER}/curator:latest` (note in a comment that a sha-pinned tag is preferred in prod), `networks: [synobridge]` (plain member — NOT network_mode service:gluetun; Anti-Pattern 1), `depends_on: [gluetun]`, env with `SLSKD_URL: "http://gluetun:5030"` (add the comment "NEVER http://slskd"), `SLSKD_API_KEY`, `GLUETUN_CONTROL_URL: "http://gluetun:8000"`, `GLUETUN_API_KEY`, `LIDARR_URL: "http://lidarr:8686"`, `LIDARR_API_KEY`, `TZ`, `ports: ["8674:8674"]`, and volumes `/volume1/docker/curator/config:/config` + `/volume1/data:/data:ro` (Phase-1 read-only proof per the threat model's least-privilege note). Add the top-level `networks: { synobridge: { external: true } }` block if Plan 02 did not already add it (do not duplicate — there must be exactly one). Confirm the final file is a single valid compose defining exactly gluetun + slskd + curator.
  </action>
  <verify>
    <automated>grep -q 'curator' docker-compose.yml && grep -q 'http://gluetun:5030' docker-compose.yml && ! grep -q 'http://slskd' docker-compose.yml && [ "$(grep -c 'external: true' docker-compose.yml)" = "1" ] && echo CURATOR_OK; docker compose config >/dev/null 2>&1 && echo COMPOSE_VALID || echo "run compose config on NAS"</automated>
  </verify>
  <done>docker-compose.yml is one valid file with gluetun + slskd + curator and a single external synobridge network; curator uses gluetun:5030 (never slskd) and mounts /data:ro.</done>
</task>

<task type="auto">
  <name>Task 2: Write scripts/smoke-test.sh (the runnable go/no-go checklist)</name>
  <files>scripts/smoke-test.sh</files>
  <read_first>
    - RESEARCH.md → "Phase 1 Smoke-Test / Go-No-Go Checklist" (all four criteria, verbatim commands) and the "Single /data Mount" hardlink block
    - ROADMAP.md → Phase 1 four success criteria
    - PITFALLS.md → "Looks Done But Isn't" checklist
  </read_first>
  <action>
    Create `scripts/smoke-test.sh` (bash, `set -euo pipefail`, sources `.env`) that runs the RESEARCH go/no-go checklist as numbered, asserting steps mapping 1:1 to the four ROADMAP criteria: Criterion 1 (1a egress PIA non-US vs home, 1b non-zero PF port + non-US via authed control server, 1c kill-switch fail-closed); Criterion 2 (2a slskd listen==PF, 2b restart re-sync after `docker restart gluetun slskd && sleep 60`); Criterion 3 (3a slskd reachable via `gluetun:5030` from synobridge, 3b curator→lidarr by name 200, 3c curator→slskd via gluetun, 3d CI green reminder, 3e `docker history` no baked secrets); Criterion 4 (4a `docker compose up -d && docker compose ps` all Up, 4b curator `/readyz` data_mount_present+readable true, 4c slskd ownership == PUID/PGID, 4d hardlink across /data succeeds). Each check prints PASS/FAIL and the script exits non-zero on any hard NO-GO (leak on 1c, US on 1b, port 0, stale port after 2b, baked secrets on 3e, HARDLINK FAILED on 4d). Make it idempotent and re-runnable.
  </action>
  <verify>
    <automated>test -f scripts/smoke-test.sh && bash -n scripts/smoke-test.sh && grep -q 'gluetun:5030' scripts/smoke-test.sh && grep -q 'portforward' scripts/smoke-test.sh && grep -qi 'hardlink' scripts/smoke-test.sh && grep -q '/readyz' scripts/smoke-test.sh && echo SMOKE_SCRIPT_OK</automated>
  </verify>
  <done>scripts/smoke-test.sh passes `bash -n`, covers all four criteria with PASS/FAIL output, and exits non-zero on any hard NO-GO condition.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run full stack from one compose on the NAS → Go/No-Go gate</name>
  <what-built>Complete single-file stack deployed on the NAS; smoke-test.sh proves INFRA-03/04/06 and re-confirms INFRA-01/02 — the Phase 1 Go/No-Go.</what-built>
  <read_first>
    - RESEARCH.md → "Phase 1 Smoke-Test / Go-No-Go Checklist" (Go/No-Go line)
    - RESEARCH.md → "Sequencing & Gotchas" (Top failure modes → catching check table)
  </read_first>
  <how-to-verify>
    On the NAS, from the repo with `.env` present: ensure the CI image is pushed (Plan 03), then `docker compose pull && docker compose up -d` and `docker compose ps` (all services Up/healthy — INFRA-04). Then run `bash scripts/smoke-test.sh` and read the summary. Specifically confirm: 3a `curl http://gluetun:5030/` from a synobridge box returns 200/302/401 (alive); 3b `docker exec curator sh -c 'curl -s -o /dev/null -w "%{http_code}" -H "X-Api-Key: $LIDARR_API_KEY" http://lidarr:8686/api/v1/system/status'` == 200 (Curator→*arr by name, INFRA-03); 3c curator→`gluetun:5030` alive; 3e `docker history --no-trunc ${DOCKERHUB_USER}/curator:latest | grep -iE 'PIA_|API_KEY|PASSWORD'` finds nothing; 4b curator `/readyz` shows data_mount_present && data_readable true (INFRA-06); 4c slskd `ls -ld /data && id` matches PUID/PGID; 4d hardlink across /data succeeds. The script must exit 0 (GO). Any hard NO-GO (IP leak, US region, port 0, stale port post-restart, baked secrets, HARDLINK FAILED) is a Phase-1 blocker — paste the failing output.
  </how-to-verify>
  <resume-signal>Type "approved" if `scripts/smoke-test.sh` exits GO with all four criteria passing; otherwise paste the failing check(s).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curator → *arr (synobridge) | LAN control-plane traffic, by container name, never via VPN |
| curator → slskd (gluetun:5030) | Only path into the VPN netns; never http://slskd |
| curator → /data | Phase-1 read-only mount (least privilege until later phases need writes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Information Disclosure | slskd/curator exposed to WAN | mitigate | Ports published to LAN/synobridge only; no NAS firewall port-forward; LAN/Tailscale access only (PITFALLS #18) |
| T-01-12 | Tampering | curator write access to library before needed | mitigate | `/data` mounted `:ro` for the Phase-1 curator stub (least privilege) |
| T-01-13 | Information Disclosure | Baked secrets reach the running image | mitigate | smoke-test 3e `docker history` scan must be clean (Task 3) |
| T-01-14 | Elevation | curator in VPN netns blinds it / over-couples | mitigate | curator stays plain synobridge member (Anti-Pattern 1), not network_mode service:gluetun |
| T-01-SC | Tampering | image pull | mitigate | gluetun/slskd digest-pinned (Plan 02); curator image is first-party (own CI); no third-party package installs added here |
</threat_model>

<verification>
- `docker compose config` valid; `docker compose up -d` brings all three services Up/healthy.
- `scripts/smoke-test.sh` exits 0 (GO) with all four ROADMAP criteria passing.
</verification>

<success_criteria>
INFRA-03: Curator reaches Lidarr by name AND slskd via gluetun:5030; FIREWALL_OUTBOUND_SUBNETS correct. INFRA-04: the stack comes online from one compose pulling the Docker Hub image. INFRA-06: /data identical mount, hardlink-capable, correct PUID/PGID. Combined with Plan 02 (INFRA-01/02) and Plan 03 (INFRA-05), all six requirements and all four ROADMAP success criteria are satisfied — Go/No-Go = GO.
</success_criteria>

<output>
Create `.planning/phases/phase-1/01-04-SUMMARY.md` when done. Record the `docker compose ps` state, each criterion's PASS/FAIL, the hardlink + ownership result, the docker-history scan result, and the final Go/No-Go verdict.
</output>
