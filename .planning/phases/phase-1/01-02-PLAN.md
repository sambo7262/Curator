---
phase: 01-vpn-routed-networking-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docker-compose.yml
files_created_partial: true
autonomous: false
requirements: [INFRA-01, INFRA-02, INFRA-03]
must_haves:
  truths:
    - "From inside slskd's namespace the public IP is the PIA IP (country != US), not the home IP, with no DNS leak."
    - "If gluetun is stopped, slskd has zero network egress (fail-closed kill-switch verified)."
    - "gluetun obtains a non-zero forwarded port on a non-US PF region via its authed control server."
    - "slskd self-applies that forwarded port as its Soulseek listen port AND re-syncs after a gluetun+slskd restart (not just first boot)."
    - "slskd's web/API port (5030) is published on the gluetun service and reachable from synobridge at http://gluetun:5030."
    - "FIREWALL_OUTBOUND_SUBNETS is set to the synobridge CIDR and does NOT overlap the VPN tunnel CIDR."
  artifacts:
    - path: "docker-compose.yml"
      provides: "gluetun (PIA OpenVPN, kill-switch, PF, control-server auth) + slskd (shared netns, native VPN PF) service definitions"
      contains: "network_mode: \"service:gluetun\""
  key_links:
    - from: "slskd SLSKD_VPN_GLUETUN_API_KEY"
      to: "gluetun HTTP_CONTROL_SERVER_AUTH_DEFAULT_ROLE apikey"
      via: "identical ${GLUETUN_API_KEY} on both services (else slskd 401)"
      pattern: "GLUETUN_API_KEY"
    - from: "gluetun ports: 5030"
      to: "synobridge consumers (Curator)"
      via: "port published on the netns owner (gluetun), not slskd"
      pattern: "5030:5030"
    - from: "gluetun FIREWALL_OUTBOUND_SUBNETS"
      to: "synobridge LAN reachability"
      via: "${SYNOBRIDGE_CIDR} from NAS-RECON.md"
      pattern: "FIREWALL_OUTBOUND_SUBNETS"
---

<objective>
Define the two VPN-critical services in `docker-compose.yml`: **gluetun** (PIA over OpenVPN, kill-switch on, port forwarding, control-server apikey auth, FIREWALL_OUTBOUND_SUBNETS, `/gluetun` persisted) and **slskd** (`network_mode: service:gluetun`, native gluetun PF sync, `user:`+`SLSKD_UMASK`, identical `/data` mount). Then verify the four hardest correctness properties on the NAS: non-US PIA egress, fail-closed kill-switch, non-zero forwarded port, and PF auto-sync surviving a restart. This is the riskiest plan in the phase (INFRA-01/02/03).

Purpose: This is the privacy chokepoint. The single most likely silent failure is gluetun v3.40+ control-server auth (slskd 401 → port sync never happens). Getting the apikey + FIREWALL_OUTBOUND_SUBNETS + non-US region right here is what makes Soulseek both private and reachable.
Output: gluetun + slskd services in docker-compose.yml (Plan 03 adds curator + networks block; Plan 04 assembles and full-smoke-tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/phase-1/RESEARCH.md
@.planning/phases/phase-1/NAS-RECON.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Verified env-var contracts the executor MUST use verbatim. Source: RESEARCH.md "Annotated docker-compose.yml" + slskd docs/vpn.md (do NOT explore the codebase — there is none). -->
gluetun (env): VPN_SERVICE_PROVIDER="private internet access", VPN_TYPE="openvpn",
  OPENVPN_USER, OPENVPN_PASSWORD, SERVER_REGIONS (non-US PF), VPN_PORT_FORWARDING="on",
  VPN_PORT_FORWARDING_PROVIDER="private internet access",
  FIREWALL_OUTBOUND_SUBNETS=${SYNOBRIDGE_CIDR}, GLUETUN_HTTP_CONTROL_SERVER_ENABLE="on",
  HTTP_CONTROL_SERVER_AUTH_DEFAULT_ROLE='{"auth":"apikey","apikey":"${GLUETUN_API_KEY}"}', TZ.
  cap_add: [NET_ADMIN]; devices: [/dev/net/tun:/dev/net/tun]; networks: [synobridge];
  ports: ["5030:5030"]; volume: /volume1/docker/gluetun:/gluetun.
slskd (env): SLSKD_VPN="true", SLSKD_VPN_PORT_FORWARDING="true",
  SLSKD_VPN_GLUETUN_URL="http://localhost:8000", SLSKD_VPN_GLUETUN_API_KEY=${GLUETUN_API_KEY},
  SLSKD_API_KEY, SLSKD_UMASK="002", TZ. network_mode:"service:gluetun" (NO ports/networks here);
  user:"${PUID}:${PGID}"; depends_on gluetun condition:service_healthy;
  volumes: /volume1/docker/slskd:/app and /volume1/data:/data.
Control-server port = 8000 (localhost inside shared netns). PF route = /v1/portforward.
Do NOT set SLSKD_SLSK_LISTEN_PORT (native PF sets it dynamically).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author gluetun + slskd services in docker-compose.yml (digest-pinned)</name>
  <files>docker-compose.yml</files>
  <read_first>
    - RESEARCH.md → "Annotated `docker-compose.yml`" (adapt verbatim; this is the source of truth)
    - RESEARCH.md → Pitfall 1 (control-server auth), Pitfall 3 (/gluetun persist), Pitfall 5 (netns/synobridge), Pitfall 6 (slskd user model)
    - NAS-RECON.md (Plan 01) → SYNOBRIDGE_CIDR, PUID/PGID, gluetun+slskd amd64 digests
    - ARCHITECTURE.md → "Direction 1/2" + "Failure / kill-switch semantics"
  </read_first>
  <action>
    Create `docker-compose.yml` with the `gluetun` and `slskd` services exactly per the RESEARCH "Annotated docker-compose.yml" block, with these non-negotiable specifics: gluetun uses `image: qmcgaw/gluetun:v3.41.1@sha256:<digest from NAS-RECON>`, `cap_add: [NET_ADMIN]`, `devices: [/dev/net/tun:/dev/net/tun]`, `networks: [synobridge]`, `ports: ["5030:5030"]`, `volumes: [/volume1/docker/gluetun:/gluetun]`, and the full verified env set (VPN_TYPE openvpn — NOT wireguard, per A2; SERVER_REGIONS=${PIA_PF_REGION}; VPN_PORT_FORWARDING on; FIREWALL_OUTBOUND_SUBNETS=${SYNOBRIDGE_CIDR}; GLUETUN_HTTP_CONTROL_SERVER_ENABLE on; HTTP_CONTROL_SERVER_AUTH_DEFAULT_ROLE apikey JSON using ${GLUETUN_API_KEY}). slskd uses `image: slskd/slskd:0.25.1@sha256:<digest>`, `network_mode: "service:gluetun"` with NO `ports:` and NO `networks:` block (Pitfall 5 — compose error otherwise), `user: "${PUID}:${PGID}"` (Pitfall 6 — NOT PUID/PGID env), `depends_on: gluetun condition: service_healthy`, the SLSKD_VPN_* env set pointing at `http://localhost:8000` with `SLSKD_VPN_GLUETUN_API_KEY: ${GLUETUN_API_KEY}` (MUST equal gluetun's apikey or 401), `SLSKD_UMASK: "002"`, and volumes `/volume1/docker/slskd:/app` + `/volume1/data:/data`. Do NOT set a static Soulseek listen port. Leave the `curator` service and the top-level `networks:` block for Plan 03/04 (note in a comment that they are appended there) — OR add an `external: true` synobridge networks block now if it makes the file valid standalone; coordinate so Plan 03 does not duplicate it. Add the code comment "NEVER http://slskd — slskd has no synobridge hostname" near slskd for downstream readers.
  </action>
  <verify>
    <automated>grep -q 'network_mode: "service:gluetun"' docker-compose.yml && grep -q 'VPN_TYPE: "openvpn"' docker-compose.yml && grep -q 'HTTP_CONTROL_SERVER_AUTH_DEFAULT_ROLE' docker-compose.yml && grep -q 'FIREWALL_OUTBOUND_SUBNETS' docker-compose.yml && grep -q 'sha256:' docker-compose.yml && ! grep -E 'slskd:(\s*)$' docker-compose.yml | grep -q 'ports:' ; docker compose -f docker-compose.yml config >/dev/null 2>&1 && echo COMPOSE_VALID || echo "compose config: run on NAS (no docker here is OK if syntax grep passed)"</automated>
  </verify>
  <done>docker-compose.yml defines gluetun + slskd with digest-pinned images, OpenVPN PIA + PF + kill-switch + control-server auth on gluetun, shared-netns slskd with native PF sync and user:/umask, and no ports/networks on slskd.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: NAS smoke-test — VPN egress, kill-switch fail-closed, PF non-zero + restart re-sync</name>
  <what-built>gluetun+slskd services deployed on the NAS; this verifies INFRA-01 (privacy + fail-closed) and INFRA-02 (PF auto-sync surviving restart) — the four hardest properties.</what-built>
  <read_first>
    - RESEARCH.md → "Phase 1 Smoke-Test / Go-No-Go Checklist" sections "Criterion 1" (1a/1b/1c) and "Criterion 2" (2a/2b)
    - PITFALLS.md → "Looks Done But Isn't" (VPN privacy, port forwarding, port re-sync rows)
  </read_first>
  <how-to-verify>
    On the NAS, bring up just gluetun + slskd (`docker compose up -d gluetun slskd`), then run the RESEARCH Criterion-1 and Criterion-2 checks in order:
    1. `docker exec slskd sh -c 'wget -qO- https://ipinfo.io/ip'` → a PIA IP, country != US, != home IP (compare to `curl -s https://ipinfo.io/ip` on the host).
    2. `docker exec gluetun wget -qO- --header="X-Api-Key: $GLUETUN_API_KEY" http://localhost:8000/v1/portforward` → `{"port": <nonzero>}`; and `/v1/publicip/ip` → non-US country. A 401 here means the control-server auth is wrong (Pitfall 1) — fix before continuing.
    3. Kill-switch: `docker stop gluetun` then `docker exec slskd sh -c 'wget -T 5 -qO- https://ipinfo.io/ip'` → MUST fail ("FAIL-CLOSED OK"); `docker start gluetun` to restore.
    4. PF sync: `docker logs slskd 2>&1 | grep -i -E 'listen|forwarded|vpn' | tail -5` → slskd's listen port equals the /v1/portforward value.
    5. Restart re-sync (the success-criterion #2 test, not just first boot): `docker restart gluetun slskd && sleep 60`, re-query /v1/portforward and `docker logs slskd | grep -i listen | tail -2` → listen port re-tracks the (possibly new) PF port.
    NO-GO if: any IP leak on step 3, US country on step 2, port `0` on step 2, or a stale/missing listen port after step 5.
  </how-to-verify>
  <resume-signal>Type "approved" if non-US egress + fail-closed + non-zero PF + restart re-sync all pass; otherwise paste the failing output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slskd → Internet | All Soulseek egress must traverse the PIA tunnel only (fail-closed) |
| slskd → gluetun control server | localhost:8000 inside the shared netns; must be authenticated |
| synobridge → gluetun:5030 | LAN consumers reach slskd's API only via gluetun's published port |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | VPN IP/DNS leak if tunnel drops | mitigate | gluetun kill-switch (default-deny firewall); smoke-test 1c proves zero egress when gluetun stopped (Task 2) |
| T-01-05 | Tampering/Elevation | Unauthenticated gluetun control server (v3.40+ all routes private) | mitigate | HTTP_CONTROL_SERVER_AUTH_DEFAULT_ROLE apikey on gluetun + matching SLSKD_VPN_GLUETUN_API_KEY on slskd; 401 check in Task 2 step 2 |
| T-01-06 | Information Disclosure | No forwarded port → leecher block / degraded sharing | mitigate | non-US PF region (SERVER_REGIONS); assert non-zero port (Task 2 step 2) |
| T-01-07 | Elevation | Over-privileged containers | accept/mitigate | NET_ADMIN only on gluetun (required for tun); slskd runs as media user via `user:` not root |
| T-01-SC | Tampering | docker image installs | mitigate | gluetun/slskd pinned by `@sha256` digest (Task 1); first-party images per legitimacy audit |
</threat_model>

<verification>
- docker-compose.yml passes `docker compose config` on the NAS.
- Criterion-1 (1a/1b/1c) and Criterion-2 (2a/2b) smoke checks pass per Task 2.
</verification>

<success_criteria>
INFRA-01: slskd egress is the PIA IP (non-US), no DNS leak, fail-closed on tunnel drop. INFRA-02: non-zero forwarded port self-applied by slskd and re-synced after restart. INFRA-03 (partial): slskd reachable via gluetun:5030 with FIREWALL_OUTBOUND_SUBNETS set correctly (full Curator→*arr reachability proven in Plan 04).
</success_criteria>

<output>
Create `.planning/phases/phase-1/01-02-SUMMARY.md` when done. Record the observed PIA country, the forwarded port value, the fail-closed result, and the post-restart re-sync result.
</output>
