---
phase: 05-autonomy-sharing-self-recovery
plan: 05
type: execute
wave: 3
depends_on: ["05-03", "05-04"]
files_modified:
  - app/main.py
  - app/core/status_page.py
  - app/tests/test_status_page.py
  - DEPLOY.md
  - .env.example
  - .planning/phases/phase-5/05-05-LIVE-PROBE.md
autonomous: false
requirements: [REL-03, REL-01, SHARE-02, GAP-03]
must_haves:
  truths:
    - "GET /status renders a bare server-rendered HTML page listing stuck/quarantined/permanently-unavailable items with counts + reasons + healthy throughput"
    - "GET /status.json returns the same data as JSON (the Phase-6 widget contract) including shares_ok"
    - "Every ledger string rendered into HTML passes through html.escape (XSS defense)"
    - "The scheduler starts on app startup (after reconcile_on_startup) and stops cleanly on shutdown"
    - "The live slskd application.shares.files shape + the *arr queue album-id field are confirmed and reconciled into the offline fixtures (A2/A3)"
  artifacts:
    - path: "app/main.py"
      provides: "scheduler lifecycle wiring + GET /status + GET /status.json"
      contains: "/status"
    - path: "app/core/status_page.py"
      provides: "render_status_html(snapshot) with html.escape"
      contains: "escape"
    - path: "DEPLOY.md"
      provides: "Phase-5 env tunables + migration_0003 redeploy note + staged-rollout steps"
      contains: "ACQ_ENABLED"
  key_links:
    - from: "app/main.py _startup"
      to: "reconcile_on_startup + Scheduler.start"
      via: "boot reconcile then daemon start"
      pattern: "reconcile_on_startup|Scheduler"
    - from: "app/main.py /status"
      to: "repo.status_counts + repo.list_by_status + html.escape"
      via: "ledger snapshot -> escaped HTML"
      pattern: "status_counts|escape"
---

<objective>
Close Phase 5: wire the scheduler + reconcile into the FastAPI lifecycle, serve the REL-03 status surface (bare server-rendered `GET /status` HTML + `GET /status.json`), and run the A2/A3 live-confirm checkpoint on the NAS (the slskd application/shares shape + the *arr queue album-id field) — reconciling the offline fixtures to reality, exactly as Phase 4's 04-05 pinned A1/A2/A3. Update DEPLOY.md / .env.example with the new tunables + the migration_0003 redeploy note + the staged-rollout procedure.

Purpose: REL-03 (surface stuck items) + the final REL-01 wiring (the daemon actually starts) ship here. The live checkpoint pins the two LOW-risk assumed shapes (A2 queue field, A3 shares JSON) so SHARE-02/GAP-03 rest on confirmed truth — but all offline code already uses the research-confirmed shapes so nothing is blocked. The owner's D-06 acceptance test (first capped daemon pass at MAX_CONCURRENT=1) is the manual rollout that this plan enables, not a code task.
Output: main.py lifecycle + status routes, core/status_page.py, test_status_page.py, the live-probe checkpoint + 05-05-LIVE-PROBE.md, and the DEPLOY.md / .env.example deltas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-5/05-CONTEXT.md
@.planning/phases/phase-5/05-RESEARCH.md
@.planning/phases/phase-5/05-PATTERNS.md
@.planning/phases/phase-5/05-VALIDATION.md
@.planning/phases/phase-4/04-05-LIVE-PROBE.md
@.planning/phases/phase-5/05-03-SUMMARY.md
@.planning/phases/phase-5/05-04-SUMMARY.md

<interfaces>
<!-- From the existing app + Waves 0-2. -->

From app/main.py (the lifecycle + lock to extend):
```python
_detect_lock = threading.Lock()
@app.on_event("startup")
def _startup():
    conn = connect(settings.db_path); run_migrations(conn); app.state.db = conn
@app.on_event("shutdown")
def _shutdown():
    conn = getattr(app.state, "db", None);  conn and conn.close()
# existing handlers: /healthz, /readyz, POST /detect (409 guard, lazy imports inside handlers)
```

From app/state/repo.py (Wave 0 / 05-01):
```python
def status_counts(conn) -> dict
def imported_recent(conn, since_iso) -> int
def list_by_status(conn, status) -> List[sqlite3.Row]
```

From app/core/scheduler.py + core/reconcile.py (Waves 1/2):
```python
class Scheduler:
    def __init__(self, app, settings); def start(self); def stop(self)
def reconcile_on_startup(conn, lock, build_adapters, settings) -> None
def ensure_shares(slskd, app_state) -> bool   # sets app.state.shares_ok
```

Status routes (RESEARCH Pattern 7, PATTERNS 356-376, D-12/REL-03):
```python
@app.get("/status.json")  -> {"counts", "stuck", "quarantined", "permanently_unavailable", "shares_ok", "throughput"}
@app.get("/status", response_class=HTMLResponse)  # html.escape EVERY ledger string (XSS)
```

A2/A3 to confirm live (from RESEARCH Assumptions Log):
- A3: GET /api/v0/application -> shares.files (int) ; PUT /api/v0/shares -> 204/409
- A2: GET /api/v1/queue -> records[].albumId (Lidarr) / bookId (Readarr)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: core/status_page.py + GET /status + GET /status.json (REL-03, D-12)</name>
  <read_first>
    - app/main.py (the /readyz JSON shape + the lazy-import-in-handler pattern + on_event)
    - .planning/phases/phase-5/05-PATTERNS.md "app/core/status_page.py" (No Analog — stdlib html.escape + f-string) + "app/main.py (MOD — ... /status)" section + RESEARCH Pattern 7
    - app/state/repo.py status_counts / list_by_status / imported_recent (Wave 0)
    - app/tests/test_health.py (the existing FastAPI TestClient route-test idiom) + test_status_page.py target
  </read_first>
  <files>app/core/status_page.py, app/main.py, app/tests/test_status_page.py</files>
  <behavior>
    - GET /status.json returns {counts: {status: n}, stuck: [...], quarantined: [...], permanently_unavailable: [...], shares_ok: bool, throughput: int(imported in last 24h)} read from the ledger via repo.status_counts / list_by_status / imported_recent.
    - Each item view (_row_view) exposes only neutral fields (app, id, title, reason/status) — no raw *arr JSON.
    - GET /status returns an HTMLResponse: a bare server-rendered page (no JS framework) listing the stuck/quarantined/permanently-unavailable rows with counts + reasons + the healthy-throughput number.
    - EVERY interpolated ledger string (title, reason, app, id) passes through html.escape — a title like `<img src=x onerror=alert(1)>` renders escaped, not as live markup (XSS/HTML-injection, V5/Pitfall).
    - Both routes are pure reads on app.state.db; a missing connection → 503 (mirror /detect's guard).
  </behavior>
  <action>
    Create app/core/status_page.py with `def render_status_html(snapshot: dict) -> str:` building the bare HTML from the status.json dict, passing EVERY ledger string through `from html import escape` (RESEARCH Pattern 7, PATTERNS 444). Keep it a pure function (ledger snapshot dict → HTML string) so it is offline-unit-testable without a live app. status_page.py is core — it speaks only the neutral snapshot dict + html.escape (no *arr/slskd wire vocab; the grep auto-scans it).
    In app/main.py add `@app.get("/status.json")` and `@app.get("/status", response_class=HTMLResponse)` per PATTERNS 361-376. Build the snapshot via `repo.status_counts(conn)`, `repo.list_by_status(conn, "stuck"/"quarantined"/"permanently-unavailable")`, `repo.imported_recent(conn, since_iso)` (since = now-24h via repo `_now_iso` style), and `app.state.shares_ok`. Add a small `_row_view(row)` mapping a sqlite3.Row to the neutral dict (app, id, title, reason). The HTML route delegates to `status_page.render_status_html(snapshot)`. Lazy-import inside handlers (the main.py:72-74 pattern) so the module parses offline. Guard a missing app.state.db with a 503 (mirror /detect).
    Write test_status_page.py (FastAPI TestClient + a temp-sqlite app.state.db seeded with a stuck/quarantined/permanently-unavailable row). This task creates the module and its route/render tests: (a) /status.json returns the four buckets + counts + shares_ok + throughput; (b) /status returns 200 text/html containing the item titles; (c) a row titled with `<script>`/`<img onerror=...>` is rendered ESCAPED (the raw `<script>` does NOT appear; `&lt;script&gt;` does) — the XSS assertion; (d) render_status_html is unit-tested directly on a hand-built snapshot dict (no app needed). Offline only. (Task 2 later ADDS a separate lifecycle test FUNCTION to this same module — do not write it here.)
    SECURITY: keep /status on the existing LAN/Tailscale-only firewalled port (no new exposure — it leaks library gaps/titles); document this in the route docstring (V4/Info-disclosure). No auth added (matches /healthz/detect posture).
  </action>
  <verify>
    <automated>cd app && python3 -m pytest tests/test_status_page.py tests/test_adapter_protocol.py -q</automated>
  </verify>
  <acceptance_criteria>
    - core/status_page.py defines `render_status_html(snapshot)` using html.escape on every interpolated string.
    - main.py serves GET /status (HTML) + GET /status.json (the four buckets + counts + shares_ok + throughput).
    - The XSS test proves a `<script>`/`<img onerror>` title is escaped (raw tag absent, escaped form present).
    - `cd app && python3 -m pytest tests/test_status_page.py -q` passes; `tests/test_adapter_protocol.py` passes (status_page.py firewall-clean).
  </acceptance_criteria>
  <done>The REL-03 status surface renders stuck/quarantined/permanently-unavailable items as escaped HTML + JSON; the firewall + XSS defenses hold.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the scheduler + reconcile into the FastAPI lifecycle (REL-01)</name>
  <read_first>
    - app/main.py (the existing _startup/_shutdown on_event handlers + _detect_lock to extend)
    - .planning/phases/phase-5/05-PATTERNS.md "app/main.py (MOD — scheduler lifecycle ...)" (the lifecycle wiring) + RESEARCH Code Examples (lines 504-526)
    - app/core/scheduler.py (Scheduler.start/stop) + core/reconcile.py (reconcile_on_startup) from Waves 1/2
    - app/tests/test_status_page.py (the module Task 1 CREATED — this task only APPENDS a new lifecycle test function to it)
    - app/tests/test_health.py / test_detect_endpoint.py (TestClient lifecycle test idiom)
  </read_first>
  <files>app/main.py, app/tests/test_status_page.py</files>
  <action>
    Extend app/main.py `_startup`: after `app.state.db = conn` (migration_0003 now runs via run_migrations), set `app.state.shares_ok = True`, call `reconcile_on_startup(conn, _detect_lock, build_adapters, settings)` (lazy-import build_adapters + reconcile_on_startup inside the handler), then construct + start the Scheduler: `app.state.scheduler = Scheduler(app, settings); app.state.scheduler.start()` (lazy-import Scheduler). Extend `_shutdown`: stop the scheduler BEFORE closing the connection (`sched = getattr(app.state, "scheduler", None); sched and sched.stop()`), then close the conn (existing). Reuse the existing `_detect_lock` as the shared writer lock so a manual `/detect` and a cycle never collide (the scheduler was built to take this lock in 05-04).
    ADD a DISTINCT lifecycle test FUNCTION to the EXISTING test_status_page.py (Task 1 already created the module with its route/XSS tests — do NOT recreate the file or those tests; append a separate, named function, e.g. `test_scheduler_lifecycle`): a TestClient context-manager startup builds app.state.scheduler and app.state.shares_ok without raising; shutdown stops it cleanly. Use a settings/env shape that does NOT actually run a live cycle (e.g. ACQ_ENABLED handling + a monkeypatched build_adapters/Scheduler or a very long interval so no real cycle fires during the test) — the test proves the WIRING, not a live acquisition. Keep Task 1's route/XSS tests and the existing /healthz, /readyz, /detect tests green.
    NOTE (proactively-flag-NAS-setup memory): this is the change that makes the daemon actually start on the NAS at deploy — Task 4 documents it in DEPLOY.md.
  </action>
  <verify>
    <automated>cd app && python3 -m pytest tests/test_status_page.py tests/test_health.py tests/test_detect_endpoint.py -q</automated>
  </verify>
  <acceptance_criteria>
    - main.py _startup calls reconcile_on_startup then Scheduler.start(); _shutdown calls scheduler.stop() before conn.close().
    - The scheduler shares the existing _detect_lock (no second lock/connection introduced in main.py).
    - A distinct `test_scheduler_lifecycle` function is added to test_status_page.py (separate from Task 1's route/XSS tests) and proves startup wires app.state.scheduler + shares_ok and shutdown stops cleanly without a live cycle firing.
    - Task 1's route/XSS tests and the existing /healthz, /readyz, /detect tests stay green.
  </acceptance_criteria>
  <done>The daemon starts after reconcile on app startup and stops cleanly on shutdown, sharing the single writer lock; existing endpoints unaffected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-confirm checkpoint — A3 slskd application/shares shape + A2 *arr queue album-id field</name>
  <read_first>
    - .planning/phases/phase-4/04-05-LIVE-PROBE.md (the Phase-4 live-probe format to mirror)
    - .planning/phases/phase-5/05-RESEARCH.md Assumptions Log A2/A3 (the exact shapes to confirm) + the recommended one-line curl probes
    - app/tests/fixtures/slskd/application.json + app/tests/fixtures/lidarr_queue.json (the [ASSUMED] fixtures from 05-02 to reconcile)
  </read_first>
  <what-built>
    The offline code already uses the research-confirmed shapes: slskd `get_shared_file_count` reads `GET /api/v0/application -> shares.files`; `get_queue_status` matches `GET /api/v1/queue -> records[].albumId == item.arr_id`. The slskd application + lidarr queue offline fixtures carry `[ASSUMED A2/A3]` provenance. This checkpoint pins those two LOW-risk shapes to live NAS truth and reconciles the fixtures + any field constant in ONE place (mirroring Phase-4 04-05's A1/A2/A3 reconciliation). Nothing offline is blocked on this — it is a truth-pin, not a build dependency.
  </what-built>
  <how-to-verify>
    On the NAS (LAN/Tailscale), run two probes and paste the outputs into a new `.planning/phases/phase-5/05-05-LIVE-PROBE.md` (mirror 04-05-LIVE-PROBE.md):
    1. slskd shares shape (A3):
       `curl -s http://<NAS-IP>:5030/api/v0/application -H "X-API-Key: $SLSKD_API_KEY" | jq '.shares'`
       Confirm it is an object with an integer `files` (and `directories`). Also confirm a rescan: `curl -s -o /dev/null -w "%{http_code}" -X PUT http://<NAS-IP>:5030/api/v0/shares -H "X-API-Key: $SLSKD_API_KEY"` returns 204 (or 409 if a scan is already running).
    2. *arr queue album-id field (A2):
       `curl -s 'http://<LIDARR-IP>:8686/api/v1/queue?page=1&pageSize=20' -H "X-Api-Key: $LIDARR_API_KEY" | jq '.records[0] | keys'`
       Confirm the per-record album identity field is `albumId` (and note the Readarr equivalent `bookId` if a Readarr queue is reachable).
    After pasting the live outputs, the executor reconciles `application.json` / `lidarr_queue.json` (and any named field constant) to the observed shapes and re-runs `tests/test_shares.py tests/test_lidarr_adapter.py -q`. If `shares.files` or `albumId` differs from the assumed shape, fix the adapter read + the fixture to the live truth, then re-pin the tests (truth → fixture → test, never weaken an assertion).
  </how-to-verify>
  <resume-signal>Type "approved" once the two probe outputs are pasted into 05-05-LIVE-PROBE.md and the fixtures/tests reconcile green, or describe any shape mismatch found.</resume-signal>
  <acceptance_criteria>
    - `.planning/phases/phase-5/05-05-LIVE-PROBE.md` exists with the live `shares` object output + the queue `keys` output.
    - `shares.files` is confirmed an integer; the rescan PUT status code is recorded (204/409).
    - The queue record's album-id field name is confirmed (`albumId`) and reconciled into the fixture/constant.
    - `cd app && python3 -m pytest tests/test_shares.py tests/test_lidarr_adapter.py -q` passes against the reconciled fixtures.
  </acceptance_criteria>
  <done>A2/A3 are pinned to live NAS truth in 05-05-LIVE-PROBE.md; the offline fixtures + tests reflect reality.</done>
</task>

<task type="auto">
  <name>Task 4: DEPLOY.md + .env.example — Phase-5 tunables, migration_0003 redeploy note, staged rollout</name>
  <read_first>
    - DEPLOY.md (the existing §"Step 8 — Phase 4 setup" + the env/redeploy structure to extend)
    - .env.example (the existing var block to extend)
    - .planning/phases/phase-5/05-RESEARCH.md "Runtime State Inventory" (migration_0003 runs on the live DB at redeploy) + "Project Constraints" (the proactively-flag-NAS-setup note)
    - .planning/phases/phase-5/05-CONTEXT.md D-05/D-06 (the staged rollout: dry-run → cap=1 → cap=3)
  </read_first>
  <files>DEPLOY.md, .env.example</files>
  <action>
    Add a DEPLOY.md §"Step 9 — Phase 5 setup" (mirror the Step 8 structure) covering: (1) the new env tunables (`ACQ_ENABLED`, `ACQ_DRY_RUN`, `MAX_CONCURRENT`, `ACQ_POLL_INTERVAL_SECONDS`, `ACQ_GRACE_SECONDS`, `ACQ_MAX_ATTEMPTS`, `ACQ_DORMANT_SECONDS`) with their defaults + one-line meanings; (2) the note that a redeploy RUNS migration_0003 on the live `/db/curator.sqlite` (preserving the ~1493 rows — back up the DB file first as a precaution, per the proactively-flag-NAS-setup memory); (3) the STAGED ROLLOUT procedure (D-05/D-06): step A = deploy with `ACQ_DRY_RUN=true` and observe the dry-run logs (would-be winners, zero side effects); step B = set `ACQ_DRY_RUN=false`, `MAX_CONCURRENT=1` and watch the FIRST capped daemon pass import ONE album end-to-end (search→download→ManualImport move→verify→purge) — this is the D-06 acceptance test; step C = raise `MAX_CONCURRENT=3` for steady state; (4) the kill-switch note: set `ACQ_ENABLED=false` to halt instantly (takes effect next cycle, no restart needed). Confirm slskd shares stay configured at `/data/media/music` + `/data/media/books` read-only (D-11) — Curator only verifies/rescans, never rewrites slskd.yml.
    Add the seven `ACQ_*` / `MAX_CONCURRENT` vars to `.env.example` with the default values + a short comment each. NO Pushover/Apprise token (D-13 — that is Phase 6).
  </action>
  <verify>
    <automated>grep -q "ACQ_ENABLED" DEPLOY.md && grep -q "migration_0003" DEPLOY.md && grep -q "MAX_CONCURRENT" .env.example && echo deploy-docs-ok</automated>
  </verify>
  <acceptance_criteria>
    - DEPLOY.md has a Phase-5 section documenting all 7 tunables, the migration_0003 redeploy/backup note, and the dry-run→cap=1→cap=3 staged rollout (D-05/D-06) + the kill-switch.
    - .env.example lists ACQ_ENABLED / ACQ_DRY_RUN / MAX_CONCURRENT / ACQ_POLL_INTERVAL_SECONDS / ACQ_GRACE_SECONDS / ACQ_MAX_ATTEMPTS / ACQ_DORMANT_SECONDS with defaults.
    - No notification/Pushover token added (D-13).
    - The verify command prints `deploy-docs-ok`.
  </acceptance_criteria>
  <done>The owner has a complete Phase-5 deploy + staged-rollout + kill-switch procedure and the migration_0003 redeploy is flagged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN/Tailscale → GET /status | The status page exposes library gaps + titles; must stay on the firewalled port, no internet exposure |
| peer/*arr title → /status HTML | Untrusted titles rendered into HTML are an injection surface |
| migration_0003 → live NAS DB | A redeploy runs the migration on the ~1493-row live DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-20 | Tampering | HTML/markup injection via a malicious title on /status | mitigate | html.escape on EVERY interpolated ledger string (Pattern 7, V5); proven by the XSS test (raw `<script>` absent, escaped form present) |
| T-05-21 | Info disclosure | /status exposing library gaps to the network | mitigate | Keep /status on the existing LAN/Tailscale-only firewalled port (no new exposure); documented in the route docstring; no auth added (matches existing posture) |
| T-05-22 | Tampering | migration_0003 on the live DB losing rows | mitigate | DEPLOY.md flags a DB backup before redeploy + the row-preservation test (05-01) already proves the rebuild preserves rows |
| T-05-23 | Info disclosure | shares/queue probe leaking the API key in a pasted log | accept | The live-probe curl uses `$SLSKD_API_KEY`/`$LIDARR_API_KEY` env refs; the executor pastes only the JSON body (`.shares` / `.records[0]|keys`), not the header — documented in the checkpoint |
| T-05-SC | Tampering | npm/pip installs | mitigate | Zero new packages; no Package Legitimacy Gate triggered |
</threat_model>

<verification>
- `cd app && python3 -m pytest -q` — full offline suite green (≥205 + all Phase-5 tests).
- `cd app && python3 -m pytest tests/test_adapter_protocol.py -q` — firewall clean over status_page.py + all new core modules.
- The XSS test proves /status escapes titles; the lifecycle test proves the daemon starts/stops; A2/A3 pinned in 05-05-LIVE-PROBE.md.
</verification>

<success_criteria>
- GET /status (HTML, escaped) + GET /status.json deliver the REL-03 stuck-item surface + the Phase-6 JSON contract.
- The scheduler starts after reconcile on startup and stops cleanly (REL-01 wiring complete).
- A2/A3 are pinned to live NAS truth and reconciled into the offline fixtures (SHARE-02/GAP-03 rest on confirmed shapes).
- DEPLOY.md / .env.example give the owner the staged-rollout + kill-switch + migration_0003 redeploy procedure.
</success_criteria>

<output>
Create `.planning/phases/phase-5/05-05-SUMMARY.md` when done.
</output>
</content>
