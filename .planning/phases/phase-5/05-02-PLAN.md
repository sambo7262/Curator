---
phase: 05-autonomy-sharing-self-recovery
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - app/adapters/slskd.py
  - app/adapters/lidarr.py
  - app/adapters/readarr.py
  - app/core/acquire.py
  - app/tests/test_shares.py
  - app/tests/test_infra_classify.py
  - app/tests/fakes.py
  - app/tests/fixtures/slskd/application.json
  - app/tests/fixtures/lidarr_queue.json
autonomous: true
requirements: [SHARE-01, SHARE-02, GAP-03, REL-02]
must_haves:
  truths:
    - "slskd client reads the shared-file count from GET /api/v0/application -> shares.files"
    - "slskd client triggers a rescan via PUT /api/v0/shares (204 -> True, 409 -> False)"
    - "Lidarr/Readarr expose get_queue_status returning a neutral bool for the D-02 race check"
    - "An infra-class (connection/timeout) fault during a decision-input fetch is distinguishable from a genuine not-found so it can avoid burning an attempt"
    - "All slskd/*arr wire keys for shares + queue stay inside app/adapters/"
  artifacts:
    - path: "app/adapters/slskd.py"
      provides: "get_shared_file_count + rescan_shares"
      contains: "def get_shared_file_count"
    - path: "app/adapters/lidarr.py"
      provides: "get_queue_status (primary, raises on fault)"
      contains: "def get_queue_status"
    - path: "app/adapters/readarr.py"
      provides: "get_queue_status (best-effort, degrades to False)"
      contains: "def get_queue_status"
    - path: "app/core/acquire.py"
      provides: "INFRA_EXC classifier seam so an infra fault on get_manifest/get_quality_profile is not a genuine stuck (A1)"
      contains: "INFRA_EXC"
  key_links:
    - from: "app/adapters/slskd.py get_shared_file_count"
      to: "slskd GET /api/v0/application"
      via: "self._base + /application, shares.files"
      pattern: "application|shares"
    - from: "app/adapters/lidarr.py get_queue_status"
      to: "Lidarr GET /api/v1/queue"
      via: "records match on item.arr_id"
      pattern: "queue|records"
---

<objective>
Build the adapter + classifier surface the Phase-5 core services compose: the slskd shares ensure/self-heal HTTP methods (D-10, SHARE-01/02), the `get_queue_status` D-02 race check on both *arr adapters (GAP-03), and the contained `acquire.py` `_safe_call` adjustment (RESEARCH open question A1) so an INFRA-class fault on the decision-input fetch is distinguishable from a genuine "not found" — the precondition for REL-02 (infra outages burn no attempt). All wire keys stay behind the firewall; core sees only neutral int/bool.

Purpose: Wave 1's `core/shares.py` consumes the neutral count/bool; the scheduler + reconcile consume `get_queue_status` and the INFRA_EXC classification. These adapter/seam shapes must exist and be offline-proven (fakes) BEFORE the services compose them.
Output: slskd shares methods, both adapters' get_queue_status, the acquire INFRA_EXC seam, the test_shares / test_infra_classify modules, the 05-02-owned tests/fakes.py FakeSlskd, and the slskd application + lidarr queue fixtures.

FILE OWNERSHIP NOTE (Wave 0 parallelism): this plan runs in parallel with 05-01, which solely owns `app/tests/conftest.py`. To keep zero same-wave file overlap, THIS plan does NOT edit conftest.py — its shared fake (`FakeSlskd`) lives in a NEW 05-02-owned `app/tests/fakes.py` module that its test modules import directly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-5/05-CONTEXT.md
@.planning/phases/phase-5/05-RESEARCH.md
@.planning/phases/phase-5/05-PATTERNS.md
@.planning/phases/phase-5/05-VALIDATION.md
@.planning/phases/phase-4/04-05-LIVE-PROBE.md

<interfaces>
<!-- Extracted from the existing codebase — use directly. -->

From app/adapters/slskd.py (the client to extend; `.get()`-defensive, capital X-API-Key, raise_for_status primary):
```python
class SlskdClient:
    def __init__(self, base_url, api_key, client: httpx.Client):
        self._base = base_url.rstrip("/") + "/api/v0"
        self._headers = {"X-API-Key": api_key}   # capital API (slskd-specific)
    # existing: search / search_state / search_responses / enqueue / transfer / cancel
    # host is settings.slskd_url (gluetun-published NAS:5030) — NEVER a container name (Pitfall 7)
```

From app/adapters/lidarr.py (the re-query idiom to mirror for get_queue_status):
```python
def __init__(self, base_url, api_key, client):
    self._base = base_url.rstrip("/"); self._headers = {"X-Api-Key": api_key}  # Servarr lowercase Api
def _paged(self, path: str) -> list: ...        # GET f"{self._base}/api/v1/{path}"
def verify_imported(self, item: "GapItem") -> bool: ...  # re-query GET /api/v1/wanted/missing-style
```

From app/adapters/base.py (the Protocol stub being implemented):
```python
def get_queue_status(self, item: GapItem) -> Any:
    """Phase 5 — fallback-only race check against the *arr download queue. Stubbed in Phase 2."""
```

From app/core/acquire.py (the seam to adjust — A1):
```python
def _safe_call(fn: Callable, arg) -> Any:
    try: return fn(arg)
    except Exception as e:   # CURRENTLY swallows ALL faults to None -> stuck (wrongly burns on *arr outage)
        log.info(...); return None
# called by: manifest = _safe_call(adapter.get_manifest, item.foreign_id)
#            profile = _safe_call(adapter.get_quality_profile, item.quality_profile_id)
#            if manifest is None or profile is None: -> set_status 'stuck'; return "stuck"
# acquire.py imports httpx LAZILY (build_acquire_clients imports it inside the function) — keep that discipline.
```

slskd shares API (RESEARCH §slskd Shares API, A3):
- Count: GET /api/v0/application -> body["shares"]["files"] (int)
- Rescan: PUT /api/v0/shares -> 204 (started) / 409 (already scanning)

*arr queue (RESEARCH get_queue_status, A2):
- GET /api/v1/queue?page=1&pageSize=100 -> body["records"][], match str(rec["albumId"])==item.arr_id (Lidarr); bookId (Readarr)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — test scaffolds + slskd/queue fixtures + tests/fakes.py FakeSlskd</name>
  <read_first>
    - app/tests/conftest.py (httpx_client factory + load_fixture — mirror the style; do NOT edit it, 05-01 owns it)
    - app/tests/test_slskd_client.py (the httpx.MockTransport pattern for slskd methods — no respx needed)
    - app/tests/fixtures/slskd/ (existing Phase-4 slskd fixtures for the shape/style)
    - .planning/phases/phase-5/05-VALIDATION.md (Wave 0 — test module names + the FakeSlskd application/shares fixture requirement)
  </read_first>
  <files>app/tests/test_shares.py, app/tests/test_infra_classify.py, app/tests/fakes.py, app/tests/fixtures/slskd/application.json, app/tests/fixtures/lidarr_queue.json</files>
  <action>
    Create `app/tests/fixtures/slskd/application.json` as a representative slskd `/api/v0/application` body containing `{"shares": {"files": 1234, "directories": 7, ...}}` (note provenance `[ASSUMED A3]` in an adjacent INDEX comment or a sibling note — the exact shape is live-confirmed in plan 05-05). Create `app/tests/fixtures/lidarr_queue.json` as a `{"records": [{"albumId": 42, ...}], "totalRecords": 1, ...}` envelope (note `[ASSUMED A2]` — albumId match field confirmed live in 05-05).
    Create a NEW `app/tests/fakes.py` (05-02-owned, so it does NOT collide with 05-01's conftest.py) exposing a `FakeSlskd` class with `get_shared_file_count()` / `rescan_shares()` returning injectable values (and a call counter so a test can assert rescan was called exactly once), plus the search/transfer/enqueue no-ops the later scheduler path will reuse. The shares + scheduler tests import `from tests.fakes import FakeSlskd` (or `from fakes import FakeSlskd` per the test import root) directly — no conftest fixture needed.
    Write test_shares.py (RED) asserting the Task-2 client-level behaviors (the ensure/self-heal CYCLE lands in 05-03). Write test_infra_classify.py (RED) asserting the Task-4 behaviors (INFRA_EXC tuple membership + that an infra fault on the decision-input fetch is classifiable distinctly from a genuine None).
    Offline only (Python 3.9 sandbox): use httpx.MockTransport for the real-client tests and FakeSlskd for orchestration; NO live slskd/*arr. Do NOT edit conftest.py (05-01 owns it this wave) — these tests only need the new fixtures + FakeSlskd, not the v0002-seed fixture.
  </action>
  <verify>
    <automated>cd app && python3 -m pytest tests/test_shares.py tests/test_infra_classify.py -q --co</automated>
  </verify>
  <acceptance_criteria>
    - `app/tests/test_shares.py` + `app/tests/test_infra_classify.py` + `app/tests/fakes.py` exist; the two test modules collect.
    - `app/tests/fixtures/slskd/application.json` has a `shares.files` integer; `app/tests/fixtures/lidarr_queue.json` has a `records` array with an `albumId`.
    - `app/tests/fakes.py` defines `FakeSlskd` with `get_shared_file_count` + `rescan_shares` (rescan call-counted).
    - conftest.py is UNCHANGED by this plan (no same-wave overlap with 05-01).
  </acceptance_criteria>
  <done>Shares + infra-classify test modules collect; the slskd application + lidarr queue fixtures and the 05-02-owned FakeSlskd exist; conftest untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: slskd shares methods — get_shared_file_count + rescan_shares</name>
  <read_first>
    - app/adapters/slskd.py (the existing SlskdClient — copy the self._base/self._headers/.get()-defensive/raise_for_status posture exactly)
    - .planning/phases/phase-5/05-PATTERNS.md "app/adapters/slskd.py (MOD ...)" section (the two method bodies)
    - .planning/phases/phase-5/05-RESEARCH.md slskd Shares API (lines 417-439, the confirmed endpoints + the 409 handling)
    - app/tests/test_shares.py (the RED target from Task 1)
  </read_first>
  <files>app/adapters/slskd.py, app/tests/test_shares.py</files>
  <behavior>
    - get_shared_file_count(): GET {base}/application; reads body["shares"]["files"]; returns it as int; absent/non-dict/non-int → 0 (defensive). Raises on a transport/HTTP fault (raise_for_status — primary posture, the caller classifies it as infra).
    - rescan_shares(): PUT {base}/shares; 204 → True (started); 409 → False (already scanning, treated as 'already healing', not an error); other non-2xx → raise.
    - Both send the capital X-API-Key header; the key is never logged/echoed into an exception (T-04-07).
  </behavior>
  <action>
    Add `get_shared_file_count(self) -> int` and `rescan_shares(self) -> bool` to SlskdClient per PATTERNS 263-278 and RESEARCH 417-435: GET `f"{self._base}/application"` then `.get()`-defensive walk `body.get("shares")` → `shares.get("files")` → return iff `isinstance(files, int)` else 0; PUT `f"{self._base}/shares"`, special-case `r.status_code == 409 -> return False`, else `raise_for_status()` then `return True`. Use `self._headers` (capital X-API-Key) + `timeout=30.0`. Same host posture: `self._base` from settings.slskd_url (gluetun port), never a container name.
    These wire keys (`application`, `shares`, `files`) stay in slskd.py — they MUST NOT leak into core (the firewall grep auto-scans core/).
    Turn test_shares.py's client-level cases GREEN (the ensure/self-heal cycle lands in 05-03's core/shares.py).
  </action>
  <verify>
    <automated>cd app && python3 -m pytest tests/test_shares.py tests/test_slskd_client.py -q</automated>
  </verify>
  <acceptance_criteria>
    - slskd.py defines `get_shared_file_count` (returns int, 0 on absent) and `rescan_shares` (True on 204, False on 409).
    - Both use `self._headers` and a 30s timeout; no key string in any log/exception path.
    - `cd app && python3 -m pytest tests/test_shares.py -q` passes the client-level count + rescan + 409 cases.
  </acceptance_criteria>
  <done>The slskd client reads the shared-file count and triggers a rescan, defensive + raise-primary, key never leaked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: get_queue_status on Lidarr (primary) + Readarr (best-effort)</name>
  <read_first>
    - app/adapters/lidarr.py verify_imported + _paged (the re-query idiom + the records-key handling style)
    - app/adapters/readarr.py (the best-effort swallow→safe-default posture for verify_imported — mirror it)
    - app/adapters/base.py get_queue_status stub (the Protocol signature to implement)
    - .planning/phases/phase-5/05-RESEARCH.md get_queue_status example (lines 556-568) + A2 note
    - app/tests/fixtures/lidarr_queue.json (the RED fixture from Task 1)
  </read_first>
  <files>app/adapters/lidarr.py, app/adapters/readarr.py, app/tests/test_lidarr_adapter.py, app/tests/test_readarr_adapter.py</files>
  <behavior>
    - Lidarr.get_queue_status(item): GET /api/v1/queue (page=1, pageSize=100); returns True iff any record's albumId (stringified) == item.arr_id (an active/queued Usenet grab exists → skip); False if no match; raises on a hard fault (primary — the scheduler classifies that raise as infra-skip, NOT a burned attempt).
    - Readarr.get_queue_status(item): mirrors best-effort over bookId; ANY fault degrades to False (never gate music, ARR-02).
    - The `records` / `albumId` / `bookId` wire keys stay INSIDE the adapters; the method returns a neutral bool.
  </behavior>
  <action>
    Implement `get_queue_status(self, item) -> bool` on LidarrAdapter per RESEARCH 556-568: `r = self._client.get(f"{self._base}/api/v1/queue", headers=self._headers, params={"page": 1, "pageSize": 100}, timeout=30.0)`; `r.raise_for_status()`; read `records = j.get("records", [])` when `isinstance(j, dict)`; `return any(str(rec.get("albumId")) == item.arr_id for rec in records if isinstance(rec, dict))`. Add an inline `# A2: confirm albumId field live (05-05)` provenance note.
    Implement `get_queue_status` on ReadarrAdapter mirroring verify_imported's best-effort posture: wrap the body in the swallow block, match on `bookId`, return False on ANY fault (never raise into the loop). Use the live re-query directly (not via an already-swallowing helper) so a 5xx degrades to False, the safe direction.
    Add tests to test_lidarr_adapter.py (match found → True; no match → False; 500 → raises) and test_readarr_adapter.py (match → True; fault → False, no raise) using the lidarr_queue fixture + httpx.MockTransport.
    FIREWALL: `records[` / `albumId` are in ARR_FIELD_NAMES; they MUST stay in the adapters. The method returns a neutral bool. Confirm the firewall grep stays clean.
  </action>
  <verify>
    <automated>cd app && python3 -m pytest tests/test_lidarr_adapter.py tests/test_readarr_adapter.py tests/test_adapter_protocol.py -q</automated>
  </verify>
  <acceptance_criteria>
    - lidarr.py + readarr.py each define `get_queue_status(self, item) -> bool`.
    - Lidarr raises on a 500; Readarr returns False on a fault (proven by tests).
    - A queue record matching item.arr_id → True; no match → False.
    - `cd app && python3 -m pytest tests/test_adapter_protocol.py -q` passes (firewall clean — `records[`/`albumId` only in adapters/).
  </acceptance_criteria>
  <done>Both adapters answer the D-02 race check as a neutral bool; Lidarr surfaces faults, Readarr degrades to False; firewall holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: acquire.py INFRA_EXC seam — distinguish infra outage from genuine not-found (A1)</name>
  <read_first>
    - app/core/acquire.py (the full file — esp. _safe_call lines 315-323 + the manifest/profile fetch + the stuck path lines 252-259 + the LAZY httpx import in build_acquire_clients)
    - .planning/phases/phase-5/05-RESEARCH.md Pattern 5 (lines 340-360) + Open Question 1 (lines 662-665) — the recommended boundary
    - .planning/phases/phase-5/05-PATTERNS.md "Infra-vs-genuine classifier" (lines 123-129) — INFRA_EXC tuple definition
    - app/tests/test_acquire.py (the existing acquire suite — keep it green) + app/tests/test_infra_classify.py (the RED target)
  </read_first>
  <files>app/core/acquire.py, app/tests/test_infra_classify.py, app/tests/test_acquire.py</files>
  <behavior>
    - An INFRA-class exception (httpx.ConnectError/ConnectTimeout/ReadTimeout/PoolTimeout/RemoteProtocolError) raised by get_manifest/get_quality_profile is RE-RAISED (or tagged) so the caller can classify it as infra — it does NOT silently become a genuine None→stuck.
    - A genuine None return (adapter found nothing) still maps to None → stuck (unchanged behavior).
    - acquire_item's existing outcomes ("imported"|"quarantined"|"stuck") for genuine paths are UNCHANGED — the existing test_acquire suite stays green.
    - INFRA_EXC is defined once and importable for reuse by the scheduler + reconcile (Waves 1/2).
  </behavior>
  <action>
    In acquire.py, add the classifier tuple `INFRA_EXC` containing `httpx.ConnectError, httpx.ConnectTimeout, httpx.ReadTimeout, httpx.PoolTimeout, httpx.RemoteProtocolError`. Because acquire.py currently does NOT import httpx at module top (offline 3.9 sandbox), define INFRA_EXC WITHOUT breaking offline import: use a module-level `try: import httpx; INFRA_EXC = (...) except ImportError: INFRA_EXC = ()` guard, OR a small `def infra_exc() -> tuple:` that imports httpx lazily and returns the tuple. Pick whichever keeps test_acquire green in the 3.9 sandbox and lets reconcile/scheduler import the SAME definition; document the choice in a comment. (Downstream 05-03/05-04 import this single definition — do not let them redefine it.)
    Adjust `_safe_call` (the MINIMAL contained change per A1): catch the INFRA family separately and RE-RAISE it (so the call site can treat an *arr decision-input outage as infra), while still mapping a genuine return-None / non-infra "not found" to None. Keep the function's signature; update its docstring to state the infra-vs-genuine boundary (REL-02). Do NOT change acquire_item's genuine flow — only make an infra fault on the decision-input fetch propagate instead of silently becoming `stuck`.
    Add test_infra_classify.py cases: get_manifest raising httpx.ConnectError → _safe_call re-raises (infra, classifiable); get_manifest returning None → _safe_call returns None (genuine); the INFRA_EXC accessor/tuple contains the five exception types. Keep test_acquire.py fully green (run it).
    FIREWALL: acquire.py is core — it stays *arr/slskd-vocabulary-free (httpx exception types are neutral library types, not wire keys); confirm the firewall grep + the matching!=selection grep both stay clean.
  </action>
  <verify>
    <automated>cd app && python3 -m pytest tests/test_infra_classify.py tests/test_acquire.py tests/test_adapter_protocol.py -q</automated>
  </verify>
  <acceptance_criteria>
    - acquire.py exposes the INFRA_EXC classification (tuple or accessor) containing the five httpx connection/timeout types, importable by other core modules.
    - `_safe_call` re-raises an INFRA-class fault but still returns None for a genuine not-found (proven by test_infra_classify.py).
    - `cd app && python3 -m pytest tests/test_acquire.py -q` stays fully green (no regression to the genuine flow).
    - `cd app && python3 -m pytest tests/test_adapter_protocol.py -q` passes (firewall + matching!=selection grep clean over acquire.py).
  </acceptance_criteria>
  <done>An infra outage on the decision-input fetch is distinguishable from a genuine not-found; the genuine acquire flow is unchanged; INFRA_EXC is reusable; firewall holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slskd peer → slskd → Curator | The /api/v0/application + queue JSON crosses an untrusted boundary; a malformed field must not crash |
| Curator → *arr / slskd | API keys live in self._headers; a failing call must not echo the key into a log/exception |
| infra fault vs genuine fault | Misclassifying an outage as a genuine failure would wrongly burn a retry attempt (REL-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Info disclosure | API key in shares/queue call failures | mitigate | Key stays only in self._headers; never echoed into a log line or exception message (extend T-04-07 to the new methods) |
| T-05-06 | DoS | malformed slskd/*arr response crashing a call | mitigate | `.get()`-defensive reads + isinstance guards; absent shares.files → 0; non-dict body → []/{} |
| T-05-07 | Spoofing/Tampering | wrong-attempt-burn on infra outage | mitigate | INFRA_EXC classifier (A1): infra faults re-raise/classify distinctly so they never push an available album toward permanently-unavailable (REL-02) |
| T-05-08 | Tampering | firewall regression in acquire.py | mitigate | acquire.py stays neutral (httpx types are library, not wire vocab); the auto-scanning firewall grep + matching!=selection grep both run in verify |
| T-05-SC | Tampering | npm/pip installs | mitigate | Zero new packages (stdlib + already-pinned httpx); no Package Legitimacy Gate triggered |
</threat_model>

<verification>
- `cd app && python3 -m pytest -q` — full offline suite green.
- `cd app && python3 -m pytest tests/test_adapter_protocol.py -q` — firewall clean: `shares.files` + `records[`/`albumId` only in adapters/, acquire.py neutral.
- The A2/A3 live shapes are `[ASSUMED]` fixtures here; they are live-confirmed in plan 05-05 (offline code uses these research-confirmed shapes so nothing blocks).
</verification>

<success_criteria>
- slskd client reads the shared-file count + triggers a rescan (D-10, SHARE-01/02), defensive + raise-primary.
- Both *arr adapters answer get_queue_status as a neutral bool (GAP-03/D-02); Lidarr surfaces faults, Readarr degrades to False.
- An infra outage on the decision-input fetch is classifiable distinctly from a genuine not-found (A1, REL-02 precondition); the genuine acquire flow is unchanged.
- The firewall holds over core; shares/queue wire keys stay in adapters/; conftest.py is untouched (no Wave-0 file overlap with 05-01).
</success_criteria>

<output>
Create `.planning/phases/phase-5/05-02-SUMMARY.md` when done.
</output>
