---
phase: 02-state-ledger-arr-adapter-gap-detection
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - app/core/gap_detector.py
  - app/tests/test_gap_detector.py
autonomous: true
requirements: [GAP-01, GAP-02, STATE-02, ARR-02]
must_haves:
  truths:
    - "detect_gaps iterates the adapters, upserts every GapItem into the ledger, and returns per-app counts"
    - "Running detect_gaps twice over the same fixtures adds zero duplicate ledger rows (end-to-end dedup, STATE-02)"
    - "When the Readarr adapter faults (returns []/raises behind the breaker), the Lidarr items are still fully upserted — books never gate music (ARR-02 end-to-end)"
    - "gap_detector imports only GapItem + the adapter Protocol + the repo — zero *arr field names (firewall intact)"
    - "A manual one-shot trigger can run detection on demand (NOT a scheduled loop — scheduling is Phase 5)"
  artifacts:
    - path: "app/core/gap_detector.py"
      provides: "detect_gaps(adapters, conn) → upserts missing+cutoff GapItems, returns counts; manual __main__ trigger"
      contains: "def detect_gaps"
    - path: "app/tests/test_gap_detector.py"
      provides: "end-to-end upsert counts, dedup-on-rerun, Readarr-fault-does-not-gate-music proofs"
      min_lines: 40
  key_links:
    - from: "app/core/gap_detector.py"
      to: "repo.upsert_gap"
      via: "loop over adapter.get_wanted() items"
      pattern: "upsert_gap"
    - from: "app/core/gap_detector.py"
      to: "adapter.get_wanted (Protocol only)"
      via: "iterate [lidarr, readarr_breaker] independently"
      pattern: "get_wanted"
---

<objective>
Wire the spine to the seam: a `core/gap_detector.py` that iterates the adapters `[lidarr, readarr_breaker]` independently, calls `adapter.get_wanted()`, upserts each `GapItem` into the ledger, and returns per-app counts. This closes Phase 2 by proving the four success criteria end-to-end against fixtures: missing+cutoff detected and persisted (GAP-01/02), re-running adds zero duplicates (STATE-02 dedup at the loop level), and a Readarr fault yields zero book items while the Lidarr items are fully upserted (ARR-02 — books never gate music). A tiny manual one-shot trigger lets the owner run detection on demand for UAT — it is NOT a scheduled loop (the daemon/scheduler is Phase 5).

Purpose: gap_detector is the only caller of the adapters and the only orchestrator of the upsert; it is the firewall's core side — it must contain ZERO *arr field names. This is the integration point where the dedup + graceful-degradation guarantees are proven together rather than in isolation.
Output: `app/core/gap_detector.py` + `app/tests/test_gap_detector.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-2/RESEARCH.md
@.planning/phases/phase-2/PATTERNS.md
@.planning/phases/phase-2/VALIDATION.md
@.planning/phases/phase-2/02-02-PLAN.md
@.planning/phases/phase-2/02-03-PLAN.md

<interfaces>
<!-- Contracts from Waves 1-2. Use directly — gap_detector touches ONLY these (never *arr fields). -->

From app/adapters/base.py (02-03):
- `GapItem` frozen dataclass; `ArrAdapter` Protocol with `app: str` and `get_wanted() -> list[GapItem]`.
- LidarrAdapter (app="lidarr") and CircuitBreaker-wrapped ReadarrAdapter both satisfy ArrAdapter.

From app/state/repo.py (02-02):
- `upsert_gap(conn, item) -> None` — status-preserving dedup upsert on (arr_app, arr_id).
- `get_gap(conn, arr_app, arr_id)`, `set_status(conn, arr_app, arr_id, status)`, `list_by_status(conn, status)`.

From app/state/db.py (02-02):
- `connect(db_path)`, `run_migrations(conn)`.

From app/config.py (02-01):
- `settings.lidarr_url/lidarr_api_key/readarr_url/readarr_api_key/db_path`.

From app/tests/conftest.py (02-01):
- `tmp_db_path`, `load_fixture`, offline httpx mock fixture.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement detect_gaps and the manual one-shot trigger</name>
  <read_first>
    - RESEARCH.md Pattern 4 core loop (lines 434-443) — detect_gaps iterates adapters independently, upserts each item, returns counts
    - RESEARCH.md Pitfall 6 (lines 695-700) — gap_detector imports GapItem + Protocol + repo ONLY; ZERO *arr field names (foreignAlbumId/records[/X-Api-Key)
    - RESEARCH.md Open Question 4 (lines 842-843) — a one-shot `python -m` trigger is an acceptable test/UAT affordance; NOT a scheduled loop (Phase 5)
    - PATTERNS.md "app/core/gap_detector.py" (lines 273-280)
    - app/main.py (house style)
    - app/state/repo.py + app/adapters/base.py (the contracts above)
  </read_first>
  <action>
    Create `app/core/gap_detector.py` (purpose comment, typed signatures). `detect_gaps(adapters, conn) -> dict[str, int]`: for each adapter in the list, call `items = adapter.get_wanted()` (the breaker-wrapped Readarr returns [] on any fault — music unaffected), `for it in items: repo.upsert_gap(conn, it)`, record `counts[adapter.app] = len(items)`; return counts. Import ONLY `from adapters.base import GapItem, ArrAdapter` (type hints) and `from state import repo` — this module must contain ZERO *arr field names (no `foreignAlbumId`, `records[`, `X-Api-Key`, `profileId`); the firewall grep enforces it.
    Add a small builder `build_adapters() -> list` that constructs the LidarrAdapter (with an `httpx.Client`, `settings.lidarr_url`, `settings.lidarr_api_key`) and the CircuitBreaker-wrapped ReadarrAdapter (`settings.readarr_url`, `settings.readarr_api_key`) — Lidarr first (primary), Readarr second (best-effort). Add an `if __name__ == "__main__":` one-shot that `connect`s + `run_migrations` on `settings.db_path`, builds the adapters, runs `detect_gaps`, and prints the per-app counts — a manual UAT trigger ONLY. Do NOT add any scheduler, sleep loop, apscheduler, grace window, or slskd call (all Phase 5/4 — PITFALL #5).
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('app/core/gap_detector.py').read())" && grep -q 'def detect_gaps' app/core/gap_detector.py && grep -q 'upsert_gap' app/core/gap_detector.py && grep -q 'get_wanted' app/core/gap_detector.py && grep -q '__main__' app/core/gap_detector.py && ! grep -qiE 'foreignAlbumId|X-Api-Key|records\[|apscheduler|while True|time.sleep|slskd' app/core/gap_detector.py</automated>
  </verify>
  <acceptance_criteria>
    `app/core/gap_detector.py` defines `detect_gaps(adapters, conn)` iterating adapters independently and upserting via `repo.upsert_gap`, returning per-app counts; it imports only adapter-Protocol + repo and contains ZERO *arr field names and ZERO scheduling/slskd code. A `__main__` one-shot manual trigger exists. Behavioral proof lands in Task 2.
  </acceptance_criteria>
  <done>detect_gaps wires adapters→ledger with per-adapter independence; firewall intact (no *arr fields); manual trigger present; no scheduling/slskd scope creep.</done>
</task>

<task type="auto">
  <name>Task 2: Write the end-to-end gap-detector test suite (counts, dedup, Readarr-no-gate)</name>
  <read_first>
    - app/tests/test_health.py (test conventions)
    - app/tests/conftest.py (tmp_db_path, load_fixture, offline httpx mock)
    - app/core/gap_detector.py (Task 1)
    - app/state/db.py + app/state/repo.py (connect/run_migrations/upsert_gap/list_by_status)
    - RESEARCH.md / VALIDATION.md "Phase Requirements → Test Map" (RESEARCH lines 759-768; VALIDATION lines 44-52) — test_readarr_fault_does_not_gate_music + end-to-end upsert counts
  </read_first>
  <action>
    Create `app/tests/test_gap_detector.py` (module docstring, flat imports `from core.gap_detector import detect_gaps`, `from state.db import connect, run_migrations`, `from state.repo import list_by_status, get_gap, set_status`). Build fake adapters in-test (small classes exposing `app` + `get_wanted()` returning lists of GapItem) so the suite needs no live network — OR drive the real adapters via the conftest offline httpx mock; either is acceptable, keep it offline.
    - `test_end_to_end_counts`: a fake Lidarr returning K missing+cutoff GapItems + a fake Readarr returning M → detect_gaps over a migrated tmp DB returns `{"lidarr": K, "readarr": M}` and the ledger holds K+M rows.
    - `test_dedup_on_rerun`: run detect_gaps TWICE over the same fake adapters → ledger row count is identical after the second run (no duplicates) — end-to-end STATE-02.
    - `test_dedup_preserves_status_end_to_end`: detect once → `set_status` one row to 'imported' → detect again → that row's status is still 'imported' (the upsert did not clobber it through the detector path).
    - `test_readarr_fault_does_not_gate_music`: a fake Lidarr returning N GapItems + a Readarr stand-in that RAISES (wrapped so get_wanted returns [] like the breaker) → detect_gaps still upserts all N Lidarr rows, returns `readarr: 0`, and raises NO exception (ARR-02 end-to-end — books never gate music).
    Use plain asserts; map each test to its REQ-ID in the docstring.
  </action>
  <verify>
    <automated>python3 -m pytest app/tests/test_gap_detector.py -q || echo "NOTE: sandbox is Python 3.9 + offline; test authoring verified by parse — real run is CI/NAS per RESEARCH Environment Availability"; python3 -c "import ast; ast.parse(open('app/tests/test_gap_detector.py').read())" && grep -Eq 'def test_(end_to_end_counts|dedup_on_rerun|dedup_preserves_status_end_to_end|readarr_fault_does_not_gate_music)' app/tests/test_gap_detector.py</automated>
  </verify>
  <acceptance_criteria>
    `app/tests/test_gap_detector.py` defines all four named tests. Under Python 3.12 (CI/NAS) `python -m pytest app/tests/test_gap_detector.py -q` exits 0 — proving end-to-end detection→upsert counts (GAP-01/02), dedup-on-rerun + status preservation (STATE-02), and that a Readarr fault upserts all Lidarr rows + returns readarr:0 without raising (ARR-02). (Local sandbox is Python 3.9 + offline; local gate is AST parse + grep; real run is CI/NAS.)
  </acceptance_criteria>
  <done>Four end-to-end tests prove detection counts, dedup-on-rerun, status preservation, and Readarr-fault-does-not-gate-music — closing the Phase 2 success criteria offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter output → ledger | GapItems (incl. from a fragile Readarr) are written to the DB |
| detection orchestration | the only place that drives outbound *arr reads + DB writes together |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Denial of Service | A Readarr fault aborts detect_gaps → music detection also fails | mitigate | Adapters iterated independently; Readarr is breaker-wrapped (returns [] on fault); `test_readarr_fault_does_not_gate_music` proves the Lidarr upserts still complete |
| T-02-12 | Tampering | gap_detector parsing *arr fields directly would leak the firewall | mitigate | gap_detector imports only GapItem + Protocol + repo; the verify grep asserts zero *arr field names in app/core |
| T-02-13 | Elevation / scope creep | Adding a scheduler/slskd call now (Phase 4/5 scope) | mitigate | The `__main__` is a one-shot manual trigger only; verify grep forbids `while True`/`time.sleep`/`apscheduler`/`slskd` in this module |
</threat_model>

<verification>
- `python -m pytest app/tests/test_gap_detector.py -q` exits 0 on Python 3.12 (CI/NAS): all four tests green.
- Full phase suite green in CI: `python -m pytest app/tests -q` (state + adapter + detector tests).
- `grep -rIE 'foreignAlbumId|X-Api-Key|records\[' app/core` returns nothing.
- No scheduling/slskd code in gap_detector.
- Local sandbox gate (Python 3.9 + offline): AST parse + grep proofs pass; real run is CI/NAS.
</verification>

<success_criteria>
- Criterion 2 (GAP-01/02): detect_gaps surfaces both missing and cutoff items via the adapter and persists them.
- Criterion 4 (STATE-02): re-running detection adds zero duplicate rows and preserves an acted-on status — end-to-end.
- Criterion 3 (ARR-02): feeding the Readarr branch a fault yields a skipped/zero book run with the Lidarr loop fully completing — books never gate music, proven in one detect_gaps call.
- Manual one-shot trigger enables on-NAS UAT without building the Phase 5 daemon.
</success_criteria>

<output>
Create `.planning/phases/phase-2/02-04-SUMMARY.md` when done.
</output>
