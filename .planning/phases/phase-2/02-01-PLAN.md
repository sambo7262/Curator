---
phase: 02-state-ledger-arr-adapter-gap-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/requirements.txt
  - app/requirements-dev.txt
  - app/config.py
  - app/state/__init__.py
  - app/adapters/__init__.py
  - app/core/__init__.py
  - app/tests/conftest.py
  - app/tests/fixtures/lidarr_missing.json
  - app/tests/fixtures/lidarr_cutoff.json
  - app/tests/fixtures/lidarr_cutoff_page2.json
  - app/tests/fixtures/readarr_missing.json
  - app/tests/fixtures/readarr_empty.json
  - app/tests/fixtures/readarr_garbage.json
  - .env.example
  - docker-compose.yml
autonomous: false
requirements: [STATE-01]
must_haves:
  truths:
    - "Recorded *arr JSON fixtures exist offline so adapter/ledger tests need no live Lidarr/Readarr"
    - "conftest.py provides a temp-SQLite-path fixture, a JSON fixture loader, and a respx-backed httpx client so later tasks have deterministic offline proof"
    - "config.py exposes LIDARR_URL/KEY, READARR_URL/KEY, DB_PATH as one typed settings singleton reading env only"
    - "The /db bind-mount + DB_PATH are wired into compose and .env.example (DB lives on its own mount, NOT under /data)"
  artifacts:
    - path: "app/config.py"
      provides: "Typed Settings singleton for *arr URLs/keys + DB_PATH from env"
      contains: "db_path"
    - path: "app/tests/conftest.py"
      provides: "Shared pytest fixtures: tmp db path, fixture loader, respx/httpx client"
      min_lines: 20
    - path: "app/tests/fixtures/lidarr_missing.json"
      provides: "Recorded Lidarr wanted/missing paged envelope"
      contains: "records"
    - path: "app/tests/fixtures/readarr_garbage.json"
      provides: "Malformed Readarr records to prove graceful degradation"
      contains: "records"
  key_links:
    - from: "docker-compose.yml"
      to: "/volume1/docker/curator/db:/db"
      via: "curator service volumes"
      pattern: "curator/db:/db"
    - from: "app/config.py"
      to: "DB_PATH"
      via: "os.getenv default /db/curator.sqlite"
      pattern: "DB_PATH"
---

<objective>
Wave 0 scaffolding for Phase 2: build the offline test substrate (recorded *arr JSON fixtures + shared conftest fixtures + dev deps) and the config/package skeleton BEFORE any behavioral code, so every later task in this phase has deterministic, offline, fixture-based pytest proof. The dev sandbox is Python 3.9 + offline (per RESEARCH "Environment Availability") — fixtures are mandatory, not optional. Also wire the new `DB_PATH` env var and the dedicated `/db` bind-mount (the SQLite file lives on its OWN mount, never under the shared `/data` tree).

Purpose: Plans 02 (state) and 03 (adapters) cannot be tested without these fixtures + conftest; this plan removes that dependency up front and locks the config seam + import-root package markers.
Output: `app/config.py`, three `__init__.py` package markers, `app/tests/conftest.py`, six fixture JSON files, dev requirements, and the `/db` mount + `DB_PATH` wired into compose + `.env.example`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-2/RESEARCH.md
@.planning/phases/phase-2/PATTERNS.md
@.planning/phases/phase-2/VALIDATION.md

<interfaces>
<!-- House-style conventions extracted from the existing Phase-1 codebase. Use directly; do not re-explore. -->

From app/main.py (module house style — replicate):
- Top-of-file `#` purpose-comment banner.
- stdlib imports first, blank line, then third-party.
- Module-level singletons after imports (e.g. `app = FastAPI(...)`, `DATA = Path("/data")`).
- Env read inline today via `os.getenv("SLSKD_URL")` (tolerates None).

From app/tests/test_health.py (test conventions — replicate):
- Module docstring stating scope: `"""Phase-1 unit coverage for the Curator FastAPI stub."""`
- FLAT imports: `from main import app` (NOT `from app.main import app`) — pyproject sets `pythonpath=["app"]`.
- Module-level client/fixtures; snake_case `test_<behavior>` functions; plain `assert`; `monkeypatch` for env; `set(body) == {...}` for dict-shape checks.

From pyproject.toml:
- `pythonpath = ["app"]`, `testpaths = ["app/tests"]`, `addopts = "-q"`. `app/` IS the import root.

From app/requirements.txt (exact == pinning, one per line):
- `fastapi==0.115.6`
- `uvicorn[standard]==0.34.0`

From docker-compose.yml curator service (env already surfaced — reuse names verbatim):
- `LIDARR_URL: "http://lidarr:8686"`, `LIDARR_API_KEY: "${LIDARR_API_KEY}"`
- `READARR_URL: "http://readarr:8787"`, `READARR_API_KEY: "${READARR_API_KEY}"`
- existing volumes: `/volume1/docker/curator/config:/config`, `/volume1/data:/data:ro`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config.py, package markers, and dev/runtime dependency pins</name>
  <read_first>
    - app/main.py (house style: purpose comment, stdlib-then-third-party imports, module-level singleton)
    - app/requirements.txt (exact `==` pin convention)
    - PATTERNS.md "app/config.py (NEW)" section (lines 78-123) — the recommended frozen-dataclass Settings shape
    - RESEARCH.md "Standard Stack" (lines 132-167) + "Package Legitimacy Audit" (lines 171-189) — httpx==0.28.1 runtime, respx dev-only
    - docker-compose.yml curator.environment block (env var names to reuse verbatim)
  </read_first>
  <action>
    Create `app/config.py` with a top-of-file purpose comment and a frozen `@dataclass(frozen=True)` `Settings` reading env via `os.getenv` for: `lidarr_url` (default `http://lidarr:8686`), `lidarr_api_key` (str|None), `readarr_url` (default `http://readarr:8787`), `readarr_api_key` (str|None), `db_path` (default `/db/curator.sqlite`, env name `DB_PATH`). Expose a module-level `settings = Settings()` singleton. Reuse the EXACT env var names already in compose (`LIDARR_URL`, `LIDARR_API_KEY`, `READARR_URL`, `READARR_API_KEY`); add `DB_PATH` as the only new name. Do NOT log keys anywhere. Full type hints on all fields.
    Create empty package markers `app/state/__init__.py`, `app/adapters/__init__.py`, `app/core/__init__.py` (each new package needs one per the import-root convention; `app/` stays the import root — never introduce a top-level `curator/` package).
    Append `httpx==0.28.1` to `app/requirements.txt` (exact pin; note in a comment to verify the exact 0.28.x on first CI build per RESEARCH Package Audit). Create `app/requirements-dev.txt` pinning `respx==0.22.0` and `pytest` (dev/test only — respx stays OUT of the runtime requirements). Do NOT add pyarr/slskd-api/apscheduler/apprise (Phase 3-6 scope; PITFALL #5).
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('app/config.py').read())" && grep -q 'DB_PATH' app/config.py && grep -q 'settings = Settings()' app/config.py && grep -q 'httpx==0.28.1' app/requirements.txt && grep -q 'respx==' app/requirements-dev.txt && test -f app/state/__init__.py && test -f app/adapters/__init__.py && test -f app/core/__init__.py && ! grep -qiE 'pyarr|apscheduler|apprise|slskd-api' app/requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    `app/config.py` parses as valid Python, defines a frozen `Settings` dataclass with a `db_path` field defaulting to `/db/curator.sqlite` and a module-level `settings` singleton; all four *arr env names match compose verbatim. `httpx==0.28.1` is in `app/requirements.txt`; `respx` is in `app/requirements-dev.txt` only (not runtime); no Phase 3-6 packages are present. The three package `__init__.py` markers exist.
  </acceptance_criteria>
  <done>config.py + 3 package markers + dependency pins exist; runtime deps add only httpx; respx is dev-only; no out-of-scope packages.</done>
</task>

<task type="auto">
  <name>Task 2: Author recorded *arr JSON fixtures and the shared conftest</name>
  <read_first>
    - app/tests/test_health.py (test conventions: module docstring, flat imports, monkeypatch)
    - RESEARCH.md "Lidarr / Readarr API Reference" (lines 617-644) — the verified `{page,pageSize,sortKey,sortDirection,totalRecords,records}` envelope; AlbumResource fields `id`, `foreignAlbumId`, `artistId`, `title`, `monitored`, `profileId` (NOT qualityProfileId), nested `artist.artistName`
    - RESEARCH.md Pattern 4 (lines 387-419) + Open Question 1 (lines 831-834) — Readarr BookResource MEDIUM-confidence keys `id`, `authorId`, `title`, `foreignBookId`, `qualityProfileId`/`profileId`, nested `author.authorName`
    - VALIDATION.md "Wave 0 Requirements" (lines 56-60) + RESEARCH.md "Wave 0 Gaps" (lines 777-784) — the exact fixture filenames + conftest fixtures required
  </read_first>
  <action>
    Create `app/tests/fixtures/` with six recorded/representative JSON files matching the VERIFIED Servarr v1 paged envelope `{page, pageSize, sortKey, sortDirection, totalRecords, records:[...]}`:
    - `lidarr_missing.json`: a single page (`totalRecords` == len(records)) of 2-3 AlbumResource-shaped records, each with `id` (int), `foreignAlbumId` (MBID-like string), `artistId`, `title`, `monitored: true`, `profileId` (int), and a nested `artist: {"artistName": ...}`.
    - `lidarr_cutoff.json`: page 1 of a TWO-page set — set `pageSize` small (e.g. 2) and `totalRecords` > pageSize so the paging loop must fetch page 2; records are AlbumResource-shaped.
    - `lidarr_cutoff_page2.json`: page 2 completing the set (page * pageSize >= totalRecords on this page).
    - `readarr_missing.json`: BookResource-shaped records with `id`, `authorId`, `title`, `monitored`, `foreignBookId`, `qualityProfileId`, nested `author: {"authorName": ...}`.
    - `readarr_empty.json`: a valid envelope with `records: []` and `totalRecords: 0`.
    - `readarr_garbage.json`: an envelope whose `records` mix valid + malformed entries — at least one record missing `id`, one that is not a dict (e.g. a bare string or null), and one with null `title` — to drive the defensive `_map()` skip-and-log path without crashing.
    Create `app/tests/conftest.py` (module docstring, flat-import style) exposing pytest fixtures: (a) `tmp_db_path(tmp_path)` returning a temp SQLite file path under `tmp_path`; (b) `load_fixture` — a loader that reads a named JSON file from `app/tests/fixtures/` and returns the parsed dict; (c) a respx-based or `httpx.MockTransport`-based `httpx_client` factory that serves a given fixture envelope for `/api/v1/wanted/missing` and `/api/v1/wanted/cutoff` so adapter tests run fully offline. Keep fixtures small and deterministic.
  </action>
  <verify>
    <automated>for f in lidarr_missing lidarr_cutoff lidarr_cutoff_page2 readarr_missing readarr_empty readarr_garbage; do python3 -c "import json; d=json.load(open('app/tests/fixtures/$f.json')); assert 'records' in d, '$f missing records'" || exit 1; done && python3 -c "import ast; ast.parse(open('app/tests/conftest.py').read())" && grep -q 'tmp_db_path' app/tests/conftest.py && grep -q 'load_fixture' app/tests/conftest.py && python3 -c "import json,sys; d=json.load(open('app/tests/fixtures/lidarr_cutoff.json')); assert d['totalRecords']>len(d['records']), 'cutoff fixture must be multi-page'"</automated>
  </verify>
  <acceptance_criteria>
    All six fixtures parse as JSON and contain a `records` key matching the verified envelope; `lidarr_cutoff.json` is genuinely multi-page (`totalRecords > len(records)`); `readarr_garbage.json` contains at least one record missing `id` and one non-dict record. `conftest.py` parses and defines `tmp_db_path`, `load_fixture`, and an offline httpx mock fixture. No live network is referenced anywhere.
  </acceptance_criteria>
  <done>Six offline fixtures + a conftest with temp-DB, fixture-loader, and offline httpx-mock fixtures exist; cutoff fixture forces pagination; garbage fixture forces the degradation path.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the /db bind-mount and DB_PATH into compose and .env.example</name>
  <read_first>
    - docker-compose.yml curator service block (lines 64-84: existing environment + volumes)
    - .env.example (lines 29-39: existing *arr keys + PUID/PGID)
    - RESEARCH.md "WAL gotchas" (lines 567-577) + "Runtime State Inventory" (lines 704-718) — DB on its OWN mount `/volume1/docker/curator/db:/db`, NOT under `/data`; dir must be writable by PUID/PGID 1031:65536
    - PATTERNS.md "Env / secrets surfacing" (lines 343-350) — add DB_PATH + the /db bind-mount
  </read_first>
  <action>
    In `docker-compose.yml` curator service: add `DB_PATH: "/db/curator.sqlite"` to the `environment:` block, and add a new volume line `- /volume1/docker/curator/db:/db` to the curator `volumes:` (its OWN mount — do NOT place the DB under the shared `/data` tree, and do NOT make it read-only since the ledger is written). Leave the existing `/data:ro` mount and all other services untouched.
    In `.env.example`: add a `DB_PATH=/db/curator.sqlite` entry under a short comment noting the dir must exist on the NAS owned by PUID/PGID 1031:65536 before first run, and that it lives on the local `/volume1` volume (WAL is unsafe on network shares). Do NOT add any secret value — DB_PATH is a path, not a secret.
    Bump `app/main.py` version string from `0.1.0-phase1` to `0.2.0-phase2` (single-line edit; leave the existing handlers untouched — the startup hook is added in Plan 02).
  </action>
  <verify>
    <automated>grep -q 'DB_PATH: "/db/curator.sqlite"' docker-compose.yml && grep -q 'curator/db:/db' docker-compose.yml && grep -q 'DB_PATH=/db/curator.sqlite' .env.example && grep -q '0.2.0-phase2' app/main.py && ! grep -q 'data:/db' docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    `docker-compose.yml` curator service has `DB_PATH: "/db/curator.sqlite"` in environment and a `/volume1/docker/curator/db:/db` volume that is NOT under `/data` and NOT read-only; `.env.example` documents `DB_PATH` + the PUID/PGID + local-FS requirement; `app/main.py` version is `0.2.0-phase2`.
  </acceptance_criteria>
  <done>DB_PATH + dedicated /db mount wired into compose and documented in .env.example; DB is on its own writable local mount; main.py version bumped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env/.env → container | *arr API keys + DB path enter the process from runtime env only |
| repo files → image | fixtures/config are committed; secrets must never be |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | config.py reading LIDARR/READARR API keys | mitigate | Keys read from env only; never logged; never written to a fixture or committed file; `.env` already gitignored (Phase 1) |
| T-02-02 | Tampering | SQLite DB file location/perms | mitigate | DB on its OWN `/db` mount on local `/volume1` (NOT under `/data`, NOT a network share), dir owned by PUID/PGID 1031:65536; documented in .env.example |
| T-02-SC | Tampering | pip installs (httpx, respx) | mitigate | slopcheck unavailable offline → packages tagged [ASSUMED]; both are first-party high-trust (encode/httpx, lundberg/respx); exact versions confirmed on first CI build where network exists. See blocking checkpoint below. |
</threat_model>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Phase 2 adds two pip dependencies: `httpx==0.28.1` (runtime) and `respx==0.22.0` (dev/test). slopcheck could not run in the offline dev sandbox (RESEARCH Package Legitimacy Audit), so both are tagged [ASSUMED] and must be confirmed legitimate before they are installed in CI.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/httpx/ — confirm it is `encode/httpx` (the official ubiquitous async HTTP client) and that `0.28.1` (or the chosen 0.28.x) is a real published release.
    2. Open https://pypi.org/project/respx/ — confirm it is `lundberg/respx` (the standard httpx mock library) and that `0.22.0` exists and is compatible with httpx 0.28.x.
    3. Confirm neither package is typosquatted (exact name match) and both are dev-trusted.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the installs, or paste the corrected pinned versions.</resume-signal>
</task>

<verification>
- `python3 -c "import ast; ast.parse(open('app/config.py').read())"` exits 0.
- All six fixtures load as JSON with a `records` key; cutoff fixture is multi-page.
- `conftest.py` parses and defines `tmp_db_path` + `load_fixture` + an offline httpx mock fixture.
- compose has the dedicated `/db` mount + `DB_PATH`; `.env.example` documents both.
- No out-of-scope packages (pyarr/apscheduler/apprise/slskd-api) anywhere.
</verification>

<success_criteria>
- Offline fixtures + conftest exist so Plans 02-04 need no live *arr (criteria 2,3,4 become provable offline).
- config.py is the single declarative env surface incl. `DB_PATH` (foundation for STATE-01 restart-durability).
- DB lives on its own local `/db` mount, never under `/data`.
- Package legitimacy gate cleared before any install.
</success_criteria>

<output>
Create `.planning/phases/phase-2/02-01-SUMMARY.md` when done.
</output>
