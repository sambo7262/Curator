---
phase: 03-matching-quality-gating
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/core/candidate.py
  - app/core/manifest.py
  - app/core/release_parse.py
  - app/tests/test_release_parse.py
  - app/tests/conftest.py
  - app/tests/fixtures/candidates/known_good_flac.json
  - app/tests/fixtures/candidates/known_good_alac.json
  - app/tests/fixtures/candidates/incomplete_tracks.json
  - app/tests/fixtures/candidates/wrong_album.json
  - app/tests/fixtures/candidates/wrong_edition.json
  - app/tests/fixtures/candidates/ambiguous_twin_a.json
  - app/tests/fixtures/candidates/ambiguous_twin_b.json
  - app/tests/fixtures/candidates/borderline_accept.json
  - app/tests/fixtures/candidates/fake_flac.json
  - app/tests/fixtures/candidates/below_cutoff_mp3.json
  - app/tests/fixtures/candidates/non_latin.json
  - app/tests/fixtures/candidates/garbage_metadata.json
  - app/tests/fixtures/candidates/no_audio_files.json
  - app/tests/fixtures/candidates/manifests/standard_12track.json
  - app/tests/fixtures/candidates/manifests/no_titles.json
  - app/tests/fixtures/candidates/manifests/non_latin.json
  - app/tests/fixtures/candidates/profiles/lossless_only.json
  - app/tests/fixtures/candidates/profiles/mp3_320_cutoff.json
  - app/tests/fixtures/candidates/INDEX.md
autonomous: true
requirements: [MATCH-01]
must_haves:
  truths:
    - "The Candidate/CandidateFile/Manifest dataclasses (the Phase 3->4 contract) exist as frozen neutral types in app/core/ with zero *arr field names"
    - "A labeled fixture corpus of slskd-shaped candidate JSON exists, each case tagged with its expected gate decision, paired with the Manifest + Profile it is scored against"
    - "release_parse pure tokenizer extracts artist/album/year/format from a noisy folder name and never crashes on garbage/non-Latin input"
  artifacts:
    - path: "app/core/candidate.py"
      provides: "Candidate / CandidateFile frozen dataclasses + audio_file_count / file_titles / audio_files() helpers"
      contains: "class Candidate"
    - path: "app/core/manifest.py"
      provides: "Manifest frozen dataclass (artist, album, track_count, track_titles|None, kind, year) + Profile/QualityRank home if co-located"
      contains: "class Manifest"
    - path: "app/core/release_parse.py"
      provides: "pure parse() -> tokens (artist, album, year, format, source, edition); NFKD fold; anchored regexes"
      contains: "def parse"
    - path: "app/tests/fixtures/candidates/INDEX.md"
      provides: "label table mapping each fixture to its expected decision + manifest + profile"
      contains: "expected"
  key_links:
    - from: "app/core/candidate.py"
      to: "app/core/release_parse.py"
      via: "construction populates parsed_artist/parsed_album/parsed_year/parsed_format"
      pattern: "release_parse|parse\\("
---

<objective>
Lay the Phase 3 foundation, test-first per the owner directive: define the Candidate/CandidateFile/Manifest dataclasses that ARE the Phase 3->4 contract, build the pure release-name tokenizer that populates a candidate's parsed identity, and author the labeled fixture corpus every later wave deserializes into and asserts against.

Purpose: Every downstream plan (matching, quality, fakeflac, gate) scores against these types and reads these fixtures. They must exist and be correct before any scorer is written. This is the "Wave 0 corpus + dataclasses FIRST" requirement.
Output: app/core/candidate.py, app/core/manifest.py, app/core/release_parse.py, test_release_parse.py, an extended conftest loader, and the full labeled fixture corpus under app/tests/fixtures/candidates/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-3/RESEARCH.md
@.planning/phases/phase-3/PATTERNS.md
@.planning/phases/phase-3/VALIDATION.md

<interfaces>
<!-- Extracted contracts the executor needs — do NOT re-explore the codebase for these. -->

The firewall vocabulary (app/adapters/base.py, GapItem, the SP-1 idiom to copy):
- @dataclass(frozen=True) with neutral field names, Optional[...]/`| None`, a docstring naming the contract, a `raw: Dict[str,Any] = field(default_factory=dict)` provenance escape hatch.

Candidate / CandidateFile shape to implement verbatim — RESEARCH.md lines 368-393:
- CandidateFile: filename:str, size_bytes:int, extension:str (lower, no dot), bitrate_kbps:int|None, length_seconds:int|None, sample_rate:int|None, bit_depth:int|None, is_vbr:bool|None
- Candidate: username:str (selector-only), folder:str, files:tuple[CandidateFile,...], free_upload_slots:int|None (selector-only), upload_speed:int|None (selector-only), parsed_artist/parsed_album:str|None, parsed_year:int|None, parsed_format:str|None
- helpers: audio_file_count, file_titles, audio_files()

Manifest shape — RESEARCH.md line 394:
- Manifest(artist, album, track_count, track_titles: tuple[str,...] | None, kind, year=None). For books the SAME type carries (author->artist, title->album) per RESEARCH §9 lines 399-401.

release_parse token set — RESEARCH.md lines 339-345 (anchored/bounded regexes, ReDoS-safe):
- year `\b(19|20)\d{2}\b`; format `FLAC|ALAC|WAV|APE|MP3|AAC|OGG|320|256|192|V0|V2|V8|24bit|16bit|24-?44|Hi-?Res`; source `\b(WEB|CD|Vinyl|LP|SACD|Cassette|Tape)\b`; edition `Deluxe|Remaster(ed)?|Anniversary|Expanded|Bonus`; then strip matched tokens + bracket/paren groups to leave clean artist/album text.
- NFKD fold (RESEARCH lines 217-221): `unicodedata.normalize("NFKD", s)` + strip combining marks for non-Latin.

conftest load_fixture (SP-5, conftest.py lines 27-39) already does `FIXTURES_DIR / f"{name}.json"`, so subpaths like `load_fixture("candidates/known_good_flac")` work unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the Candidate/CandidateFile/Manifest contract dataclasses</name>
  <read_first>
    - app/adapters/base.py (the GapItem frozen-dataclass idiom to copy — SP-1, lines 17-35; firewall doc-comment style lines 1-8)
    - app/core/gap_detector.py (the SP-2 core-module header + import discipline — lines 1-20)
    - .planning/phases/phase-3/RESEARCH.md lines 368-395 (verbatim Candidate/CandidateFile/Manifest field shapes + helpers)
    - .planning/phases/phase-3/PATTERNS.md SP-1, SP-2, and the candidate.py / manifest.py pattern-assignment sections (lines 131-141)
  </read_first>
  <action>
    Create app/core/candidate.py with two frozen dataclasses per RESEARCH lines 368-393: CandidateFile (filename, size_bytes, extension, bitrate_kbps, length_seconds, sample_rate, bit_depth, is_vbr) and Candidate (username, folder, files, free_upload_slots, upload_speed, parsed_artist, parsed_album, parsed_year, parsed_format). Add pure derived helpers: audio_file_count (count of files whose extension is an audio ext), file_titles (filename stems of audio files, for track-title coverage), audio_files() (filter to audio extensions: flac/mp3/m4a/alac/ape/wav/ogg/aac). Document username/upload_speed/free_upload_slots as "selector-only, NEVER read by matching" (Pitfall 5). Carry the SP-2 core-firewall header comment and import ONLY stdlib + (next task) release_parse. Helpers are pure (no I/O) so the frozen dataclass holds.
    Create app/core/manifest.py with the Manifest frozen dataclass per RESEARCH line 394: artist, album, track_count, track_titles (tuple|None), kind (Literal album/book), year (default None). track_titles=None is the graceful-omission path. Add the SP-2 header. Note in the docstring that books reuse this type (author->artist, title->album) per RESEARCH §9. Do NOT add any *arr field names. (Profile/QualityRank are owned by plan 03-04's quality.py — do not define them here to keep file ownership clean.)
  </action>
  <verify>
    <automated>cd app && python -c "import ast,sys; ast.parse(open('core/candidate.py').read()); ast.parse(open('core/manifest.py').read()); print('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - app/core/candidate.py defines `class Candidate` and `class CandidateFile`, both `@dataclass(frozen=True)`, with the exact fields from RESEARCH 368-393 and helpers audio_file_count / file_titles / audio_files().
    - app/core/manifest.py defines `class Manifest` `@dataclass(frozen=True)` with artist, album, track_count, track_titles, kind, year.
    - Source assertion: grep finds 0 occurrences of `foreignAlbumId`, `profileId`, `qualityProfileId`, `records[`, `items[` in either new file (firewall preserved).
    - Both modules carry the SP-2 core-firewall header comment; neither imports from adapters concrete modules (only adapters.base neutral types if needed, or nothing).
  </acceptance_criteria>
  <done>Candidate/CandidateFile/Manifest exist as frozen neutral dataclasses with the contract fields + helpers, no *arr vocabulary, parse-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure release-name tokenizer (release_parse.py) + its tests</name>
  <read_first>
    - app/core/gap_detector.py (SP-2 core-module header + import discipline)
    - .planning/phases/phase-3/RESEARCH.md lines 339-345 (the regex token set) and lines 217-221 (NFKD fold)
    - .planning/phases/phase-3/PATTERNS.md release_parse.py assignment (lines 166-170) — SP-2/SP-3, ReDoS-safety note (RESEARCH 591)
    - app/tests/test_state_repo.py (SP-6 pure-unit + local-builder + per-REQ docstring header + sandbox note idiom)
  </read_first>
  <action>
    Create app/core/release_parse.py exposing a pure `parse(folder_name: str) -> ParsedRelease` (or a small frozen dataclass / dict with artist, album, year, format, source, edition). Implement the anchored, bounded regexes from RESEARCH 339-345 for year/format/source/edition; strip matched tokens AND bracket/paren groups (`[FLAC]`, `(2007)`, `{WEB}`) to leave clean artist/album text; split remaining text on a `-` separator heuristic into artist/album. NFKD-normalize + strip combining marks (RESEARCH 217-221) before/within parsing for non-Latin. SP-3 graceful: garbage or empty input returns None fields, never raises. Keep every regex anchored/bounded (no nested quantifiers) to avoid catastrophic backtracking on hostile folder names.
    Create app/tests/test_release_parse.py (SP-6): per-REQ docstring header citing MATCH-01 + the Phase-2 sandbox note ("Python 3.9 + offline locally; authoritative green at CI/NAS Python 3.12"). Test: a clean `Artist - Album (2007) [FLAC]` parses to artist/album/year=2007/format=flac; bracket/paren noise stripped; a non-Latin folder folds and parses without crash; a pathological/garbage folder returns None fields and does NOT raise; a deliberately long/adversarial folder name returns within a trivially bounded time (ReDoS guard).
  </action>
  <verify>
    <automated>cd app && python -m pytest tests/test_release_parse.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && python -m pytest tests/test_release_parse.py -x` passes (at CI/NAS; locally may be collection-only if rapidfuzz-free — release_parse imports NO rapidfuzz, so it runs locally too).
    - parse() of `"Pink Floyd - The Wall (1979) [FLAC]"` yields artist≈"Pink Floyd", album≈"The Wall", year=1979, format="flac".
    - parse() of a garbage string (e.g. `"!!!___"`) returns None-valued artist/album and raises nothing.
    - Source assertion: every regex in release_parse.py is anchored/bounded; a test feeds a 500+ char adversarial folder and asserts it returns (no hang).
    - grep finds 0 *arr field-name tokens in release_parse.py.
  </acceptance_criteria>
  <done>release_parse.parse is pure, ReDoS-safe, non-Latin-robust, returns None on garbage, and its tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire release_parse into Candidate construction + author the labeled fixture corpus</name>
  <read_first>
    - app/tests/conftest.py (load_fixture, SP-5, lines 27-39 — already supports subpath names)
    - app/tests/fixtures/lidarr_missing.json (the hand-authored offline fixture shape to mirror)
    - .planning/phases/phase-3/VALIDATION.md (the corpus case list + expected decisions, lines 64-92) and Nyquist boundary cases (lines 60-71)
    - .planning/phases/phase-3/RESEARCH.md lines 350 (slskd per-file attrs: filename/size/bitRate/length/sampleRate/bitDepth/isVariableBitRate) and 367-395 (shapes)
    - .planning/phases/phase-3/PATTERNS.md fixture-corpus assignment (lines 228-231) — SP-5, security note V5
  </read_first>
  <action>
    Add a constructor helper to candidate.py (e.g. `Candidate.from_slskd(result_dict)` classmethod, or a module-level `build_candidate(result_dict)` factory) that maps a slskd-shaped result dict into Candidate/CandidateFile and populates parsed_artist/parsed_album/parsed_year/parsed_format by calling release_parse.parse(folder). Per-file: derive `extension` from filename (lower, no dot); read size_bytes from `size`; read optional bitrate_kbps/length_seconds/sample_rate/bit_depth/is_vbr from the slskd attrs named in RESEARCH 350, all `.get()`-defensive (absent -> None, never KeyError — SP-3). This factory is the Phase-3-owned contract Phase 4 will feed real slskd JSON into.
    Author the labeled corpus under app/tests/fixtures/candidates/ as small hand-written JSON mirroring lidarr_missing.json's offline style. Each candidate file is a slskd-shaped result; create the matching manifests/ and profiles/ targets. Cover the Nyquist classes from VALIDATION 64-71: known_good_flac (ACCEPT), known_good_alac, borderline_accept (just <= strong, ACCEPT), incomplete_tracks (DECLINE, track-count gap), wrong_album (DECLINE), wrong_edition (DECLINE), ambiguous_twin_a + ambiguous_twin_b (rec-gap DECLINE), fake_flac (DECLINE), below_cutoff_mp3 (DECLINE, quality), non_latin (ACCEPT when correct), garbage_metadata (DECLINE, never throw), no_audio_files (DECLINE). manifests/: standard_12track, no_titles (track_titles omitted — the graceful path), non_latin. profiles/: lossless_only (FLAC/ALAC allowed, cutoff at lossless), mp3_320_cutoff (allows mp3-320+ and lossless, cutoff at mp3-320). Write fixtures/candidates/INDEX.md tabulating each fixture -> {manifest, profile, expected_decision, why} so later waves and verify-work assert the labeled outcome, not an invented one.
    If conftest's load_fixture needs a subpath tweak, confirm it already accepts `load_fixture("candidates/known_good_flac")` (SP-5 says it does); only edit conftest.py if a nested-loader convenience is genuinely needed — keep the existing fixture untouched.
  </action>
  <verify>
    <automated>cd app && python -c "import json,glob; [json.load(open(f)) for f in glob.glob('tests/fixtures/candidates/**/*.json',recursive=True)]; print('corpus-json-ok')" && python -m pytest tests/test_release_parse.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All JSON files under app/tests/fixtures/candidates/ (incl. manifests/ + profiles/) parse as valid JSON.
    - The corpus contains at minimum the 13 candidate cases, 3 manifests, and 2 profiles listed in <files_modified>, each appearing in INDEX.md with an explicit expected_decision.
    - INDEX.md tags every candidate with one of ACCEPT / DECLINE and names its paired manifest + profile (so calibration is provable against labels, never asserted — Pitfall 3).
    - build_candidate/from_slskd populates parsed_* via release_parse and is `.get()`-defensive on every optional slskd attr (a fixture missing bitRate/length yields None, not KeyError).
    - no_audio_files candidate has audio_file_count == 0 (drives the immediate-decline path tested downstream).
  </acceptance_criteria>
  <done>The Candidate factory parses slskd-shaped JSON into the contract types, and the full labeled corpus + INDEX exist as valid offline JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slskd result JSON -> Candidate | Untrusted free-text folder/file names + attacker-influenceable strings cross into the parser. Phase 3 only parses (no exec, no path use); treated as opaque match text. |
| fixtures on disk -> tests | Local trusted test data (hand-authored). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | release_parse regexes on hostile folder names (ReDoS) | mitigate | All regexes anchored/bounded, no nested quantifiers; a test feeds a 500+ char adversarial folder and asserts bounded return (RESEARCH 591) |
| T-03-02 | Tampering | candidate folder/filename strings used as anything but match text | accept | Phase 3 never uses candidate strings as filesystem paths or shell args (that risk is Phase 4 staging); here they are opaque text only (V5) |
| T-03-03 | Information disclosure | *arr field names leaking into core via the new dataclasses | mitigate | candidate.py/manifest.py carry zero *arr vocabulary; enforced by the firewall grep extended in plan 03-05 |
</threat_model>

<verification>
- `cd app && python -m pytest tests/test_release_parse.py -x` passes.
- All corpus JSON parses; INDEX.md labels every fixture.
- `cd app && grep -rEn 'foreignAlbumId|qualityProfileId|profileId|records\[|items\[' core/candidate.py core/manifest.py core/release_parse.py | grep -v '^[^:]*:[0-9]*: *#'` returns nothing (firewall clean, comment-stripped).
</verification>

<success_criteria>
- The Phase 3->4 contract (Candidate/CandidateFile/Manifest) exists as frozen neutral dataclasses with the RESEARCH-specified fields + helpers.
- release_parse is a pure, ReDoS-safe, non-Latin-robust tokenizer that never crashes; its tests pass.
- A labeled fixture corpus straddling the decision boundary (good / boundary / error classes per VALIDATION) exists with an INDEX mapping each case to its expected decision + manifest + profile.
</success_criteria>

<output>
Create `.planning/phases/phase-3/03-01-SUMMARY.md` when done.
</output>
