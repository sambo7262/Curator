---
phase: 03-matching-quality-gating
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - app/requirements.txt
  - app/config.py
autonomous: false
requirements: [MATCH-02, QUAL-03]
user_setup: []
must_haves:
  truths:
    - "rapidfuzz is human-verified legitimate against PyPI before it lands in requirements.txt (Phase-2 precedent)"
    - "All Phase-3 thresholds/weights/fake-FLAC floor are config-tunable via Settings.from_env() with defaults equal to MatchConfig defaults"
  artifacts:
    - path: "app/requirements.txt"
      provides: "pinned rapidfuzz runtime dependency"
      contains: "rapidfuzz"
    - path: "app/config.py"
      provides: "extended frozen Settings with MATCH_STRONG_THRESH/MATCH_REC_GAP_THRESH/weights/FAKEFLAC_MIN_KBPS env reads"
      contains: "from_env"
  key_links:
    - from: "app/config.py"
      to: "os.environ"
      via: "from_env() snapshots each new tunable"
      pattern: "MATCH_STRONG_THRESH|FAKEFLAC_MIN_KBPS"
---

<objective>
Unblock the matcher/gate waves: human-verify rapidfuzz's legitimacy and pin it into requirements.txt (the Phase-2 package-legitimacy checkpoint precedent), and extend the existing hand-rolled frozen Settings with the config-tunable thresholds, weights, and fake-FLAC floor the owner must be able to tune up over time without a rebuild.

Purpose: rapidfuzz is the one new runtime dependency this phase needs; per the offline-sandbox legitimacy protocol it must pass a blocking human-verify before install. The thresholds/weights are explicitly owner-tunable (no hard-coded magic) — they live in Settings.from_env() to reuse the one config pattern, not introduce pydantic-settings.
Output: a verified+pinned rapidfuzz line in requirements.txt and the extended Settings dataclass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/phase-3/RESEARCH.md
@.planning/phases/phase-3/PATTERNS.md

<interfaces>
<!-- The existing Settings to extend in place (SP-4) — app/config.py lines 10-48 -->
- @dataclass(frozen=True) class Settings with static field defaults + a from_env() classmethod that reads each via os.getenv(NAME, default). Tests rebuild via Settings.from_env() after monkeypatch.setenv (WR-01 fix; precedent test_gap_detector.py 137-147).
- Module singleton settings = Settings.from_env() at the bottom.

Tunable names + starting values to add — RESEARCH.md lines 323-327, 356; mirror MatchConfig defaults (RESEARCH 207-215):
- MATCH_STRONG_THRESH = 0.15 (float)
- MATCH_REC_GAP_THRESH = 0.10 (float)
- MATCH_W_ARTIST = 3.0, MATCH_W_ALBUM = 3.0, MATCH_W_TRACK_COUNT = 4.0, MATCH_W_TRACK_TITLES = 4.0 (floats)
- FAKEFLAC_MIN_KBPS = 400 (int)

Package legitimacy: RESEARCH "Package Legitimacy Audit" lines 98-110 — rapidfuzz is [ASSUMED]; planner MUST gate behind checkpoint:human-verify (Phase-2 httpx/respx precedent). rapidfuzz 3.13.x line, MIT, cp312 manylinux x86_64 wheels.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Nothing installed yet. This is the package-legitimacy gate for rapidfuzz — the single new runtime dependency Phase 3 needs — before it is written into app/requirements.txt. slopcheck could not run in the offline dev sandbox (RESEARCH 98-110), so per protocol rapidfuzz is [ASSUMED] and requires a blocking human verification, exactly as httpx/respx were verified at the Phase-2 checkpoint.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/rapidfuzz/ — confirm: maintained (recent release in the 3.13.x line, 2025), source repo github.com/rapidfuzz/RapidFuzz, MIT license, very high download volume.
    2. Confirm a cp312 manylinux x86_64 wheel exists on the Files tab (so it installs without a compiler on the Synology linux/amd64 image).
    3. Pin the exact current 3.13.x patch (e.g. run pip index versions rapidfuzz on a networked machine / CI) and note it for the next task.
    4. Reply with the exact pin to use (e.g. rapidfuzz==3.13.0) or report any concern.
  </how-to-verify>
  <resume-signal>Reply "approved: rapidfuzz==3.13.X" with the confirmed pin, or describe the concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Pin rapidfuzz into requirements.txt + extend Settings with the tunables</name>
  <read_first>
    - app/requirements.txt (the Phase-2 human-verify checkpoint comment precedent — mirror its commenting style)
    - app/config.py (SP-4 — the frozen Settings + from_env() to extend in place, lines 10-48)
    - app/tests/test_gap_detector.py lines 137-147 (the monkeypatch.setenv -> Settings.from_env() rebuild idiom, so the new fields are env-overridable in tests)
    - .planning/phases/phase-3/RESEARCH.md lines 323-327, 356 (env names + starting values) and PATTERNS.md SP-4 (lines 92-106) + config.py assignment (lines 224-226)
  </read_first>
  <action>
    Append the human-approved rapidfuzz pin to app/requirements.txt with a comment recording it passed the package-legitimacy human-verify checkpoint (mirroring the Phase-2 httpx/respx comment precedent). Use the exact pin from the checkpoint reply. Do NOT add guessit (fallback-only; only if the corpus later defeats the custom tokenizer).
    Extend the frozen Settings dataclass in app/config.py (SP-4, in place — do NOT introduce pydantic-settings) with the new fields and their static defaults: match_strong_thresh=0.15, match_rec_gap_thresh=0.10, match_w_artist=3.0, match_w_album=3.0, match_w_track_count=4.0, match_w_track_titles=4.0, fakeflac_min_kbps=400. Add a matching os.getenv read for each in from_env() using the env names from RESEARCH 323-327/356 (MATCH_STRONG_THRESH, MATCH_REC_GAP_THRESH, MATCH_W_ARTIST, MATCH_W_ALBUM, MATCH_W_TRACK_COUNT, MATCH_W_TRACK_TITLES, FAKEFLAC_MIN_KBPS), casting to float/int. Defaults MUST equal the MatchConfig defaults (RESEARCH 207-215) so behavior is identical with no env set. Keep the existing fields and the settings = Settings.from_env() singleton intact.
  </action>
  <verify>
    <automated>cd app && python -c "import os; os.environ['MATCH_STRONG_THRESH']='0.2'; os.environ['FAKEFLAC_MIN_KBPS']='450'; from config import Settings; s=Settings.from_env(); assert s.match_strong_thresh==0.2 and s.fakeflac_min_kbps==450 and s.match_w_track_count==4.0, vars(s); print('config-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - app/requirements.txt contains the exact human-approved rapidfuzz pin (e.g. rapidfuzz==3.13.X) with a comment recording the package-legitimacy human-verify approval (Phase-2 precedent style). guessit is NOT added.
    - app/config.py Settings has new frozen fields match_strong_thresh, match_rec_gap_thresh, match_w_artist, match_w_album, match_w_track_count, match_w_track_titles, fakeflac_min_kbps with defaults 0.15/0.10/3.0/3.0/4.0/4.0/400.
    - from_env() reads each new field via os.getenv with the RESEARCH env names, casting float/int; the verify one-liner asserts an env override is honored and an unset field keeps its default.
    - Defaults equal the MatchConfig defaults (RESEARCH 207-215) so no-env behavior is identical; the existing settings singleton + Phase-2 fields are intact (existing suite still imports config cleanly).
  </acceptance_criteria>
  <done>rapidfuzz is human-verified, pinned, and commented; Settings exposes all Phase-3 tunables via from_env() with matching defaults.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> app image | A new third-party package enters the supply chain; offline slopcheck could not run. |
| env -> Settings | Operator-supplied tuning values cross into runtime config (trusted operator, but cast-validated). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | rapidfuzz npm/pip supply chain ([ASSUMED] legitimacy) | mitigate | Blocking human-verify checkpoint against pypi.org/project/rapidfuzz + exact pin before it lands in requirements.txt (Phase-2 precedent); never auto-approved |
| T-03-12 | Tampering | malformed env value for a threshold/weight (e.g. non-numeric) | accept | Operator-trusted; float()/int() cast surfaces a clear ValueError at startup rather than silently mis-gating — acceptable fail-fast |
</threat_model>

<verification>
- The rapidfuzz legitimacy checkpoint is approved with an exact pin before Task 1 runs (blocking-human; not auto-advanced).
- cd app && python -c "...Settings.from_env()..." prints config-ok (env override honored, default preserved).
- The existing Phase-2 suite still imports config without error (no regression to the singleton).
</verification>

<success_criteria>
- rapidfuzz passes the blocking package-legitimacy human-verify and is pinned + commented in requirements.txt.
- All Phase-3 thresholds/weights/fake-FLAC floor are config-tunable via Settings.from_env() with defaults equal to the MatchConfig defaults.
</success_criteria>

<output>
Create `.planning/phases/phase-3/03-02-SUMMARY.md` when done.
</output>
