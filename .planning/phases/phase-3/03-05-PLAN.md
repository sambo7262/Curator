---
phase: 03-matching-quality-gating
plan: 05
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - app/core/selector.py
  - app/core/gate.py
  - app/adapters/base.py
  - app/adapters/lidarr.py
  - app/adapters/readarr.py
  - app/tests/test_gate.py
  - app/tests/test_adapter_protocol.py
  - app/tests/test_lidarr_adapter.py
  - app/tests/test_readarr_adapter.py
autonomous: true
requirements: [QUAL-01, MATCH-01, MATCH-02]
must_haves:
  truths:
    - "gate.py composes matching + quality + fakeflac + selector into a single (candidate_list, manifest, profile) -> GateResult(decision, chosen, distance, reasons) over the labeled corpus (MATCH-01/02 end-to-end)"
    - "the end-to-end corpus run proves QUAL-02 in both directions: below_cutoff_mp3 declines at the quality stage AND known_good_mp3_320 (paired with the mp3_320_cutoff profile) is ACCEPTED end-to-end (the profile-acceptable lossy permit path)"
    - "selector is a separate dumb/swappable best-pick over ALREADY-accepted candidates that is the ONLY reader of uploader speed/slots (matching != selection)"
    - "the adapters implement get_quality_profile + get_manifest returning NORMALIZED Profile/Manifest so no *arr field names cross into core (QUAL-01)"
    - "the firewall grep test is extended to the new core modules and finds 0 forbidden *arr tokens; Readarr profile/manifest faults degrade and never gate music"
  artifacts:
    - path: "app/core/gate.py"
      provides: "GateResult frozen dataclass + evaluate(candidates, manifest, profile, cfg)->GateResult composing all gates"
      contains: "def evaluate"
    - path: "app/core/selector.py"
      provides: "select(accepted)->Candidate|None by (distance, format pref, free-slots/speed) — the only uploader-field reader"
      contains: "def select"
    - path: "app/adapters/lidarr.py"
      provides: "get_quality_profile(id)->Profile + get_manifest(foreign_id)->Manifest, all *arr keys local"
      contains: "def get_quality_profile"
    - path: "app/tests/test_adapter_protocol.py"
      provides: "extended firewall grep over new core modules + Protocol conformance for the new methods"
      contains: "ARR_FIELD_NAMES"
  key_links:
    - from: "app/core/gate.py"
      to: "app/core/matching.py + app/core/quality.py + app/core/fakeflac.py + app/core/selector.py"
      via: "evaluate() filters eligible (quality+fakeflac pass) -> scores -> recommend -> select"
      pattern: "matching\\.|quality\\.|fakeflac\\.|selector\\."
    - from: "app/adapters/lidarr.py"
      to: "app/core/quality.Profile + app/core/manifest.Manifest"
      via: "get_quality_profile/get_manifest return the normalized neutral types"
      pattern: "Profile\\(|Manifest\\("
---

<objective>
Close Phase 3: compose all the gates into one GateResult-producing function, add the dumb/swappable selector (the only uploader-field reader, keeping matching != selection), implement the adapter normalization that surfaces *arr profile + manifest as neutral types (QUAL-01), and extend the locked firewall grep test to the new core modules. End-to-end-prove the full (candidate, manifest, profile) -> (decision, score, reasons) over the labeled corpus.

Purpose: This is the integration point — MATCH-01/02 + QUAL-01/02/03 wired together exactly as detect_gaps composed Phase 2. It also locks the firewall over the new modules and proves Readarr's best-effort book path degrades without gating music.
Output: gate.py, selector.py, the three adapter edits, the extended firewall + Protocol test, adapter-normalization tests, and end-to-end test_gate.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/phase-3/RESEARCH.md
@.planning/phases/phase-3/PATTERNS.md
@.planning/phases/phase-3/VALIDATION.md
@.planning/phases/phase-3/03-01-SUMMARY.md
@.planning/phases/phase-3/03-02-SUMMARY.md
@.planning/phases/phase-3/03-03-SUMMARY.md
@.planning/phases/phase-3/03-04-SUMMARY.md

<interfaces>
<!-- All prior-wave contracts the executor composes — do NOT re-explore. -->
matching (plan 03-03): score(candidate, manifest, cfg)->(float, list[str]); recommend(scored, cfg)->(decision, chosen, distance, reasons); MatchConfig (frozen, defaults match Settings).
quality (plan 03-04): Profile(allowed: frozenset[int], cutoff_rank: int); gate(candidate, profile)->(bool, str); rank_for(ext, bitrate).
fakeflac (plan 03-04): check(candidate, min_kbps)->(bool, str).
candidate/manifest (plan 03-01): Candidate (username/upload_speed/free_upload_slots are selector-only), Manifest.
config (plan 03-02): settings.match_strong_thresh/match_rec_gap_thresh/match_w_* + settings.fakeflac_min_kbps.
corpus (plan 03-01): INDEX.md pairs each candidate with its manifest + profile + expected decision; known_good_mp3_320 is paired with the mp3_320_cutoff profile and labeled ACCEPT (the QUAL-02 permit-direction case), distinct from below_cutoff_mp3 (DECLINE).

EXISTING firewall + adapters to EDIT:
- app/adapters/base.py: GapItem (lines 17-35); ArrAdapter Protocol (38-71) with get_quality_profile STUBBED at 52-54 returning dict, NO get_manifest yet. Firewall doc-comment lines 1-8.
- app/adapters/lidarr.py: _paged (42-75, GET+envelope+raise_for_status — primary), _map (91-109, normalize-behind-firewall). Auth: self._headers={"X-Api-Key": api_key}; injected httpx.Client (28-36).
- app/adapters/readarr.py: defensive _paged swallow-to-[] (41-83), _map tolerate-both-keys (95-119: rec.get("qualityProfileId") or rec.get("profileId")).
- app/tests/test_adapter_protocol.py: ARR_FIELD_NAMES = re.compile(r"foreignAlbumId|X-Api-Key|records\[|profileId") (line 24); _strip_comment (54-64); test_core_state_have_no_arr_field_names rglobs app/core+app/state *.py/*.sql (67-78); test_both_satisfy_protocol (32-51).

Lidarr quality-profile API (RESEARCH §5 line 349, A4-unconfirmed -> all key access local + .get()-defensive): GET /api/v1/qualityprofile/{id} -> ordered items[] (each allowed:bool, nested quality {id,name}) + cutoff quality id. Manifest source: MB track list via foreign_id.
gate.py composition mirrors detect_gaps (gap_detector.py 23-39). GateResult shape: RESEARCH line 160.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: selector.py (dumb/swappable) + gate.py (compose) + end-to-end test_gate.py</name>
  <read_first>
    - app/core/gap_detector.py (the detect_gaps composition pattern to mirror — lines 23-39 — and SP-2 header)
    - app/core/matching.py, quality.py, fakeflac.py, candidate.py, manifest.py (the functions/types to compose — from plans 03-01/03/04)
    - app/config.py (settings.* tunables from plan 03-02 to build MatchConfig + pass fakeflac_min_kbps)
    - .planning/phases/phase-3/RESEARCH.md line 160 (GateResult shape) + lines 153-161 (selector role) + Pitfall 5 (435-437, matching!=selection)
    - .planning/phases/phase-3/PATTERNS.md selector.py (178-181) + gate.py (183-198) assignments + test_gate.py (237-239)
    - app/tests/fixtures/candidates/INDEX.md (the labeled candidate->manifest->profile->decision map; note known_good_mp3_320 -> mp3_320_cutoff -> ACCEPT)
    - app/tests/test_gap_detector.py (SP-6 end-to-end + FakeAdapter idiom)
  </read_first>
  <behavior>
    - select([]) -> None. select(single accepted) -> that candidate. select(ties on distance) -> tie-broken by format preference then free_upload_slots/upload_speed (the ONLY place those fields are read).
    - evaluate(candidates, manifest, profile, cfg): each candidate is eligible iff quality.gate passes AND fakeflac.check passes; ineligible candidates are excluded with their reason; eligibles are scored, recommend() picks accept/decline (strong+rec-gap), selector picks among accepts; returns GateResult(decision, chosen, distance, reasons).
    - Over the corpus: known_good_flac/known_good_alac/borderline_accept/non_latin -> accept (chosen set); below_cutoff_mp3 declines at the quality stage; fake_flac declines at the fakeflac stage; incomplete_tracks/wrong_album/wrong_edition/garbage_metadata/no_audio_files decline at the match stage; ambiguous_twins decline at rec-gap.
    - QUAL-02 permit direction (e2e): evaluate(known_good_mp3_320, standard_12track manifest, mp3_320_cutoff profile) -> GateResult.decision == "accept" with chosen set — the profile-acceptable lossy candidate passes the quality stage AND matches end-to-end (its ONLY differentiator from the FLAC accept is format, so a non-accept here means the cutoff/rank ladder over-rejects).
  </behavior>
  <action>
    Create app/core/selector.py (SP-2 header): `select(accepted: list[tuple[float, Candidate, list[str]]]) -> Candidate | None` — a small, swappable, dumb tie-break that sorts already-accepted candidates by (distance asc, then a format-preference order e.g. flac>alac>mp3-320, then free_upload_slots desc, then upload_speed desc) and returns the top, or None if empty. This is the ONLY module that reads Candidate.username/upload_speed/free_upload_slots (Pitfall 5). Keep it ~20 lines.
    Create app/core/gate.py (SP-2 header) mirroring detect_gaps composition (gap_detector 23-39). Define a frozen `GateResult(decision: Literal["accept","decline"], chosen: Candidate | None, distance: float, reasons: list[str])` (SP-1, RESEARCH 160). Define `evaluate(candidates, manifest, profile, cfg=None) -> GateResult`: build cfg from config.settings (SP-4 — match_* + fakeflac_min_kbps) when None; for each candidate run quality.gate + fakeflac.check, collect (distance-from matching.score, candidate, reasons) ONLY for eligibles (both pass); sort ascending; call matching.recommend; if accept, call selector.select among the accepted set to pick chosen; assemble GateResult with the full reason trail (eligibility + score + decision). Optional `__main__` is unnecessary (offline phase) — omit it.
    Create app/tests/test_gate.py (SP-6 end-to-end, like test_gap_detector): per-REQ docstring header (MATCH-01/02 + QUAL-02/03 composed) + sandbox note. A FakeAdapter-style local helper supplies the normalized Profile/Manifest from the corpus profiles/ + manifests/ fixtures (no live *arr). Drive evaluate() over every corpus candidate using its INDEX.md-paired manifest + profile; assert each fixture's LABELED GateResult.decision; assert chosen is set on accept and None on decline; assert reasons is non-empty on every result (explainability). Include test_known_good_accepts, test_declines_below_cutoff (quality stage, below_cutoff_mp3 vs lossless_only), test_accepts_mp3_320_when_cutoff_allows (the QUAL-02 PERMIT direction — evaluate(known_good_mp3_320, standard_12track, mp3_320_cutoff) accepts with chosen set; pairs with the reject test to prove both QUAL-02 directions e2e), test_declines_fake_flac (fakeflac stage), test_declines_ambiguous (rec-gap), test_declines_incomplete (track-count), and test_selector_only_reads_uploader_fields (source-level: grep core/ for upload_speed/free_upload_slots/.username and assert they appear ONLY in selector.py).
  </action>
  <verify>
    <automated>cd app && python -m pytest tests/test_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - app/core/gate.py defines frozen `GateResult` and `evaluate(candidates, manifest, profile, cfg=None) -> GateResult`; app/core/selector.py defines `select(accepted) -> Candidate | None`.
    - Eligibility-before-acceptance: a candidate failing quality or fakeflac is excluded before scoring (asserted: a perfect-identity but below-cutoff candidate declines).
    - QUAL-02 both directions e2e: below_cutoff_mp3 vs lossless_only declines at the quality stage AND known_good_mp3_320 vs the mp3_320_cutoff profile is ACCEPTED with chosen set (test_accepts_mp3_320_when_cutoff_allows) — matching the INDEX.md / VALIDATION.md label.
    - Over the corpus, evaluate() returns each fixture's INDEX-labeled decision; chosen set iff accept; reasons non-empty on every result.
    - Source assertion: `grep -rn 'upload_speed\|free_upload_slots\|\.username' app/core/` shows those tokens ONLY in selector.py (matching != selection — Pitfall 5).
    - `cd app && python -m pytest tests/test_gate.py -x` passes (CI/NAS).
  </acceptance_criteria>
  <done>gate.evaluate composes all four gates into a labeled-corpus-correct GateResult (incl. the QUAL-02 permit-direction accept of known_good_mp3_320 against mp3_320_cutoff); selector is the lone swappable uploader-field reader.</done>
</task>

<task type="auto">
  <name>Task 2: Adapter normalization — base.py Profile/Manifest + Protocol, lidarr/readarr get_quality_profile + get_manifest</name>
  <read_first>
    - app/adapters/base.py (the Protocol + GapItem to edit in place; the stubbed get_quality_profile at 52-54; firewall doc-comment 1-8)
    - app/adapters/lidarr.py (_paged 42-75, _map 91-109, auth/client 9-36 — the normalize-behind-firewall idiom + raise_for_status primary policy)
    - app/adapters/readarr.py (defensive _paged 41-83, tolerate-both-keys _map 95-119 — the best-effort idiom)
    - app/core/quality.py (Profile + rank_for from plan 03-04) + app/core/manifest.py (Manifest from plan 03-01)
    - .planning/phases/phase-3/RESEARCH.md §5 line 349 (Lidarr qualityprofile API shape, A4-unconfirmed -> .get()-defensive, keep keys local) + §8 lines 394-397 (get_manifest now or Phase-4-wired) + §9 399-401 (books best-effort)
    - .planning/phases/phase-3/PATTERNS.md base.py (200-210), lidarr.py (212-217), readarr.py (219-222) assignments — SP-3
  </read_first>
  <action>
    Edit app/adapters/base.py: import Profile from core.quality and Manifest from core.manifest (these are neutral types — importing them into adapters is allowed; the firewall forbids *arr names in CORE, not neutral types in adapters). Change the Protocol's get_quality_profile signature from `-> dict` to `-> Profile` (this phase implements it) and add `def get_manifest(self, foreign_id: str) -> Manifest: ...` alongside it, keeping the "declared now" stub style for any still-future methods. Keep the firewall doc-comment accurate (note Profile/Manifest are the normalized shapes that cross).
    Edit app/adapters/lidarr.py: implement get_quality_profile(profile_id) — GET /api/v1/qualityprofile/{id} via the injected client + self._headers, raise_for_status (primary), read the ordered items[] (allowed bool + nested quality) + cutoff id ALL with .get()-defensive access, map each allowed quality name to a neutral QualityRank via core.quality.rank_for (or a name->rank map kept LOCAL to lidarr.py), and return Profile(allowed=frozenset(ranks), cutoff_rank=...). Implement get_manifest(foreign_id) — build Manifest(artist, album, track_count, track_titles, kind="album", year) from the MB/Lidarr album+track data (per RESEARCH §8 it may fetch now or, if MB track-list retrieval is non-trivial, return a Manifest from the album record's available fields with track_titles=None as the graceful path; either keeps Phase 3 offline-provable). ALL *arr/MB field-name access stays inside lidarr.py.
    Edit app/adapters/readarr.py: implement best-effort get_quality_profile + get_manifest mirroring lidarr but with the defensive swallow-to-safe-default stance (SP-3) — a malformed/missing profile or manifest returns a stub-safe value or skips the book rather than crashing; tolerate both profile-id spellings (line 114 idiom). Book Profile is a format ladder (EPUB/MOBI/PDF/AZW3, RESEARCH 352); Manifest(author->artist, title->album, track_count=1, track_titles=None, kind="book"). Books must degrade and never gate music (ARR-02 continues).
  </action>
  <verify>
    <automated>cd app && python -m pytest tests/test_lidarr_adapter.py tests/test_readarr_adapter.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - base.py Protocol declares get_quality_profile(profile_id) -> Profile AND get_manifest(foreign_id) -> Manifest, importing the neutral Profile/Manifest types.
    - lidarr.get_quality_profile returns a `Profile` built from the *arr profile JSON with ALL *arr keys accessed only inside lidarr.py and .get()-defensive (A4-unconfirmed safe); lidarr.get_manifest returns a `Manifest`.
    - readarr.get_quality_profile/get_manifest degrade on malformed/missing input (return safe default or skip) and never raise into the loop (best-effort, ARR-02).
    - New/updated adapter tests assert the normalized return TYPES (isinstance Profile/Manifest) and that a known profile fixture maps to the expected allowed-set + cutoff_rank.
    - `cd app && python -m pytest tests/test_lidarr_adapter.py tests/test_readarr_adapter.py -x` passes (CI/NAS).
  </acceptance_criteria>
  <done>The adapters surface *arr profile + manifest as normalized neutral Profile/Manifest, *arr keys stay local, Readarr stays best-effort.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the locked firewall grep + Protocol conformance test</name>
  <read_first>
    - app/tests/test_adapter_protocol.py (ARR_FIELD_NAMES line 24; _strip_comment 54-64; test_core_state_have_no_arr_field_names 67-78; test_both_satisfy_protocol 32-51)
    - .planning/phases/phase-3/RESEARCH.md Pitfall 2 (lines 420-423, the forbidden tokens to add) + line 195 (firewall MUST extend to new modules)
    - .planning/phases/phase-3/PATTERNS.md test_adapter_protocol.py assignment (lines 241-248) — exact extension point
    - all new app/core/ modules from plans 03-01/03/04 (candidate, manifest, matching, quality, fakeflac, release_parse, selector, gate) — the grep auto-covers them via the existing rglob
  </read_first>
  <action>
    Extend the ARR_FIELD_NAMES regex (test_adapter_protocol.py line 24) to add the Pitfall-2 tokens (RESEARCH 423): `qualityProfileId`, and the profile-JSON shape leaks `items\[`, `"allowed"`, and `cutoff` AS A JSON-KEY pattern. CAUTION: `cutoff_rank` is a legitimate neutral identifier in core/quality.py — craft the `cutoff` pattern to match only the *arr JSON-key usage (e.g. `cutoff"` or `["cutoff"]` or `\bcutoff\b(?!_rank)`), NOT the neutral `cutoff_rank` field, so the test does not false-positive on the legitimate Profile field. Verify by running the test: it must find 0 forbidden tokens across all app/core + app/state modules (the existing rglob already covers the new files). Use the comment-aware `_strip_comment` so doc-mentions in `#` comments don't trip it.
    Extend test_both_satisfy_protocol (lines 32-51) to assert the concrete LidarrAdapter and ReadarrAdapter now expose callable get_quality_profile AND get_manifest (now implemented, not stubbed), keeping the attribute/callable-check approach (not isinstance over the runtime_checkable Protocol, per the Phase-2 decision).
  </action>
  <verify>
    <automated>cd app && python -m pytest tests/test_adapter_protocol.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - ARR_FIELD_NAMES now includes qualityProfileId, items[, "allowed", and a cutoff-JSON-key pattern that does NOT match the neutral `cutoff_rank` identifier.
    - test_core_state_have_no_arr_field_names passes: 0 forbidden *arr tokens across ALL app/core (incl. the 8 new modules) + app/state, comment-stripped.
    - test_both_satisfy_protocol asserts both adapters expose callable get_quality_profile and get_manifest.
    - `cd app && python -m pytest tests/test_adapter_protocol.py -x` passes (the firewall holds over the new modules).
    - Negative control: a deliberate temporary insertion of `qualityProfileId` into a core module would fail the grep (the regex actually matches the new token) — confirmed by the executor, then reverted.
  </acceptance_criteria>
  <done>The locked firewall grep covers the new core modules with 0 forbidden tokens, and Protocol conformance covers the newly-implemented adapter methods.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *arr profile/manifest JSON -> adapter -> core | *arr/MB vocabulary must be normalized away inside adapters; only neutral Profile/Manifest cross into core. |
| Readarr (best-effort) -> core | A book-side fault must not propagate to gate music. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information disclosure | *arr field names leaking into core via gate/selector/adapter edits | mitigate | Extended firewall grep over all 8 new core modules = 0 forbidden tokens; adapters return normalized Profile/Manifest only (RESEARCH Pitfall 2) |
| T-03-10 | Denial of Service | a Readarr profile/manifest fault crashing the music gate | mitigate | readarr.get_quality_profile/get_manifest swallow-to-safe-default (SP-3); books never gate music (ARR-02 continues) |
| T-03-06 | Elevation of privilege | uploader speed/slots bleeding into the match decision via the gate composition | mitigate | gate scores via matching (no uploader fields); selector is the lone reader, applied only after acceptance (Pitfall 5) — asserted by source grep in Task 1 |
| T-03-11 | Tampering | a malformed *arr profile (A4-unconfirmed shape) corrupting the cutoff gate | mitigate | All *arr key access is .get()-defensive inside the adapter; a wrong-guess shape is a localized fix that fails safe (no crash into core) |
</threat_model>

<verification>
- `cd app && python -m pytest tests/test_gate.py tests/test_adapter_protocol.py tests/test_lidarr_adapter.py tests/test_readarr_adapter.py -x` passes (CI/NAS).
- `cd app && python -m pytest tests/ -q` — the full Phase-3 suite plus the Phase-2 suite stays green (no regression).
- E2E QUAL-02 both directions: below_cutoff_mp3 declines at the quality stage AND known_good_mp3_320 accepts against the mp3_320_cutoff profile (test_accepts_mp3_320_when_cutoff_allows).
- Firewall: test_core_state_have_no_arr_field_names = 0 forbidden tokens across the 8 new core modules.
- `grep -rn 'upload_speed\|free_upload_slots\|\.username' app/core/` shows those only in selector.py.
</verification>

<success_criteria>
- gate.evaluate composes matching + quality + fakeflac + selector into a single GateResult that grades the full labeled corpus correctly (MATCH-01/02 + QUAL-02/03 end-to-end, zero false-accepts, and the QUAL-02 permit direction proven via known_good_mp3_320 accepting against mp3_320_cutoff).
- The adapters surface *arr profile + manifest as normalized neutral Profile/Manifest (QUAL-01) with *arr vocabulary confined to app/adapters/.
- The locked firewall grep is extended to and passes over the new core modules; matching!=selection is structurally proven; Readarr stays best-effort.
</success_criteria>

<output>
Create `.planning/phases/phase-3/03-05-SUMMARY.md` when done.
</output>
