---
phase: 04-acquisition-staging-clean-import
plan: 05
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - app/adapters/slskd.py
  - app/adapters/lidarr.py
  - app/tests/fixtures/slskd/transfer_completed.json
  - app/tests/fixtures/slskd/transfer_failed.json
  - app/tests/fixtures/manualimport/expected_post.json
  - .planning/phases/phase-4/04-05-LIVE-PROBE.md
autonomous: false
requirements: [ACQ-02, ACQ-03, IMPORT-01, IMPORT-02]
must_haves:
  truths:
    - "Before any live download, slskd shares are confirmed configured with shared-file count > 0 (D-11 leech-block precondition)"
    - "The exact slskd terminal transfer-state strings (A3) are observed on the live NAS and pinned as the client's named constants"
    - "The exact Lidarr ManualImport POST envelope + importMode casing (A1) is captured and the client payload + fixtures are reconciled to it"
    - "The slskd batchId-vs-remote-folder staging route (A2) is determined live; acquire.py uses whichever the live server honors (fallback documented)"
  artifacts:
    - path: ".planning/phases/phase-4/04-05-LIVE-PROBE.md"
      provides: "recorded live observations: share count, state strings, real ManualImport POST, batchId behavior"
      contains: "## Observed"
  key_links:
    - from: ".planning/phases/phase-4/04-05-LIVE-PROBE.md (A3 observed states)"
      to: "app/adapters/slskd.py terminal-state constants"
      via: "pin the [ASSUMED] constants to the observed strings"
      pattern: "state"
    - from: ".planning/phases/phase-4/04-05-LIVE-PROBE.md (A1 captured POST)"
      to: "app/adapters/lidarr.py execute_import body + expected_post fixture"
      via: "reconcile the payload + fixture to the real envelope"
      pattern: "ManualImport"
---

<objective>
Pin the three load-bearing live assumptions (A1 ManualImport envelope, A2 batchId routing, A3 transfer-state strings) against the real NAS, gated by the D-11 manual-sharing precondition, then reconcile the offline code + fixtures to what the live server actually does.

Purpose: Waves 0-2 are fully proven offline against [ASSUMED] envelopes/strings. Production trust requires confirming the real shapes once. This plan does the minimal live capture and folds the result back into the client + fixtures so the offline suite stays the source of truth — now pinned to reality.
Output: 04-05-LIVE-PROBE.md (the recorded observations) + reconciled slskd terminal-state constants, lidarr execute_import payload, and the affected fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-4/04-CONTEXT.md
@.planning/phases/phase-4/04-RESEARCH.md
@.planning/phases/phase-4/RESEARCH-SEED.md

<interfaces>
<!-- What gets pinned. The offline suite (04-02/03/04) must stay green after reconciliation. -->

A3 (04-RESEARCH §Open Q1): slskd terminal transfer `state` is a compound flag string (e.g. "Completed, Succeeded" / "Completed, Errored" / "Completed, Cancelled"). The [ASSUMED] named constants live in app/adapters/slskd.py — re-pin them to the observed strings.

A1 (04-RESEARCH §Open Q3): the real Lidarr "Manual Import" POST envelope (key casing, whether importMode is "move"/"Move", whether `quality` must echo the full QualityModel, whether albumReleaseId is per-file). Captured via browser DevTools on live Lidarr. Reconcile app/adapters/lidarr.py execute_import + fixtures/manualimport/expected_post.json.

A2 (04-RESEARCH §Open Q2): whether `batchId` is settable on POST /transfers/downloads/{username} (-> downloads/{batchId}/...). If not, acquire.py resolves the actual downloads/{remote-folder}/ subdir post-completion and points manualimport there. Either satisfies IMPORT-01.

D-11 precondition (RESEARCH-SEED.md): slskd.yml shares /data/media/music + /data/media/books; verify GET /api/v0/shares shows shared file count > 0 BEFORE any live download. Phase 4 code does NOT configure shares.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: D-11 + D-12 preconditions — slskd shares + download-dir/path-identity setup, before any live download</name>
  <what-built>Waves 0-2 delivered the full offline-proven acquisition loop. Before the first REAL download against live Soulseek peers, two owner-side setups must hold: (D-11) slskd must be a sharer (not a leecher) or the account gets queue-deprioritized/blocked; and (D-12) the file-movement path must be wired — slskd's DOWNLOAD dir must be inside `/data`, curator's `/data` mount must be read-write, and the *arr must see the staging dir for a hardlink Move. This is all CONFIG/infra (manual) — Curator code does NOT configure shares or download dirs (share automation is Phase 5 / SHARE-01/02).</what-built>
  <how-to-verify>
    Full owner checklist with commands: DEPLOY.md §"Step 8 — Phase 4 setup". Summary:
    0. D-12 PATH SETUP (distinct from shares): slskd.yml `directories.downloads: /data/downloads/soulseek` (+ `incomplete`) — MUST match curator STAGING_ROOT; pre-create `/volume1/data/downloads/soulseek` owned 1031:65536; flip curator `/data` mount `:ro`→read-write in docker-compose.yml and recreate; verify `docker exec lidarr ls /data/downloads/soulseek` succeeds AND Lidarr root folder = /data/media/music (so the ManualImport Move is a hardlink, not a copy).
    Then D-11 SHARES (per RESEARCH-SEED.md "Manual slskd share setup"):
    1. Edit slskd.yml (/volume1/docker/slskd/slskd.yml) to add shares.directories: ["/data/media/music", "/data/media/books"] — the CONTAINER-internal paths, the CLEAN LIBRARY, NOT the download/staging tree. Restart slskd (docker restart slskd).
    2. Verify shared-file count > 0:
       - slskd web UI (http://<NAS-IP>:5030, never http://slskd) → System → Shares shows a non-zero shared file count after the scan; OR
       - API: GET /api/v0/shares with header X-API-Key: $SLSKD_API_KEY returns the directories with a non-zero file count.
    3. Sanity: `docker exec slskd ls /data/media/music` lists real albums (if empty, the mount/path is wrong — fix the mount, not the share config).
    Common failure modes + fixes are in RESEARCH-SEED.md (wrong host path, scan not triggered, PUID/PGID read-permission, PF not applied).
  </how-to-verify>
  <resume-signal>Type "shares confirmed: N files" (N > 0) AND "path setup done" (download-dir + curator rw + *arr sees staging) to proceed to the live probes, or describe the blocker.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Live probe — capture A1 (ManualImport POST), A3 (transfer-state strings), A2 (batchId routing)</name>
  <what-built>The offline suite is pinned to [ASSUMED] envelopes/strings for the three load-bearing live assumptions. This step captures the real shapes once, on the NAS, so production trust is earned. No code runs here — it is observation + recording.</what-built>
  <how-to-verify>
    With shares confirmed (Task 1), capture and record into 04-05-LIVE-PROBE.md:
    A3 (transfer-state strings): trigger ONE real acquisition for a known small gap (or enqueue one download via slskd directly); poll GET /api/v0/transfers/downloads/{username}/{id} and record the exact `state` string at: in-progress, terminal success, and a deliberately-cancelled/failed transfer. Record the literal strings.
    A1 (ManualImport envelope): in the live Lidarr web UI, do ONE "Manual Import" of a folder manually with browser DevTools → Network open; capture the exact POST /api/v1/command request body (key casing, importMode value, whether `quality` is the full QualityModel object, whether albumReleaseId is per-file). Save the captured JSON verbatim.
    A2 (batchId): enqueue a download (via the live slskd) with a batchId set on the body; observe whether the file lands in downloads/{batchId}/... If batchId is ignored, record the actual landing path (downloads/{remote-folder}/...) so acquire.py uses the resolve-post-completion fallback.
    Record all observations under a "## Observed" section in 04-05-LIVE-PROBE.md.
  </how-to-verify>
  <resume-signal>Type "probe recorded" once 04-05-LIVE-PROBE.md has the observed state strings, the captured ManualImport POST, and the batchId result — or "skip A2/fallback" if batchId is not honored.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Reconcile the offline code + fixtures to the live observations; keep the suite green</name>
  <read_first>
    - .planning/phases/phase-4/04-05-LIVE-PROBE.md (the observations recorded in Task 2 — the source of truth for this reconciliation)
    - app/adapters/slskd.py (the [ASSUMED] terminal-state named constants to re-pin to the observed A3 strings)
    - app/adapters/lidarr.py execute_import (the payload to reconcile to the captured A1 envelope: importMode casing, quality echo, albumReleaseId placement)
    - app/tests/fixtures/slskd/transfer_completed.json + transfer_failed.json (re-pin the state strings to A3)
    - app/tests/fixtures/manualimport/expected_post.json (reconcile to the captured A1 body)
    - app/core/acquire.py (only if A2 forces the remote-folder-resolution fallback for the staging path — adjust the staging-path resolution accordingly, keeping the firewall clean)
  </read_first>
  <action>
    Using ONLY what 04-05-LIVE-PROBE.md recorded:
    - A3: replace the [ASSUMED] terminal-state constant values in app/adapters/slskd.py with the observed literal strings; update transfer_completed.json / transfer_failed.json fixtures to the observed `state` values so the offline tests assert against reality.
    - A1: reconcile app/adapters/lidarr.py execute_import to the captured envelope (fix importMode casing if needed, echo the full quality QualityModel if the real POST requires it, place albumReleaseId per-file or top-level as observed) and update fixtures/manualimport/expected_post.json to match; keep all *arr field names inside lidarr.py (firewall).
    - A2: if batchId is NOT honored, adjust acquire.py's staging-path step to resolve the actual downloads/{remote-folder}/ subdir after completion (list the staging root or read the slskd-reported local path) and point manual_import_candidates at that resolved path — keep acquire.py firewall-clean (no wire vocabulary). If batchId IS honored, no change.
    Re-run the full offline suite; every test must stay green against the now-real strings/envelope. Do NOT weaken any assertion — update the fixtures/constants to truth, then ensure the tests pass.
  </action>
  <acceptance_criteria>
    - source: slskd.py terminal-state constants == the observed A3 strings; lidarr.py execute_import + expected_post.json == the captured A1 envelope; if A2 fallback was needed, acquire.py resolves the real staging subdir and stays firewall-clean.
    - behavior: the offline suite (test_slskd_client, test_lidarr_adapter, test_acquire, the firewall grep) passes against the reconciled real values; no assertion was weakened (fixtures/constants moved to truth instead).
    - test-command: `cd app && python3 -m pytest -q` green; firewall grep over acquire.py still reports zero wire-vocabulary tokens.
  </acceptance_criteria>
  <verify>
    <automated>cd app && python3 -m pytest -q</automated>
  </verify>
  <done>The offline suite is pinned to the live-observed transfer-state strings + ManualImport envelope (and the batchId/remote-folder staging route is settled), so the acquisition loop is production-trustworthy with the full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Soulseek peers → first real download | the first untrusted-peer transfer + the leech-block risk the D-11 share precondition mitigates |
| live Lidarr → captured POST envelope | the real import command shape crosses into the client design here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | DoS (availability) | zero-share account leech-block | mitigate | D-11 blocking-human precondition: confirm shared-file count > 0 before ANY live download (Task 1 gate) |
| T-04-18 | Tampering | wrong ManualImport envelope imports nothing/wrong release | mitigate | A1 live DevTools capture pins the exact POST before production trust; offline fixtures reconciled to it (Task 3) |
| T-04-19 | Tampering | wrong terminal-state check fires import early or never | mitigate | A3 live probe pins the exact `state` strings into the client constants + fixtures (Task 3) |
| T-04-SC | Tampering | pip installs | accept | No new package. No install task in this plan. |
</threat_model>

<verification>
- Task 1 share-count > 0 confirmed before any live download (blocking-human).
- 04-05-LIVE-PROBE.md "## Observed" has the A3 state strings, the A1 captured POST, and the A2 batchId result.
- `cd app && python3 -m pytest -q` green against the reconciled real values; firewall grep over acquire.py clean.
</verification>

<success_criteria>
The D-11 leech-block precondition is satisfied, the three load-bearing live assumptions (A1/A2/A3) are observed on the NAS and recorded, and the offline suite + client constants + fixtures are reconciled to reality with no weakened assertions — making the Phase-4 acquisition loop production-trustworthy.
</success_criteria>

<output>
Create `.planning/phases/phase-4/04-05-SUMMARY.md` when done (and 04-05-LIVE-PROBE.md during Task 2).
</output>
