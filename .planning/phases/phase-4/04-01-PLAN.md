---
phase: 04-acquisition-staging-clean-import
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/state/migration_0002.sql
  - app/state/db.py
  - app/state/repo.py
  - app/config.py
  - app/tests/conftest.py
  - app/tests/fixtures/slskd/search_responses.json
  - app/tests/fixtures/slskd/transfer_completed.json
  - app/tests/fixtures/slskd/transfer_stalled.json
  - app/tests/fixtures/slskd/transfer_failed.json
  - app/tests/fixtures/manualimport/get_mapping.json
  - app/tests/fixtures/manualimport/expected_post.json
  - app/tests/test_state_repo.py
autonomous: true
requirements: [IMPORT-05, ACQ-02]
must_haves:
  truths:
    - "The ledger accepts the new acquisition lifecycle statuses (downloading/importing/imported/quarantined/stuck) without an IntegrityError"
    - "A staged_files row is recorded when a download begins and updated with quarantine path + reason on failure"
    - "Phase-4 tunables (slskd url/key, stall/search-window/poll seconds, staging+quarantine roots, quarantine TTL, plex url/token/section) are env-overridable with sane defaults"
    - "Offline slskd + manualimport fixtures exist so later-wave adapter/orchestration tests run with no live services"
  artifacts:
    - path: "app/state/migration_0002.sql"
      provides: "widened items.status CHECK enum + staged_files table"
      contains: "CREATE TABLE IF NOT EXISTS staged_files"
    - path: "app/state/repo.py"
      provides: "record_staged_file + record_quarantine DAOs"
      contains: "def record_staged_file"
    - path: "app/config.py"
      provides: "Phase-4 acquisition tunables on Settings.from_env()"
      contains: "acq_stall_seconds"
    - path: "app/tests/fixtures/slskd/search_responses.json"
      provides: "canned slskd search-responses envelope for from_slskd + gate"
  key_links:
    - from: "app/state/db.py"
      to: "app/state/migration_0002.sql"
      via: "MIGRATIONS append (0002, _SCHEMA_0002)"
      pattern: "_SCHEMA_0002"
    - from: "app/state/repo.py set_status"
      to: "items.status widened enum"
      via: "set_status writes 'downloading'/'quarantined'/'stuck' without IntegrityError"
      pattern: "record_staged_file"
---

<objective>
Lay the Phase-4 foundation so later waves compose on a tested base: widen the SQLite acquisition state machine, add the staged_files ledger surface, register the Phase-4 config tunables, and land the offline fixtures every later-wave test depends on.

Purpose: The acquisition loop (04-04) writes new lifecycle statuses and staged_files rows; the slskd client + adapters (04-02/04-03) read tunables and assert against fixtures. All of that must exist and be test-proven first so no later task hits an IntegrityError or a missing-fixture wall.
Output: migration_0002 (enum widen + staged_files), two new repo DAOs, the Phase-4 Settings block, and the slskd/manualimport offline fixtures + conftest wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-4/04-CONTEXT.md
@.planning/phases/phase-4/04-RESEARCH.md
@.planning/phases/phase-4/04-PATTERNS.md

<interfaces>
<!-- Existing surfaces this plan extends. Use directly — no codebase exploration needed. -->

From app/state/db.py (the migration runner — append-only, NEVER reorder shipped tuples):
- _SCHEMA_0001 = read schema.sql; MIGRATIONS = [("0001", _SCHEMA_0001)]
- run_migrations(conn): applies any migration whose 1-based index > PRAGMA user_version, each in one BEGIN/COMMIT.
- _split_statements(sql): strips `--` comments then splits on top-level `;` — so DO NOT embed a `;` inside a string literal in the .sql.

From app/state/schema.sql (migration 0001 — the items table; status CHECK enum):
- status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','searching','grabbed','downloaded','imported','unavailable','blacklisted'))
- UNIQUE (arr_app, arr_id); idx_items_status ON items(status)

From app/state/repo.py (DAO style — `?` placeholders ONLY, _now_iso() for timestamps):
- set_status(conn, arr_app, arr_id, status): UPDATE items SET status=? WHERE arr_app=? AND arr_id=?  (already exists; new enum values work for free once migration 0002 widens the CHECK)
- get_gap(conn, ...), list_by_status(conn, status): existing readers
- _now_iso(): ISO timestamp helper (repo.py)

From app/config.py (frozen dataclass Settings + from_env() reading env NOW):
- existing fields: lidarr_url/api_key, readarr_url/api_key, db_path, the 7 Phase-3 match_* / fakeflac_min_kbps tunables.
- numeric tunables cast via float()/int(os.getenv(...,default)) — fail-fast ValueError on bad value.

From app/tests/conftest.py (load_fixture loads JSON from app/tests/fixtures/; tmp_db_path from tmp_path):
- load_fixture(name) reads app/tests/fixtures/{name}.json; tmp_db_path returns a throwaway sqlite path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0002 — widen status enum + staged_files table + register in db.py</name>
  <read_first>
    - app/state/schema.sql (migration 0001 DDL style: IF NOT EXISTS, inline `--` comments, the items status CHECK enum to widen)
    - app/state/db.py lines 14-22 (MIGRATIONS append pattern) and _split_statements (no `;` inside string literals)
    - 04-PATTERNS.md §"app/state/migration_0002.sql" (the table-rebuild approach for widening a CHECK) and §"app/state/db.py"
    - 04-RESEARCH.md §"Pitfall 6" (re-detect upsert must not clobber in-flight status — widen the enum so set_status('downloading') never raises IntegrityError)
  </read_first>
  <action>
    Create app/state/migration_0002.sql. It must do two things inside the single transaction the runner wraps it in (Pitfall 6 — the enum widen comes FIRST so the status-preserving upsert protects the new states for free):
    (1) Widen the items.status CHECK enum to add 'downloading','importing','quarantined','stuck' (in ADDITION to the existing pending/searching/grabbed/downloaded/imported/unavailable/blacklisted). SQLite has no ALTER COLUMN, so do the standard table-rebuild: ALTER TABLE items RENAME TO items_old; recreate items with the full widened CHECK (copy ALL columns + constraints from schema.sql verbatim, only the CHECK list changes); INSERT INTO items SELECT * FROM items_old; DROP TABLE items_old; recreate idx_items_status (and any other index 0001 defined) with IF NOT EXISTS. Match 0001's comment style. Do NOT embed a `;` inside any string literal (the runner splits on `;`).
    (2) Add the staged_files table per 04-PATTERNS.md: CREATE TABLE IF NOT EXISTS staged_files (id INTEGER PRIMARY KEY, item_id INTEGER NOT NULL REFERENCES items(id), staging_path TEXT NOT NULL, quarantine_path TEXT, failure_reason TEXT, quarantined_at TEXT, created_at TEXT NOT NULL).
    Then edit app/state/db.py: add `_SCHEMA_0002 = (Path(__file__).parent / "migration_0002.sql").read_text(encoding="utf-8")` and append `("0002", _SCHEMA_0002)` to MIGRATIONS (NEVER reorder 0001). The 1-based index 2 IS the user_version this bumps to.
    Verify the rebuild preserves the existing UNIQUE(arr_app, arr_id) constraint and column order so `INSERT INTO items SELECT * FROM items_old` aligns.
  </action>
  <acceptance_criteria>
    - source: migration_0002.sql contains both the items rebuild (widened CHECK with all 11 status values) and `CREATE TABLE IF NOT EXISTS staged_files`; db.py MIGRATIONS ends with `("0002", _SCHEMA_0002)` and 0001 is untouched.
    - behavior: on a fresh DB, run_migrations bumps user_version to 2; set_status(conn, app, id, 'downloading') and 'quarantined' and 'stuck' all succeed with no sqlite3.IntegrityError; existing Phase-2 rows survive the rebuild (count + identity preserved).
    - behavior: run_migrations is idempotent — a second call on a v2 DB applies nothing and leaves user_version=2.
    - test-command: `cd app && python3 -m pytest tests/test_state_repo.py -x -q` (the new migration/enum tests added in Task 3) passes.
  </acceptance_criteria>
  <verify>
    <automated>cd app && python3 -m pytest tests/test_state_repo.py -x -q</automated>
  </verify>
  <done>migration_0002 widens the status enum and adds staged_files; db.py registers it; existing rows survive the rebuild; the new statuses write without IntegrityError; the runner is idempotent to v2.</done>
</task>

<task type="auto">
  <name>Task 2: staged_files DAOs in repo.py + Phase-4 tunables in config.py</name>
  <read_first>
    - app/state/repo.py (existing DAO style: `?` placeholders only, sqlite3.Connection first arg, _now_iso() for timestamps; set_status to be reused with new enum values)
    - app/config.py (frozen Settings dataclass + from_env() reading env NOW; the Phase-3 float()/int() cast-or-fail pattern)
    - 04-PATTERNS.md §"app/state/repo.py" (record_staged_file / record_quarantine signatures) and §"app/config.py" (the exact Phase-4 field list + from_env lines + defaults)
    - 04-RESEARCH.md §"Runtime State Inventory" (SLSKD_URL/SLSKD_API_KEY already exist in Phase 1 env; NO Plex env — revised D-04, Curator does not call Plex)
  </read_first>
  <action>
    Add two DAOs to app/state/repo.py using ONLY `?` placeholders and _now_iso():
    - record_staged_file(conn, item_id: int, staging_path: str) -> int: INSERT INTO staged_files (item_id, staging_path, created_at) VALUES (?,?,?) using _now_iso(); return cur.lastrowid. Called when a download starts (D-05/D-06 lifecycle anchor).
    - record_quarantine(conn, staged_file_id: int, quarantine_path: str, reason: str) -> None: UPDATE staged_files SET quarantine_path=?, failure_reason=?, quarantined_at=? WHERE id=? using _now_iso() (D-06).
    Extend app/config.py Settings (keep @dataclass(frozen=True)) with the Phase-4 fields (per 04-PATTERNS.md §config.py, EXCLUDING the Plex fields — revised D-04 dropped Plex), and add the matching os.getenv reads to from_env(): slskd_url (default http://localhost:5030), slskd_api_key (Optional, None), acq_search_window_seconds (float, 12.0 — D-07 Claude's-discretion default), acq_stall_seconds (float, 600.0 — D-01 ~10min), acq_poll_seconds (float, 5.0), staging_root (str, /data/downloads/soulseek), quarantine_root (str, /data/downloads/soulseek/.quarantine), quarantine_ttl_seconds (float, 604800.0 — D-06 ~7d). Do NOT add plex_url/plex_token/plex_section_id (Curator does not call Plex). Cast every numeric via float()/int(os.getenv(...,default)) so a bad operator value fails fast (Phase-3 precedent). Tokens/keys stay Optional[str]=None — never baked, never logged.
  </action>
  <acceptance_criteria>
    - source: repo.py has record_staged_file + record_quarantine, both `?`-placeholder only (no f-string into SQL), both using _now_iso(); config.py Settings carries all 8 Phase-4 fields (no Plex fields) and from_env() reads each from the documented env name.
    - behavior: record_staged_file returns a positive rowid; record_quarantine sets quarantine_path/failure_reason/quarantined_at on that row.
    - behavior: Settings.from_env() with no env returns the documented defaults; setting ACQ_STALL_SECONDS=120 then from_env() yields acq_stall_seconds==120.0; a non-numeric ACQ_STALL_SECONDS raises ValueError at from_env() time.
    - test-command: `cd app && python3 -m pytest tests/test_state_repo.py -x -q` passes (DAO tests added in Task 3); `cd app && python3 -c "import config; config.Settings.from_env()"` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd app && python3 -m pytest tests/test_state_repo.py -x -q</automated>
  </verify>
  <done>Two `?`-safe staged_files DAOs exist; Settings carries the 8 env-overridable Phase-4 tunables (no Plex fields) with sane defaults and fail-fast numeric casts.</done>
</task>

<task type="auto">
  <name>Task 3: Offline fixtures + conftest wiring + migration/DAO/config tests</name>
  <read_first>
    - app/tests/conftest.py (load_fixture loader + tmp_db_path; how fixtures are loaded from app/tests/fixtures/)
    - app/tests/test_state_repo.py (existing tmp_db_path + VALID_STATUSES + _gap() stand-in + migration test style to extend)
    - app/tests/fixtures/lidarr_missing.json (the existing *arr fixture shape to mirror)
    - app/core/candidate.py from_slskd / build_candidate (the slskd search-response shape the fixture must satisfy: username, hasFreeUploadSlot, uploadSpeed, queueLength, files:[{filename,size,extension,bitRate,bitDepth,length,sampleRate}])
    - 04-RESEARCH.md §"Pattern 1" (slskd search_responses field names) and §"Pattern 3" (ManualImportResource fields: path,name,artist,album,albumReleaseId,tracks,quality,rejections)
    - 04-VALIDATION.md §"Wave 0 Requirements" (the fixtures this plan must land)
  </read_first>
  <action>
    Create the offline fixtures under app/tests/fixtures/ that later waves assert against (keep Phase 4 fully offline-provable):
    - slskd/search_responses.json: a list of >=2 SearchResponseItem dicts (one clean FLAC album that gate.evaluate will accept, one weaker candidate) shaped so Candidate.from_slskd consumes them directly — each with username, hasFreeUploadSlot, uploadSpeed, queueLength, files[] carrying filename (a path with a folder), size, extension, bitRate, bitDepth, length, sampleRate.
    - slskd/transfer_completed.json, slskd/transfer_stalled.json, slskd/transfer_failed.json: single transfer dicts carrying state + bytesTransferred + size + percentComplete, representing terminal success, a no-byte-progress snapshot, and a failed/errored snapshot respectively. Use plausible compound state strings; mark them clearly in a sibling note as [ASSUMED] pending the 04-05 live probe (A3) — later-wave tests must reference state via named constants so the live strings can be pinned without touching every test.
    - manualimport/get_mapping.json: a list of ManualImportResource-shaped dicts (path, name, artist{id}, album{id}, albumReleaseId, tracks[{id}], quality, rejections[]) — at least one importable (empty rejections, real tracks) and one rejected (non-empty rejections) so the wanted-file filter is testable.
    - manualimport/expected_post.json: the expected ManualImport command body Curator should POST for the importable file(s) — name=ManualImport, importMode, files[] with path/artistId/albumId/albumReleaseId/trackIds/quality. Mark importMode casing + envelope as [ASSUMED] pending 04-05 (A1).
    Wire conftest.py only if a new shared fixture/helper is needed to load the slskd/manualimport subdirs (mirror load_fixture; prefer reusing it with a subpath like load_fixture("slskd/transfer_completed")).
    Add tests to app/tests/test_state_repo.py proving Task-1 + Task-2 behavior: fresh-DB migrate→user_version 2; set_status with each new enum value succeeds; pre-existing rows survive the rebuild; record_staged_file/record_quarantine roundtrip; idempotent re-migrate. Config from_env default/override/fail-fast assertions live in this plan's files (extend test_state_repo or add a small test block — keep it within this plan's files_modified set).
  </action>
  <acceptance_criteria>
    - source: the six fixture JSON files exist and are valid JSON; slskd/search_responses.json entries satisfy Candidate.from_slskd's expected keys; manualimport/get_mapping.json has both an importable and a rejected resource.
    - behavior: a test loads search_responses.json and builds >=2 Candidates via from_slskd with no KeyError; a test loads get_mapping.json and filters to exactly the importable resource(s).
    - behavior: test_state_repo proves migrate→v2, the three new statuses write cleanly, prior rows survive the table rebuild, and the staged_files DAOs roundtrip.
    - test-command: `cd app && python3 -m pytest tests/test_state_repo.py -x -q` is green and the full `cd app && python3 -m pytest -q` shows no regression (was 128 passing).
  </acceptance_criteria>
  <verify>
    <automated>cd app && python3 -m pytest -q</automated>
  </verify>
  <done>The offline slskd + manualimport fixtures exist and parse; conftest loads them; test_state_repo proves the migration, the widened enum, the staged_files DAOs, and the config tunables; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → Settings | SLSKD_API_KEY crosses here as a secret — read-only, never logged/baked |
| ledger writes → SQLite | peer-influenced strings (staging_path derived from peer filenames in later waves) reach the staged_files DAO |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | staged_files DAO (repo.py) | mitigate | All writes use `?` placeholders only (repo.py discipline) — no peer string is ever f-stringed into SQL |
| T-04-02 | Information Disclosure | config.py secrets (slskd_api_key) | mitigate | Stored as Optional[str], read from env only, never logged; mirrors existing Settings T-02-01 |
| T-04-03 | Tampering/DoS | migration 0002 items rebuild | mitigate | Rebuild runs inside the runner's single BEGIN/COMMIT (db.py WR-02) so a mid-migration crash rolls back wholly; existing rows copied via SELECT *, identity-preserved |
| T-04-SC | Tampering | pip installs (respx) | accept | No new package — respx==0.22.0 already pinned + human-approved (Phase 2 checkpoint). No install task in this phase. |
</threat_model>

<verification>
- `cd app && python3 -m pytest -q` green (no regression from the prior 128).
- `cd app && python3 -c "import config; config.Settings.from_env()"` succeeds.
- A fresh tmp DB migrates to user_version 2 and accepts all 11 status values.
</verification>

<success_criteria>
Migration 0002 widens the acquisition state machine and adds staged_files; repo exposes the two staged_files DAOs; Settings carries the env-overridable Phase-4 tunables; the offline slskd + manualimport fixtures exist so 04-02/03/04 are provable with no live services. No new runtime dependency.
</success_criteria>

<output>
Create `.planning/phases/phase-4/04-01-SUMMARY.md` when done.
</output>
